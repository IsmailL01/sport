---
phase: 05-observability-backend
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - infra/ansible/roles/sentry-prep/tasks/main.yml
  - infra/ansible/roles/sentry-prep/tasks/create_projects.yml
  - infra/ansible/roles/sentry-prep/tasks/install_alerter.yml
  - infra/ansible/roles/sentry-prep/templates/sentry-stack.service.j2
  - infra/ansible/roles/sentry-prep/templates/alerter.service.j2
  - infra/ansible/roles/sentry-prep/files/alerter/main.go
  - infra/ansible/roles/sentry-prep/files/alerter/go.mod
  - infra/ansible/roles/sentry-prep/files/alerter/main_test.go
  - .secrets/prod/sentry.yaml
  - .secrets/staging/sentry.yaml
  - scripts/smoke_sentry.py
  - docs/RUNBOOKS/sentry-ops.md
autonomous: false
requirements: [OBS-01, OBS-02]
user_setup:
  - service: "Telegram BotFather"
    why: "OBS alert routing per D-24 (Telegram bot, NOT PagerDuty — $40/mo overkill for 2-dev team)"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "BotFather DM `/newbot` flow returns token string"
      - name: TELEGRAM_CHAT_ID
        source: "Send any message to bot, then GET https://api.telegram.org/bot<TOKEN>/getUpdates — chat.id field in JSON response"
    dashboard_config:
      - task: "Create new bot via @BotFather: send /newbot → name `running-ecosystem-alerts` → username `running_ecosystem_alerts_bot` (must end in `_bot`)"
        location: "Telegram → @BotFather DM"
      - task: "Create new private group `Running Ecosystem Alerts`, add the bot as admin, send 1 test message, retrieve numeric chat_id"
        location: "Telegram client"
  - service: "Sentry self-hosted admin"
    why: "OBS-02 requires 4 projects (prod-backend, staging-backend, prod-mobile, staging-mobile) per D-06"
    env_vars:
      - name: SENTRY_ADMIN_EMAIL
        source: "User-chosen — likely same as caddy_acme_email"
      - name: SENTRY_ADMIN_PASSWORD
        source: "10-char random generated offline (per D-07 + Phase 2 SEC-02 discipline — NEVER pasted in chat)"

must_haves:
  truths:
    - "Sentry self-hosted v26.5.0 (per RESEARCH §1.1) reachable at https://sentry.<sentry-ip-dashed>.sslip.io with admin login working"
    - "4 Sentry projects exist with slugs: prod-backend, staging-backend, prod-mobile, staging-mobile (per D-06)"
    - "SOPS slot .secrets/prod/sentry.yaml contains 4 DSNs + SENTRY_ADMIN_PASSWORD + SENTRY_AUTH_TOKEN + TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID"
    - "Telegram alerter Go service running on sentry VPS at 127.0.0.1:9099, rate-limited to 25 msg/sec (RESEARCH §1.5 — under Telegram's 30/sec cap)"
    - "Sentry's Generic Webhook integration points at alerter on 127.0.0.1:9099 for prod-backend project — synthetic alert reaches Telegram chat"
    - "smoke_sentry.py POST envelope → 200 + event visible in Sentry UI within 30s"
  artifacts:
    - path: ".secrets/prod/sentry.yaml"
      provides: "SENTRY_DSN_BACKEND, SENTRY_DSN_MOBILE, SENTRY_DSN_BACKEND_STAGING, SENTRY_DSN_MOBILE_STAGING, SENTRY_ADMIN_PASSWORD, SENTRY_AUTH_TOKEN, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID — SOPS-encrypted"
      contains: "SENTRY_DSN_BACKEND: ENC["
    - path: ".secrets/staging/sentry.yaml"
      provides: "Same key set as prod/sentry.yaml; staging values may be empty placeholders (staging env lands v1.1 per Phase 3 D-23)"
      contains: "SENTRY_DSN_BACKEND_STAGING:"
    - path: "infra/ansible/roles/sentry-prep/files/alerter/main.go"
      provides: "Tiny Go HTTP server: /sentry-hook + /grafana-hook endpoints, token bucket 25 msg/sec, POST to api.telegram.org/bot<TOKEN>/sendMessage (RESEARCH §1.5)"
      min_lines: 80
    - path: "infra/ansible/roles/sentry-prep/templates/sentry-stack.service.j2"
      provides: "systemd unit running `docker compose -f /opt/sentry/docker-compose.yml up` (D-31 — separate from sport-stack.service)"
      contains: "ExecStart=/usr/bin/docker compose"
    - path: "scripts/smoke_sentry.py"
      provides: "POST synthetic envelope to <DSN>/api/<project_id>/envelope/ then GET via Sentry API to verify event ingested"
      min_lines: 60
  key_links:
    - from: "Sentry prod-backend project webhook"
      to: "127.0.0.1:9099/sentry-hook"
      via: "Generic Webhook integration configured via sentry-cli or admin UI"
      pattern: "Generic Webhook"
    - from: "infra/ansible/roles/sentry-prep/files/alerter/main.go"
      to: "https://api.telegram.org/bot<TOKEN>/sendMessage"
      via: "Token-bucket-rate-limited HTTP POST per RESEARCH §1.5"
      pattern: "sendMessage"
    - from: ".secrets/prod/sentry.yaml"
      to: "App services' env (Plans 05-03 / 05-04 / 05-05 consume DSN)"
      via: "Existing pkg/envrequire + SOPS-decrypt pattern from Phase 3"
      pattern: "SENTRY_DSN_BACKEND"
---

<objective>
Install Sentry self-hosted v26.5.0 on the VPS that Plan 05-01 provisioned, create the 4 Sentry projects (per D-06), populate the SOPS slot with all 4 DSNs + admin password + auth token + Telegram bot credentials, deploy a tiny Go alerter service (~80 LOC per RESEARCH §1.5) on sentry VPS that rate-limits Sentry+Grafana webhook fan-in to 25 msg/sec (under Telegram's 30/sec cap), and verify end-to-end: synthetic Sentry alert → alerter → Telegram chat (USER ACTION 3 confirms message receipt).

Purpose: OBS-01 production usage (the install + admin + Caddy proxy chain works) + OBS-02 (4 projects exist with DSNs in SOPS). Without DSNs, Plans 05-03 (slog handler) and 05-05 (OTel + sentry-go SDK init) cannot be wired. Without the Telegram alerter, ROADMAP §Phase 5 criterion 8 (alert rules → Telegram chat) cannot pass.

Output: A fully-operational Sentry instance with 4 projects, DSNs in SOPS, and a verified Sentry-to-Telegram alert path. Plan 05-03/04/05 consume the prod-backend DSN (single project per RESEARCH §1.9, services discriminated via `setTag("service", ...)`). Plan 05-06 wires the final Loki + Alloy log-shipping leg to the same sentry VPS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-observability-backend/05-CONTEXT.md
@.planning/phases/05-observability-backend/05-RESEARCH.md
@.planning/phases/05-observability-backend/05-VALIDATION.md
@.planning/phases/05-observability-backend/05-PATTERNS.md
@.planning/phases/05-observability-backend/05-01-SUMMARY.md
@.planning/phases/02-secrets-and-config-hardening/02-04-SUMMARY.md
@.planning/phases/03-infrastructure-as-code/03-03-SUMMARY.md

# Source files the executor reads to copy patterns:
@infra/ansible/roles/sport-stack/templates/sport-stack.service.j2
@infra/ansible/roles/sport-stack/tasks/decrypt_sops.yml
@.secrets/prod/mapbox.yaml
@services/backend/scripts/smoke_otp.py
@docs/RUNBOOKS/sops-edit.md
@infra/ansible/roles/sentry-prep/tasks/main.yml

<interfaces>
<!-- Sentry CLI commands the executor will invoke (RESEARCH §1.2) -->

sentry-cli (binary pinned at v2.40.0 per RESEARCH §4):
  - sentry-cli login --auth-token <SENTRY_AUTH_TOKEN>     # configures CLI for subsequent calls
  - sentry-cli projects create -o sentry <slug>           # idempotent: 409 on re-run, OK
  - sentry-cli projects list -o sentry --json             # output schema: [{slug, name, id, ...}]

Sentry create-admin (one-time, in container):
  - docker compose -f /opt/sentry/docker-compose.yml run --rm web createuser \
        --email <email> --password <password> --superuser --no-input

Sentry envelope POST endpoint (smoke_sentry.py target):
  - POST https://sentry.<host>/api/<project_id>/envelope/
    Headers: X-Sentry-Auth: Sentry sentry_version=7, sentry_key=<public_key>
    Body: NDJSON envelope (header + event item)

Telegram Bot API endpoints (alerter Go service POSTs here):
  - POST https://api.telegram.org/bot<TOKEN>/sendMessage
    Body: {"chat_id": <CHAT_ID>, "text": "...", "parse_mode": "Markdown"}
  - Global rate limit: 30 msg/sec; per-chat: 1 msg/sec (RESEARCH §1.5 + Telegram docs)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: USER ACTION 2 — BotFather creates @running_ecosystem_alerts_bot + share token + chat_id</name>
  <what-needed>
    User opens Telegram and:
    1. DM `@BotFather` → `/newbot` → name `Running Ecosystem Alerts` → username `running_ecosystem_alerts_bot` (must end in `_bot`)
    2. Receive bot token (format `<digits>:<alphanumeric>`)
    3. Create a new private group titled `Running Ecosystem Alerts`, add the bot as admin (Group Settings → Administrators → Add `@running_ecosystem_alerts_bot`)
    4. Send any message to the group (e.g., "hello")
    5. From dev workstation: `curl -s "https://api.telegram.org/bot<TOKEN>/getUpdates" | jq '.result[-1].message.chat.id'` → returns numeric chat_id (negative for groups, e.g., `-1001234567890`)
    6. Disable "Privacy Mode" for the bot via BotFather → `/setprivacy` → choose `running_ecosystem_alerts_bot` → Disable (so bot can read group messages if future Grafana commands hit it)

    **DO NOT paste token in chat per Phase 2 SEC-02 discipline.** Provide via direct file write or interactive prompt during Task 3.
  </what-needed>
  <resume-signal>Confirm: (a) bot token retrieved offline; (b) chat_id retrieved; (c) you are ready to populate SOPS slot via `sops edit .secrets/prod/sentry.yaml` (Task 3 walks you through this).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Wave-0 — scripts/smoke_sentry.py + .secrets/{prod,staging}/sentry.yaml SOPS placeholders</name>
  <files>
    scripts/smoke_sentry.py,
    .secrets/prod/sentry.yaml,
    .secrets/staging/sentry.yaml
  </files>
  <read_first>
    - services/backend/scripts/smoke_otp.py (urllib + must() helper pattern per PATTERNS.md `scripts/smoke_sentry.py` analog)
    - .secrets/prod/mapbox.yaml (SOPS slot shape — KEY: ENC[...] lines + age recipient block per PATTERNS.md `.secrets/prod/sentry.yaml` analog)
    - .sops.yaml (recipient list — sentry.yaml inherits same recipients as mapbox.yaml)
    - docs/RUNBOOKS/sops-edit.md (sops --encrypt workflow + SOPS_AGE_KEY_FILE env construct)
    - .planning/phases/05-observability-backend/05-RESEARCH.md §1.2 (sentry-cli projects create + DSN extraction) + §1.3 (envelope endpoint path + X-Sentry-Auth header format)
    - .planning/phases/05-observability-backend/05-PATTERNS.md "Python smoke / Bash audit scripts" + ".secrets/prod/sentry.yaml" sections
  </read_first>
  <action>
    Create `scripts/smoke_sentry.py` per RESEARCH §1.3 + PATTERNS.md analog:
    - stdlib-only (urllib.request + json + os + uuid + sys + time)
    - `BASE` resolved from `SENTRY_DSN_BACKEND` env (parse via urllib.parse — host = endpoint, user = public_key, path stripped = project_id; matches `pkg/observability/otel_init.go` parse code in RESEARCH §1.3)
    - `must()` helper following smoke_otp.py shape: prints `✓ <name> → <status>` on success, `❌ <name>: expected <X>, got <Y>: <body>` then `sys.exit(1)` on failure
    - Two stages:
      1. POST synthetic envelope to `<endpoint>/api/<project_id>/envelope/` with `X-Sentry-Auth: Sentry sentry_version=7, sentry_key=<public_key>` header; body = NDJSON 2-line envelope (header `{"event_id":"<uuid>","sent_at":"<iso>"}` + item header `{"type":"event"}` + item body `{"event_id":"<uuid>","platform":"go","message":{"formatted":"smoke_sentry.py synthetic"}}`); `must` 200.
      2. Sleep 5s; GET `<endpoint>/api/0/organizations/sentry/events/<event_id>/` with `Authorization: Bearer <SENTRY_AUTH_TOKEN>` header; `must` 200 + JSON body contains the event_id.
    - Exit 0 with `🎉 smoke_sentry.py passed.` on success.
    - Header comment per PATTERNS.md: `#!/usr/bin/env python3` + `# scripts/smoke_sentry.py — Phase 5 / OBS-01+02 smoke (RESEARCH §1.3 envelope round-trip)` + idempotency note ("safe to re-run; creates one synthetic event per run").
    - Minimum 60 lines.

    Create empty SOPS slot `.secrets/prod/sentry.yaml` with placeholder ENC values that Task 3 replaces. Use the same age recipient as `.secrets/prod/mapbox.yaml`:
    1. Author plaintext file `/tmp/sentry-plaintext.yaml` locally with all 8 keys as `<placeholder>`:
       ```
       SENTRY_DSN_BACKEND: <placeholder-task-3>
       SENTRY_DSN_MOBILE: <placeholder-task-3>
       SENTRY_DSN_BACKEND_STAGING: <empty-staging-not-deployed>
       SENTRY_DSN_MOBILE_STAGING: <empty-staging-not-deployed>
       SENTRY_ADMIN_PASSWORD: <placeholder-task-3>
       SENTRY_AUTH_TOKEN: <placeholder-task-3>
       TELEGRAM_BOT_TOKEN: <placeholder-task-3>
       TELEGRAM_CHAT_ID: <placeholder-task-3>
       ```
    2. Encrypt: `sops --age $(grep -m1 'age1' .sops.yaml | awk '{print $NF}') -e /tmp/sentry-plaintext.yaml > .secrets/prod/sentry.yaml`
    3. `shred -u /tmp/sentry-plaintext.yaml`
    4. Mirror for `.secrets/staging/sentry.yaml` — same keys but all values are `<empty-staging-not-deployed>` placeholder (staging env lands v1.1 per Phase 3 D-23).
    5. Verify: `sops -d .secrets/prod/sentry.yaml | grep -E '^SENTRY_DSN_BACKEND:'` returns the placeholder line.

    Do NOT actually run the smoke yet — Plan 05-02 Task 3 invokes it after Sentry is installed and SOPS is populated. This task only creates the script + the SOPS slot infrastructure.
  </action>
  <verify>
    <automated>
      # Script is executable + syntax-valid Python + stdlib-only
      chmod +x scripts/smoke_sentry.py \
        && python3 -c "import ast; ast.parse(open('scripts/smoke_sentry.py').read())" \
        && ! grep -qE "^import (requests|httpx)" scripts/smoke_sentry.py \
        && test $(wc -l < scripts/smoke_sentry.py) -ge 60 \
        && grep -q "envelope" scripts/smoke_sentry.py \
        && grep -q "X-Sentry-Auth" scripts/smoke_sentry.py \
        && sops -d .secrets/prod/sentry.yaml | grep -qE '^SENTRY_DSN_BACKEND:' \
        && sops -d .secrets/prod/sentry.yaml | grep -qE '^TELEGRAM_BOT_TOKEN:' \
        && sops -d .secrets/staging/sentry.yaml | grep -qE '^SENTRY_DSN_BACKEND_STAGING:'
    </automated>
  </verify>
  <done>
    smoke_sentry.py is ≥60 lines, stdlib-only, executable, syntax-valid, references envelope endpoint + X-Sentry-Auth header per RESEARCH §1.3; SOPS slot .secrets/prod/sentry.yaml + staging/sentry.yaml exist with all 8 keys (placeholders for now); `sops -d` returns expected keys.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 — Tiny Go alerter service (RESEARCH §1.5) + unit tests + Ansible install_alerter task + sentry-stack.service systemd unit</name>
  <files>
    infra/ansible/roles/sentry-prep/files/alerter/main.go,
    infra/ansible/roles/sentry-prep/files/alerter/go.mod,
    infra/ansible/roles/sentry-prep/files/alerter/main_test.go,
    infra/ansible/roles/sentry-prep/tasks/install_alerter.yml,
    infra/ansible/roles/sentry-prep/templates/alerter.service.j2,
    infra/ansible/roles/sentry-prep/templates/sentry-stack.service.j2
  </files>
  <read_first>
    - infra/ansible/roles/sport-stack/templates/sport-stack.service.j2 (systemd unit pattern: ExecStart with `docker compose` space-form per Phase 3 D-04 — copy verbatim for sentry-stack.service.j2 per PATTERNS.md)
    - infra/ansible/roles/sport-stack/tasks/main.yml (handler install pattern — used as analog for alerter systemd install)
    - .planning/phases/05-observability-backend/05-RESEARCH.md §1.5 (alerter sketch 50 LOC; token bucket 25/sec; /sentry-hook + /grafana-hook endpoints)
    - .planning/phases/05-observability-backend/05-PATTERNS.md "No Analog Found" → services/backend/alerter/main.go fallback (RESEARCH §1.5)
    - services/backend/pkg/clientversion/middleware_test.go (table-driven HTTP test pattern with httptest.NewRecorder — copy for main_test.go)
  </read_first>
  <action>
    Build the Go alerter service per RESEARCH §1.5 sketch + Telegram Bot API docs:

    `files/alerter/main.go`:
    - `package main`
    - Imports: stdlib only (`net/http`, `encoding/json`, `fmt`, `log/slog`, `os`, `time`) + `golang.org/x/time/rate` for the token bucket (well-established, single-purpose dep — pin in go.mod)
    - `var limiter = rate.NewLimiter(rate.Limit(25), 25)` — 25 tokens/sec sustained, burst 25 (under Telegram's 30/sec global cap per RESEARCH §1.5)
    - Two HTTP handlers:
      - `/sentry-hook` — parses `SentryWebhook struct { Level, Project, Title, WebURL string }` from JSON body; on `!limiter.Allow()` returns 429; templates message `🚨 *{Level}* in `{Project}`\n{Title}\n{WebURL}`; calls `sendTelegram(chatID, msg)`.
      - `/grafana-hook` — parses Grafana alert JSON (`alerts[].labels.alertname` + `alerts[].annotations.summary`); same rate limit + send.
    - `sendTelegram(chatID string, msg string) error`: POST `https://api.telegram.org/bot<token>/sendMessage` with body `{"chat_id":chatID, "text":msg, "parse_mode":"Markdown"}`; reads token from `os.Getenv("TELEGRAM_BOT_TOKEN")`, chatID from `os.Getenv("TELEGRAM_CHAT_ID")`.
    - `http.ListenAndServe("127.0.0.1:9099", nil)` — localhost-only (Caddy/Sentry on same VPS reverse-proxy or call directly).
    - slog JSON logging on stdout (matches Plan 05-03 backend convention even though alerter is its own binary).
    - Minimum 80 lines (RESEARCH §1.5 estimated ~50; rate-limit + dual-endpoint + tests round up).

    `files/alerter/go.mod`:
    ```
    module github.com/runningecosystem/backend/alerter

    go 1.22

    require golang.org/x/time v0.5.0
    ```

    `files/alerter/main_test.go`:
    - 3 table-driven tests (copy `httptest.NewRecorder` shape from `pkg/clientversion/middleware_test.go`):
      1. `TestSentryHook_HappyPath` — POSTs valid SentryWebhook JSON; assert 200 + 1 call to a mocked `sendTelegram` (override via package-level `var sendTelegram = func(...)` for testability)
      2. `TestSentryHook_RateLimit` — POSTs 30 requests in <1 sec; assert first 25 return 200, remainder return 429 (validates limiter behavior)
      3. `TestSentryHook_MalformedBody` — POSTs invalid JSON; assert 400 + no `sendTelegram` call

    `tasks/install_alerter.yml` (imported from `tasks/main.yml` after sentry-stack installation):
    1. `name: "Install Go 1.22 on sentry VPS"` — `apt: name=golang-go state=present` (Ubuntu 24.04 ships Go 1.22 by default)
    2. `name: "Copy alerter sources to /opt/alerter/"` — `synchronize` or `copy` of `roles/sentry-prep/files/alerter/` to `/opt/alerter/`
    3. `name: "Build alerter binary"` — `command: chdir=/opt/alerter go build -o /usr/local/bin/alerter ./` with `creates: /usr/local/bin/alerter` guard
    4. `name: "Install alerter.service systemd unit"` — `template: src=alerter.service.j2 dest=/etc/systemd/system/alerter.service` + notify reload
    5. `name: "Enable + start alerter.service"` — `systemd: name=alerter enabled=true state=started`
    6. Smoke: `uri: url=http://127.0.0.1:9099/sentry-hook method=POST body='{"Level":"error","Project":"test","Title":"smoke","WebURL":"http://example.com"}' status_code=[200,429]` — proves alerter is up; the 429 case (if rate-limit window not reset) also passes.

    `templates/alerter.service.j2`:
    ```jinja
    {# /etc/systemd/system/alerter.service — Phase 5 / OBS alerts (RESEARCH §1.5).
       Reads TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID from /opt/sentry/.env (SOPS-sourced). #}
    [Unit]
    Description=Sentry+Grafana → Telegram alerter (rate-limited 25/sec)
    After=network-online.target sentry-stack.service
    Wants=network-online.target

    [Service]
    Type=simple
    EnvironmentFile=/opt/sentry/.env
    ExecStart=/usr/local/bin/alerter
    Restart=on-failure
    RestartSec=5
    User=root

    [Install]
    WantedBy=multi-user.target
    ```

    `templates/sentry-stack.service.j2` (per D-31 + PATTERNS.md analog):
    ```jinja
    {# /etc/systemd/system/sentry-stack.service — Phase 5 / D-31.
       Sentry self-hosted compose umbrella; separate from sport-stack.service (different VPS). #}
    [Unit]
    Description=Sentry self-hosted compose stack
    Requires=docker.service
    After=docker.service network-online.target

    [Service]
    Type=oneshot
    RemainAfterExit=true
    WorkingDirectory={{ sentry_install_dir }}
    ExecStart=/usr/bin/docker compose -f docker-compose.yml -f docker-compose.override.yml up -d
    ExecStop=/usr/bin/docker compose -f docker-compose.yml -f docker-compose.override.yml down
    TimeoutStartSec=600

    [Install]
    WantedBy=multi-user.target
    ```

    Tests run on dev workstation: `cd infra/ansible/roles/sentry-prep/files/alerter && go test -race ./...` — all 3 tests pass.
  </action>
  <verify>
    <automated>
      cd infra/ansible/roles/sentry-prep/files/alerter \
        && go vet ./... \
        && go test -race ./... -run "TestSentryHook_HappyPath|TestSentryHook_RateLimit|TestSentryHook_MalformedBody" \
        && test $(wc -l < main.go) -ge 80 \
        && grep -q "rate.NewLimiter(rate.Limit(25)" main.go \
        && grep -q "/sentry-hook" main.go \
        && grep -q "/grafana-hook" main.go \
        && grep -q "ExecStart=/usr/bin/docker compose" ../../templates/sentry-stack.service.j2 \
        && grep -q "ExecStart=/usr/local/bin/alerter" ../../templates/alerter.service.j2
    </automated>
  </verify>
  <done>
    alerter Go service compiles + 3 unit tests pass; main.go ≥80 lines with rate.NewLimiter(25,25) + /sentry-hook + /grafana-hook handlers + Telegram POST sender; sentry-stack.service.j2 uses docker compose space-form per Phase 3 D-04; alerter.service.j2 reads TELEGRAM_BOT_TOKEN/CHAT_ID from /opt/sentry/.env (SOPS-sourced).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: USER ACTION 3 — Run Ansible to install Sentry + 4 projects + populate SOPS DSNs + verify Telegram delivery</name>
  <what-built>
    Plan 05-01 deployed Caddy + sentry-prep role skeleton with install.sh task STUBBED. Task 2 of this plan built the Go alerter + systemd units. This task uncomments the install.sh task, runs Ansible end-to-end, then walks the user through the irreducible manual steps (admin user password generation + Sentry UI login + DSN copy to SOPS + Telegram alert smoke).
  </what-built>
  <how-to-verify>
    **Step 3.1 — Generate admin password offline (per D-07 + Phase 2 SEC-02):**
    ```
    LC_ALL=C tr -dc 'A-Za-z0-9!@#$%' </dev/urandom | head -c 16 ; echo
    ```
    Save the 16-char password locally (password manager). DO NOT paste in chat.

    **Step 3.2 — Write admin password + bot token + chat_id into SOPS:**
    ```
    SOPS_AGE_KEY_FILE=$HOME/.config/sops/age/keys.txt sops edit .secrets/prod/sentry.yaml
    ```
    Editor opens. Replace placeholders for:
    - `SENTRY_ADMIN_PASSWORD: <the 16-char string>`
    - `TELEGRAM_BOT_TOKEN: <token from BotFather>`
    - `TELEGRAM_CHAT_ID: <chat_id from getUpdates>`

    Leave `SENTRY_DSN_*` + `SENTRY_AUTH_TOKEN` as placeholders for now (filled after install.sh runs).

    **Step 3.3 — Uncomment install.sh task in sentry-prep/tasks/main.yml + add createuser + sentry-stack.service + alerter tasks:**

    Edit `infra/ansible/roles/sentry-prep/tasks/main.yml`:
    - Uncomment the install.sh task stub from Plan 05-01 Task 2. The active task body:
      ```yaml
      - name: "Run Sentry install.sh (idempotent — creates docker-compose.yml on first run)"
        ansible.builtin.command:
          cmd: ./install.sh --skip-user-prompt --no-report-self-hosted-issues
          chdir: "{{ sentry_install_dir }}"
          creates: "{{ sentry_install_dir }}/docker-compose.yml"
        register: install_result
        async: 1800
        poll: 30
      ```
    - Append after install.sh task — sentry-stack systemd install (template sentry-stack.service.j2 + enable + start), then `import_tasks: create_projects.yml` (Task 3.4 creates this), then `import_tasks: install_alerter.yml`.

    **Step 3.4 — Create `tasks/create_projects.yml` (admin user + 4 projects bootstrap, per RESEARCH §1.2):**
    ```yaml
    - name: "Create Sentry admin user (idempotent — creates .admin-created sentinel per RESEARCH P14)"
      ansible.builtin.command:
        cmd: docker compose -f {{ sentry_install_dir }}/docker-compose.yml run --rm web createuser --email {{ sentry_admin_email }} --password "{{ sentry_secrets_sentry.SENTRY_ADMIN_PASSWORD }}" --superuser --no-input
        chdir: "{{ sentry_install_dir }}"
        creates: "{{ sentry_install_dir }}/.admin-created"
        no_log: true
      register: createuser_result

    - name: "Mark admin user created"
      ansible.builtin.file:
        path: "{{ sentry_install_dir }}/.admin-created"
        state: touch
      when: createuser_result is changed

    - name: "Install sentry-cli (pinned 2.40.0 per RESEARCH §4 + P18)"
      ansible.builtin.get_url:
        url: "https://release-registry.services.sentry.io/apps/sentry-cli/2.40.0?response=download&platform=Linux&arch=x86_64"
        dest: /usr/local/bin/sentry-cli
        mode: '0755'

    - name: "Create 4 Sentry projects (per D-06; sentry-cli 409 on re-run is OK)"
      ansible.builtin.command:
        cmd: sentry-cli projects create -o sentry "{{ item }}"
      environment:
        SENTRY_AUTH_TOKEN: "{{ sentry_secrets_sentry.SENTRY_AUTH_TOKEN | default('') }}"
        SENTRY_URL: "https://{{ sentry_caddy_host }}"
      loop:
        - prod-backend
        - staging-backend
        - prod-mobile
        - staging-mobile
      register: proj_result
      failed_when: proj_result.rc not in [0, 4]   # 4 = already exists; idempotent
      changed_when: "'Created project' in proj_result.stdout"
    ```

    Note: `SENTRY_AUTH_TOKEN` for the projects create call comes from a User Auth Token created in Sentry UI **after** admin user exists. This is the chicken-and-egg per RESEARCH §1.2 — we land it via two-pass:

    - First pass: createuser succeeds; projects create fails (no token). Operator opens Sentry UI → User Settings → Auth Tokens → "Create New Token" → scopes `project:write`, `org:read`. Pastes into SOPS via `sops edit .secrets/prod/sentry.yaml` → `SENTRY_AUTH_TOKEN: <token>`.
    - Second pass: re-run `ansible-playbook --tags sentry` — projects create succeeds.

    **Step 3.5 — Run Ansible (first pass):**
    ```
    cd infra/ansible && ansible-playbook -i inventory/sentry site.yml --tags sentry
    ```
    Expected: install.sh runs ~15 min on first call (downloads ~30 Sentry images, generates docker-compose.yml); admin user created; sentry-stack.service starts; project creation fails with no-auth-token warning (expected first-pass).

    **Step 3.6 — Login + create User Auth Token + populate SOPS:**
    - Open `https://sentry.<sentry-ip-dashed>.sslip.io/auth/login/`
    - Login with admin email + the 16-char password
    - Navigate User Settings → Auth Tokens → Create New Token
    - Scopes: `project:write`, `org:read`, `project:read`
    - Copy token, then `sops edit .secrets/prod/sentry.yaml` → `SENTRY_AUTH_TOKEN: <token>`

    **Step 3.7 — Run Ansible (second pass):**
    ```
    ansible-playbook -i inventory/sentry site.yml --tags sentry
    ```
    Expected: createuser idempotent (already-done), install.sh idempotent (creates: guard), 4 projects created. Sentry-cli output captured in Ansible log.

    **Step 3.8 — Extract 4 DSNs + populate SOPS:**
    - SSH to sentry VPS: `ssh deploy@<sentry-ip>`
    - Run: `sentry-cli projects list -o sentry --json | jq -r '.[] | "\(.slug): \(.dsn)"'`
    - Copy 4 lines like `prod-backend: https://<key>@sentry.../<project_id>`
    - On dev workstation: `sops edit .secrets/prod/sentry.yaml` → paste 4 DSNs into matching keys (SENTRY_DSN_BACKEND, SENTRY_DSN_MOBILE, etc.)

    **Step 3.9 — Smoke 1: Sentry envelope roundtrip:**
    ```
    SOPS_AGE_KEY_FILE=$HOME/.config/sops/age/keys.txt
    eval "$(sops -d .secrets/prod/sentry.yaml | grep -E '^(SENTRY_DSN_BACKEND|SENTRY_AUTH_TOKEN)=' | sed 's/^/export /')"
    python3 scripts/smoke_sentry.py
    ```
    Expected: `🎉 smoke_sentry.py passed.` — event POSTed + retrieved within 30s.

    **Step 3.10 — Configure Sentry Generic Webhook for prod-backend → 127.0.0.1:9099/sentry-hook:**
    - Open `https://sentry.<host>/settings/sentry/projects/prod-backend/alerts/new/`
    - Add Issue Alert: trigger "An event is seen", action "Send a notification via Generic Webhook" with URL `http://127.0.0.1:9099/sentry-hook` (alerter listens localhost-only — Sentry container can reach it via host networking; if not, expose 9099 inside docker network). Save.

    **Step 3.11 — Smoke 2: Telegram alert:**
    ```
    # Trigger another synthetic event to fire the alert rule
    python3 scripts/smoke_sentry.py
    # Watch Telegram chat — expect a message within 60s shaped like "🚨 *error* in `prod-backend`..."
    ```
    Confirm Telegram message received. Screenshot and attach to docs/RUNBOOKS/sentry-ops.md §3 "Telegram Alert Evidence".
  </how-to-verify>
  <resume-signal>Confirm: (a) admin user logs in to Sentry UI; (b) 4 projects exist via `sentry-cli projects list`; (c) all 4 DSNs are in SOPS (`sops -d .secrets/prod/sentry.yaml | grep DSN`); (d) smoke_sentry.py passes; (e) test alert reaches Telegram chat with the formatted message. If any step fails, paste the journalctl excerpt and we will triage.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Document acceptance evidence in sentry-ops.md + close Wave 2</name>
  <files>
    docs/RUNBOOKS/sentry-ops.md
  </files>
  <read_first>
    - docs/RUNBOOKS/sentry-ops.md (skeleton from Plan 05-01 — append §3 + §4)
    - docs/RUNBOOKS/sops-edit.md (evidence section style — copy per PATTERNS.md analog)
    - .planning/phases/05-observability-backend/05-VALIDATION.md rows OBS-01/OBS-02 (acceptance evidence shapes)
    - .planning/phases/05-observability-backend/05-RESEARCH.md §P14 (install.sh re-run safety) + §P15 (event volume budget)
  </read_first>
  <action>
    Append to docs/RUNBOOKS/sentry-ops.md:

    **§3 — Sentry Bootstrap Evidence** (verbatim outputs from Task 3.5-3.11):
    - install.sh run timing (Ansible async/poll result)
    - createuser stdout (with password redacted as `<REDACTED>`)
    - `sentry-cli projects list -o sentry --json` JSON output (4 entries)
    - smoke_sentry.py output (`🎉 smoke_sentry.py passed.`)
    - Telegram chat screenshot reference (file path placeholder: `docs/RUNBOOKS/sentry-ops-evidence/telegram-alert-<date>.png` — user attaches)
    - UFW table snapshot post-Sentry-up (port 443 from anywhere, port 22 LIMIT — same as Plan 05-01 evidence)

    **§4 — Telegram Bot + Alerter Operations**:
    - How to rotate `TELEGRAM_BOT_TOKEN`: `sops edit .secrets/prod/sentry.yaml` → update → re-run `ansible-playbook --tags sentry` → alerter service reloads .env.
    - How to add new alert source: append handler in `roles/sentry-prep/files/alerter/main.go`, re-deploy via `--tags sentry`.
    - Token-bucket capacity: 25 tokens/sec; burst 25. To raise/lower, edit `rate.NewLimiter(rate.Limit(25), 25)` constant (RESEARCH §1.5).
    - Telegram global rate: 30/sec; per-chat 1/sec — alerter respects via single shared limiter.

    **§5 — Pitfalls (link to RESEARCH P12-P19)**:
    - §P12 — Kafka heap OOM (already mitigated by docker-compose.override.yml.j2 in Plan 05-01)
    - §P14 — install.sh re-run safety (already mitigated by `creates: docker-compose.yml` + `creates: .admin-created`)
    - §P15 — Event volume budget (target <1000 events/hr — use Loki+Grafana for log inspection, NOT Sentry)
    - §P18 — sentry-cli version drift (pin via Ansible `get_url` to 2.40.0 — already done in create_projects.yml)

    **§6 — Sentry Operations Quickref**:
    - View events: `https://sentry.<host>/issues/?project=<project-id>`
    - Stats: `https://sentry.<host>/organizations/sentry/stats/`
    - Retention tuning (RESEARCH §1.10): `SENTRY_EVENT_RETENTION_DAYS` in /opt/sentry/.env, default 90; lower to 30 if `df /var/lib/docker` crosses 60%.

    Minimum total file length: 200 lines.
  </action>
  <verify>
    <automated>
      test $(wc -l < docs/RUNBOOKS/sentry-ops.md) -ge 200 \
        && grep -q "Bootstrap Evidence" docs/RUNBOOKS/sentry-ops.md \
        && grep -q "Telegram Bot + Alerter Operations" docs/RUNBOOKS/sentry-ops.md \
        && grep -q "P14" docs/RUNBOOKS/sentry-ops.md \
        && grep -q "P15" docs/RUNBOOKS/sentry-ops.md \
        && grep -q "SENTRY_EVENT_RETENTION_DAYS" docs/RUNBOOKS/sentry-ops.md
    </automated>
  </verify>
  <done>
    sentry-ops.md ≥200 lines with §3 Bootstrap Evidence (verbatim outputs), §4 Telegram operations, §5 pitfalls (P12/14/15/18 cross-refs), §6 quickref. All 8 SOPS keys populated. Plan 05-03 / 05-04 / 05-05 can read SENTRY_DSN_BACKEND from SOPS unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public Internet → sentry VPS:443 (Caddy → Sentry UI + envelope ingest) | TLS terminated at Caddy; Sentry uses DSN public_key for event auth |
| public Internet → sentry VPS:443 (Caddy → /loki/api/v1/push) | Plan 05-06 enforces UFW allowlist; currently no route (Loki only reachable internally on sentry VPS) |
| Sentry container → 127.0.0.1:9099 (alerter) | Container shares host network or routes via Caddy `localhost:9099`; Sentry's outbound webhook IS the trust boundary |
| alerter → api.telegram.org:443 | Outbound HTTPS; bot token in env; rate-limited 25/sec |
| dev workstation → Sentry web UI (login) | TLS + Sentry CSRF-protected login form; admin user is superuser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01-S | Spoofing | Inbound Sentry events with forged public_key | accept | Public_key is intentionally not a secret per Sentry DSN model; impersonation produces events that look real — mitigated by ratelimit at Sentry and tag filtering |
| T-05-02-02-T | Tampering | /opt/sentry/.env on sentry VPS | mitigate | mode=0600 owner=root; SOPS-sourced; Ansible re-template overwrites on each run |
| T-05-02-03-R | Repudiation | Sentry admin user activity | mitigate | Sentry's audit log records project create/delete/role-change |
| T-05-02-04-I | Info disclosure | Telegram chat message body | mitigate | Alerter formats only event title + project + URL (no PII attrs from event body); RESEARCH §1.5 sketch verified |
| T-05-02-05-I | Info disclosure | sentry-cli output captured in Ansible logs (DSN with public_key) | mitigate | `no_log: true` on createuser + project create tasks; DSN public_key is intentionally non-secret |
| T-05-02-06-D | DoS | Alerter rate-limit token bucket exhaustion under Sentry storm | accept | Token-bucket drops excess (429 to Sentry); Sentry retries with backoff per Generic Webhook docs; tradeoff: alert latency under storm vs Telegram block |
| T-05-02-07-D | DoS | Sentry envelope ingest under abuse | mitigate | Sentry built-in rate limits per project; isolation = sentry VPS is separate from app VPS |
| T-05-02-08-E | EoP | sentry-cli auth token leak | mitigate | Token in SOPS only; `--auth-token` env var via Ansible; rotate via Sentry UI → User Auth Tokens |
| T-05-02-09-E | EoP | Telegram bot privacy-mode disabled means bot reads group messages | accept | Group is private + invite-only; bot has admin role; no other users will be in chat for v1.0 |

</threat_model>

<verification>
1. `curl -fsSI https://sentry.<host>/auth/login/` returns HTTP 200 with Let's Encrypt cert chain
2. Admin login at Sentry UI works (user opens browser; manual)
3. `sentry-cli projects list -o sentry --json | jq -r '.[].slug' | sort` lists `prod-backend, prod-mobile, staging-backend, staging-mobile`
4. `sops -d .secrets/prod/sentry.yaml | grep -cE '^SENTRY_DSN_'` returns 4
5. `python3 scripts/smoke_sentry.py` exits 0 with "🎉 ... passed"
6. Telegram chat receives a synthetic alert message (manual verify by USER ACTION 3)
7. `cd infra/ansible/roles/sentry-prep/files/alerter && go test -race ./...` exits 0
8. Re-running `ansible-playbook -i inventory/sentry site.yml --tags sentry` reports `changed=0`
</verification>

<success_criteria>
Plan 05-02 is complete when:
- [ ] USER ACTION 2 done (Telegram bot + chat_id retrieved offline)
- [ ] USER ACTION 3 done (admin password set, Sentry login works, 4 DSNs in SOPS, Telegram alert delivered)
- [ ] All 4 Sentry projects exist (sentry-cli enumeration)
- [ ] smoke_sentry.py passes (envelope roundtrip)
- [ ] alerter Go service running on 127.0.0.1:9099, 3 unit tests pass
- [ ] sentry-stack.service enabled + active on sentry VPS (separate from sport-stack per D-31)
- [ ] docs/RUNBOOKS/sentry-ops.md ≥200 lines with §3-§6 populated
</success_criteria>

<output>
After completion, create `.planning/phases/05-observability-backend/05-02-SUMMARY.md` documenting: Sentry version installed (26.5.0), install.sh duration, 4 project IDs + DSN host portions, Telegram bot username, chat_id (numeric, can be in SUMMARY — not a secret per Telegram docs; token is), screenshot reference for Telegram alert evidence. Update STATE.md `stopped_at` to reflect Wave 2 complete. Plan 05-03/05/06 unblocked.
</output>
