---
phase: 05-observability-backend
plan: 03
type: execute
wave: 2
depends_on: []  # Originally [05-01]; 05-01 superseded per ADR-0010. 05-03 was already executed (commits 6dfcef3 / 320975c / ea1e65d / 183beb0).
files_modified:
  - services/backend/pkg/observability/slog_handler.go
  - services/backend/pkg/observability/pii_deny_list.go
  - services/backend/pkg/observability/slog_handler_test.go
  - services/backend/pkg/observability/pii_deny_list_test.go
  - services/backend/identity/internal/service/otp.go
  - services/backend/identity/internal/service/otp_test.go
  - services/backend/identity/cmd/server/main.go
  - services/backend/activity-sync/cmd/server/main.go
  - services/backend/feed/cmd/server/main.go
  - services/backend/media/cmd/server/main.go
  - services/backend/messaging/cmd/server/main.go
  - services/backend/notifications/cmd/server/main.go
  - services/backend/realtime-gw/cmd/server/main.go
  - services/backend/social-graph/cmd/server/main.go
  - services/backend/gateway/cmd/server/main.go
  - services/backend/pkg/go.mod
  - scripts/pii_audit.sh
  - .github/workflows/backend-ci.yml
autonomous: true
requirements: [OBS-03, OBS-04, OBS-06]
must_haves:
  truths:
    - "All 8 Go services + gateway bootstrap with observability.NewSlogJSONHandler — slog.Default() produces JSON output on stdout with default attrs (service, env, version, request_id)"
    - "OTP `code` attribute removed from prod path in otp.go:70-74 — emits only via slog.DebugContext under `if devMode { ... }` (D-13)"
    - "Even at LOG_LEVEL=debug with devMode=true, the PII deny-list handler drops `code` attr (defense-in-depth — D-13)"
    - "Email attribute hashed to first-8 hex of SHA-256 (D-10) — operational correlation without plaintext"
    - "scripts/pii_audit.sh exits 0 on current tree (no leaks) and exits 1 if a new slog call uses any deny-list key"
    - "CI job `pii-audit` runs `bash scripts/pii_audit.sh` and blocks PR merge on non-zero"
  artifacts:
    - path: "services/backend/pkg/observability/slog_handler.go"
      provides: "NewSlogJSONHandler(Config) *slog.Logger constructor; Config{ServiceName, Env, Version, Level}; wraps slog.JSONHandler; adds default attrs; intercepts each Attr to drop/hash per PII deny-list (D-10)"
      contains: "func NewSlogJSONHandler"
      min_lines: 120
    - path: "services/backend/pkg/observability/pii_deny_list.go"
      provides: "var PIIDenyList map[string]struct{} keyed lowercase per D-12; var EmailHashKeys map[string]struct{} for hash-not-drop set"
      contains: "PIIDenyList"
      min_lines: 60
    - path: "services/backend/pkg/observability/slog_handler_test.go"
      provides: "TestSlogHandler_DefaultAttrs, TestSlogHandler_DropsCode, TestSlogHandler_HashesEmail, TestSlogHandler_DropsAllDenyListKeys (table-driven across all D-12 keys)"
      contains: "TestSlogHandler_DropsCode"
      min_lines: 200
    - path: "services/backend/identity/internal/service/otp_test.go"
      provides: "TestRequestCode_ProdNoLeak (no `code` attr when devMode=false at LevelInfo), TestRequestCode_DevButInfoLevel (no `code` even at devMode=true if LOG_LEVEL=info)"
      contains: "TestRequestCode_ProdNoLeak"
    - path: "scripts/pii_audit.sh"
      provides: "Bash grep audit per D-14 — exit 1 on slog call with deny-list attr name"
      contains: "slog\\.\\\\w+Context"
      min_lines: 30
    - path: ".github/workflows/backend-ci.yml"
      provides: "New `pii-audit` job per D-14 — runs scripts/pii_audit.sh; blocks PR merge on failure"
      contains: "pii-audit"
  key_links:
    - from: "8 services + gateway main.go"
      to: "services/backend/pkg/observability"
      via: "import observability; slog.SetDefault(observability.NewSlogJSONHandler(...))"
      pattern: "observability.NewSlogJSONHandler"
    - from: "services/backend/identity/internal/service/otp.go (line 70-74 fix)"
      to: "Plan 01-03 featureflags pkg (devMode source unchanged)"
      via: "if devMode { slog.DebugContext(ctx, ..., 'code', code) } — DebugContext-only emission"
      pattern: "if devMode"
    - from: ".github/workflows/backend-ci.yml `pii-audit` job"
      to: "scripts/setup-branch-protection.sh required-checks list"
      via: "Add 'PII Audit (slog grep)' to the CICD-06 branch protection job array"
      pattern: "PII Audit"
---

<objective>
Land the structured-JSON slog seam + PII deny-list scrubbing across all 8 Go services (+ gateway), fix the OBS-04 OTP `code`-leak per D-13 (defense-in-depth: gate at call-site AND drop at handler), and add a CI grep audit gate (D-14) that blocks any future PR from re-introducing PII attribute names. This plan does NOT yet wire Sentry SDK or OpenTelemetry — those land in Plan 05-05 (which depends on this plan only because Plan 05-05 needs `pkg/observability` to already exist as an import path).

Purpose: Three of Phase 5's eight ROADMAP success criteria depend on this plan:
- **Criterion 3** "Structured JSON logs on all Go services; OTP codes never logged" (CONCERNS.md P0 closure)
- **Criterion 5** "No GPS coords / phone numbers / displayName / DM content / external_uuid in logs — verified via grep audit + runtime sample" (CI half; Plan 05-06 supplies runtime probe)
- **Criterion 7** seam (LevelDebug elevation infrastructure — debug_session_middleware uses the same handler; Plan 05-06 wires the middleware)

Output: A reusable `pkg/observability` package containing slog_handler.go (JSON output + PII scrub) + pii_deny_list.go (single source of truth for both slog AND OTel span scrubbing in Plan 05-05). All 8 services + gateway boot with the new logger; identity OTP path is fixed per D-13; CI grep gate prevents regression. Plan 05-05 will extend `pkg/observability` with `otel_init.go` + `sentry_init.go` importing `PIIDenyList` from this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-observability-backend/05-CONTEXT.md
@.planning/phases/05-observability-backend/05-RESEARCH.md
@.planning/phases/05-observability-backend/05-VALIDATION.md
@.planning/phases/05-observability-backend/05-PATTERNS.md

# Source files the executor reads to copy patterns:
@services/backend/pkg/clientversion/clientversion.go
@services/backend/pkg/clientversion/middleware.go
@services/backend/pkg/clientversion/middleware_test.go
@services/backend/identity/cmd/server/main.go
@services/backend/identity/internal/service/otp.go
@services/backend/feed/cmd/server/main.go
@services/backend/activity-sync/cmd/server/main.go
@.github/workflows/backend-ci.yml
@scripts/setup-branch-protection.sh

<interfaces>
<!-- Existing slog default — what we replace per D-32 -->
From services/backend/identity/cmd/server/main.go (current):
```go
logger := slog.New(slog.NewJSONHandler(os.Stdout, &slog.HandlerOptions{Level: slog.LevelInfo}))
slog.SetDefault(logger)
```

<!-- Target after this plan -->
```go
logger := observability.NewSlogJSONHandler(observability.Config{
    ServiceName: "identity",
    Env:         envOr("ENV", "prod"),
    Version:     versionLDFlag,  // -ldflags "-X main.version=$(git rev-parse --short HEAD)"
    Level:       observability.ParseLevel(envOr("LOG_LEVEL", "info")),
})
slog.SetDefault(logger)
```

<!-- pkg/observability public API the executor builds in this plan -->
package observability

type Config struct {
    ServiceName string  // e.g., "identity"
    Env         string  // "prod" | "staging" | "dev"
    Version     string  // git SHA from -ldflags
    Level       slog.Level
}

func NewSlogJSONHandler(cfg Config) *slog.Logger
func ParseLevel(s string) slog.Level

// PII deny-list — single source of truth; imported by Plan 05-05 OTel span processor too
var PIIDenyList map[string]struct{}     // exact lowercase key match → drop
var EmailHashKeys map[string]struct{}   // {"email"} → hash to email_hash (SHA-256 first 8)

// Internal log-level override context (used by Plan 05-06 debug_session_middleware)
type logLevelKey struct{}
func WithLogLevel(ctx context.Context, lvl slog.Level) context.Context
func LogLevelFromContext(ctx context.Context) (slog.Level, bool)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 — pkg/observability package: pii_deny_list.go + slog_handler.go + unit tests (RED → GREEN)</name>
  <files>
    services/backend/pkg/observability/pii_deny_list.go,
    services/backend/pkg/observability/pii_deny_list_test.go,
    services/backend/pkg/observability/slog_handler.go,
    services/backend/pkg/observability/slog_handler_test.go,
    services/backend/pkg/go.mod
  </files>
  <behavior>
    - PIIDenyList contains all 29 keys from D-12: code, otp_code, otp, phone, phone_number, phoneNumber, tel, displayName, display_name, external_uuid, strava_external_id, garmin_external_id, lat, lon, latitude, longitude, coords, gps, location, dm_content, message_body, body, content, mapbox_token, strava_token, jwt, access_token, refresh_token, password (case-insensitive lookup)
    - NewSlogJSONHandler(Config{ServiceName:"identity", Env:"prod", Version:"abc123", Level:slog.LevelInfo}) returns a *slog.Logger that emits JSON to os.Stdout
    - Every log line emitted by the handler contains all 4 default attrs: "service":"identity", "env":"prod", "version":"abc123", "request_id":"<uuid-from-context-or-empty>"
    - Attr with key in PIIDenyList (any case) is OMITTED from output (NOT replaced with [REDACTED]; entirely dropped — minimum-information principle)
    - Attr with key "email" (or "Email" or "EMAIL") becomes attr "email_hash" with value = first 8 hex chars of SHA-256(value)
    - LogLevelFromContext(ctx) returns the override if set via WithLogLevel; otherwise returns the handler's baseline level
    - Handler emits at the override level when ctx-override > baseline level (used by Plan 05-06 debug_session_middleware to elevate to Debug per-request)
    - ParseLevel("info") → slog.LevelInfo; ParseLevel("debug") → slog.LevelDebug; ParseLevel("warn") → slog.LevelWarn; ParseLevel("") → slog.LevelInfo (default); ParseLevel("garbage") → slog.LevelInfo (fail-safe)
  </behavior>
  <read_first>
    - services/backend/pkg/clientversion/clientversion.go (package-init pattern + ctxKey{} private struct per PATTERNS.md `slog_handler.go` + `pii_deny_list.go` analogs)
    - services/backend/pkg/clientversion/middleware.go (loggerOrDefault fallback idiom)
    - services/backend/pkg/clientversion/middleware_test.go (table-driven HTTP handler test pattern — copy newLogger() + buffer assertion idiom per PATTERNS.md)
    - services/backend/identity/cmd/server/main.go:52-53 (current `slog.NewJSONHandler` baseline call to be replaced)
    - .planning/phases/05-observability-backend/05-CONTEXT.md §decisions D-09 / D-10 / D-11 / D-12
    - .planning/phases/05-observability-backend/05-RESEARCH.md §4 (no extra deps needed — stdlib `log/slog` + `crypto/sha256` + `context`)
    - .planning/phases/05-observability-backend/05-PATTERNS.md "Go: services/backend/pkg/observability/" subsections "slog_handler.go" + "pii_deny_list.go"
    - .planning/phases/05-observability-backend/05-VALIDATION.md rows OBS-03 / OBS-04 / OBS-06 (test names + expected evidence)
  </read_first>
  <action>
    **RED phase — author tests first:**

    `pkg/observability/pii_deny_list_test.go` — table-driven:
    - `TestPIIDenyList_Contains29Keys`: assert len(PIIDenyList) == 29; assert each of the 29 D-12 keys present (lowercase canonicalization — table-driven over all 29 names listed in D-12).
    - `TestEmailHashKeys_ContainsEmail`: assert "email" ∈ EmailHashKeys; assert "Email" + "EMAIL" lookups (case-insensitive) also match.

    `pkg/observability/slog_handler_test.go` — copy table-driven shape from `clientversion/middleware_test.go`:
    - `TestSlogHandler_DefaultAttrs`: create handler with Config{ServiceName:"identity", Env:"prod", Version:"abc123def", Level:slog.LevelInfo}; emit one Info log; parse stdout JSON; assert "service":"identity", "env":"prod", "version":"abc123def", "level":"INFO", "msg":"<expected>".
    - `TestSlogHandler_DropsCode`: emit `slog.InfoContext(ctx, "x", "code", "424242", "email", "u@example.com")`; assert output JSON does NOT contain key "code"; assert "email_hash" present + length 8.
    - `TestSlogHandler_HashesEmail`: emit `slog.InfoContext(ctx, "x", "email", "alice@example.com")`; assert output JSON contains `"email_hash":"<first 8 hex of sha256("alice@example.com")>"`; assert NO "email" key.
    - `TestSlogHandler_DropsAllDenyListKeys`: table-driven across all 29 D-12 keys — for each, emit with that attr key + assert it's absent from output.
    - `TestSlogHandler_CaseInsensitiveDenyList`: emit `"Code"`, `"CODE"`, `"PhoneNumber"`, `"DISPLAYNAME"` — all dropped.
    - `TestSlogHandler_RequestIDFromContext`: set ctx via `clientversion.WithRequestID(ctx, "req-abc")` analog (or whatever existing request-id helper exists; if none, add a minimal `WithRequestID/RequestIDFromContext` to slog_handler.go — sourced from `clientversion.go:56-83` ctxKey pattern); emit log; assert "request_id":"req-abc".
    - `TestSlogHandler_LevelOverrideFromContext`: handler baseline Level=Info; ctx with `WithLogLevel(ctx, slog.LevelDebug)`; emit Debug; assert debug line emitted (would have been dropped at baseline).
    - `TestParseLevel`: cases "info" → Info; "debug" → Debug; "warn" → Warn; "error" → Error; "" → Info; "garbage" → Info; "INFO" → Info (case-insensitive).
    - Minimum 200 lines across both test files.

    Run tests → expect compile failures or all RED.

    **GREEN phase — implementation:**

    `pkg/observability/pii_deny_list.go` per PATTERNS.md `pii_deny_list.go` analog:
    - Package comment header (RU/EN OK per CLAUDE.md): "Package observability — single source of truth for PII attribute names dropped from slog AND OTel spans. Phase 5 / OBS-04 / OBS-06 / D-12 / D-21."
    - `var PIIDenyList = map[string]struct{}{ "code": {}, "otp_code": {}, ... }` — 29 entries lowercase (per D-12 list)
    - `var EmailHashKeys = map[string]struct{}{ "email": {} }`
    - Helper: `func IsDenied(key string) bool { _, ok := PIIDenyList[strings.ToLower(key)]; return ok }`
    - Helper: `func ShouldHash(key string) bool { _, ok := EmailHashKeys[strings.ToLower(key)]; return ok }`
    - Helper: `func HashEmail(value string) string { h := sha256.Sum256([]byte(value)); return hex.EncodeToString(h[:])[:8] }`
    - Minimum 60 lines.

    `pkg/observability/slog_handler.go` per PATTERNS.md `slog_handler.go` analog:
    - Package + file-level doc comment (D-10).
    - `type Config struct { ServiceName, Env, Version string; Level slog.Level }`.
    - Private `type ctxKey struct{ name string }`; private vars `var requestIDKey = ctxKey{"request_id"}`, `var logLevelKey = ctxKey{"log_level"}`.
    - `func WithRequestID(ctx context.Context, id string) context.Context` + `func RequestIDFromContext(ctx context.Context) (string, bool)` — minimal request-id plumbing for the default attr.
    - `func WithLogLevel(ctx context.Context, lvl slog.Level) context.Context` + `func LogLevelFromContext(ctx context.Context) (slog.Level, bool)` (used by Plan 05-06).
    - `func ParseLevel(s string) slog.Level` per behavior contract.
    - Private `type piiScrubHandler struct { inner slog.Handler; cfg Config }`. Implements `slog.Handler` interface:
      - `Enabled(ctx, lvl)` — returns true if `lvl >= cfg.Level` OR `lvl >= overrideFromCtx(ctx)`.
      - `Handle(ctx, record)`:
        1. Walk `record.Attrs()` — for each Attr, skip if `IsDenied(attr.Key)`; replace key with "email_hash" and value with HashEmail(value) if `ShouldHash(attr.Key)`.
        2. Add default attrs: service/env/version (from cfg) + request_id (from RequestIDFromContext(ctx); empty string if not set).
        3. Call inner.Handle(ctx, newRecord).
      - `WithAttrs(attrs)` — pass through (still scrubs at Handle time).
      - `WithGroup(name)` — pass through.
    - `func NewSlogJSONHandler(cfg Config) *slog.Logger`:
      ```
      base := slog.NewJSONHandler(os.Stdout, &slog.HandlerOptions{ Level: cfg.Level, AddSource: false })
      return slog.New(&piiScrubHandler{inner: base, cfg: cfg})
      ```
    - Minimum 120 lines.

    Update `services/backend/pkg/go.mod` — no new external deps (stdlib only: `log/slog`, `crypto/sha256`, `encoding/hex`, `context`, `os`, `strings`).

    Run tests → expect all GREEN. `go test -race ./services/backend/pkg/observability/...` exits 0.
  </action>
  <verify>
    <automated>
      cd services/backend \
        && go test -race ./pkg/observability/... -run "TestPIIDenyList_Contains29Keys|TestEmailHashKeys_ContainsEmail|TestSlogHandler_DefaultAttrs|TestSlogHandler_DropsCode|TestSlogHandler_HashesEmail|TestSlogHandler_DropsAllDenyListKeys|TestSlogHandler_CaseInsensitiveDenyList|TestSlogHandler_RequestIDFromContext|TestSlogHandler_LevelOverrideFromContext|TestParseLevel" \
        && test $(wc -l < pkg/observability/slog_handler.go) -ge 120 \
        && test $(wc -l < pkg/observability/pii_deny_list.go) -ge 60 \
        && test $(wc -l < pkg/observability/slog_handler_test.go) -ge 200 \
        && go vet ./pkg/observability/...
    </automated>
  </verify>
  <done>
    pkg/observability package compiles + all unit tests GREEN; PIIDenyList contains 29 D-12 keys; EmailHashKeys contains "email"; NewSlogJSONHandler wraps slog.JSONHandler with PII scrub + default attrs + context-level-override seam; ParseLevel fail-safe to Info.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-13 OTP fix — otp.go:70-74 + extend otp_test.go with no-leak tests</name>
  <files>
    services/backend/identity/internal/service/otp.go,
    services/backend/identity/internal/service/otp_test.go
  </files>
  <behavior>
    - When `devMode=false` (production path), slog.InfoContext at line 70-74 emits attrs `email` only — NO `code` attr (D-13)
    - When `devMode=true`, an additional slog.DebugContext line emits with `email` + `code` attrs — but this line is gated by handler Level=Debug (won't emit at LOG_LEVEL=info even if devMode=true)
    - Defense-in-depth: even if devMode=true AND LOG_LEVEL=debug, the PII deny-list handler from Task 1 drops the `code` attr from the output
    - Test `TestRequestCode_ProdNoLeak`: devMode=false + Level=Info → captured log buffer contains "otp issued" but does NOT contain any 6-digit substring
    - Test `TestRequestCode_DevButInfoLevel`: devMode=true + Level=Info → buffer does NOT contain the code (Debug line dropped at Info baseline)
    - Test `TestRequestCode_DevAndDebugLevel_HandlerStillScrubs`: devMode=true + Level=Debug + use the Plan 05-03 piiScrubHandler → buffer contains "otp dev-mode echo" message but NOT the code value (deny-list still drops it)
  </behavior>
  <read_first>
    - services/backend/identity/internal/service/otp.go (current state — line 70-74 is the bug; lines 111+124 are the precedent pattern per PATTERNS.md `otp.go` MODIFY analog)
    - services/backend/identity/internal/service/otp.go:104-125 (existing `if devMode { slog.WarnContext(...) }` shape — copy verbatim for new D-13 fix)
    - services/backend/identity/internal/service/otp_test.go (existing test file — if absent, create from scratch following pattern of any neighbor `*_test.go`)
    - services/backend/pkg/observability/slog_handler.go (just-built handler — used by test to verify defense-in-depth)
    - .planning/phases/05-observability-backend/05-CONTEXT.md §decisions D-13
    - .planning/phases/05-observability-backend/05-PATTERNS.md "Go: per-service MODIFY" → "services/backend/identity/internal/service/otp.go" subsection
  </read_first>
  <action>
    **RED phase — extend otp_test.go:**

    Add 3 tests to `otp_test.go` (create file if it doesn't yet exist; minimum test framework: `bytes.Buffer` log capture via custom slog handler — mirror `pkg/clientversion/middleware_test.go:33-45 newLogger()`):

    1. `TestRequestCode_ProdNoLeak(t *testing.T)`:
       - Set up identity service with `devMode=false`.
       - Capture slog output via `bytes.Buffer` + `slog.NewJSONHandler` at LevelInfo.
       - Call `svc.RequestCode(ctx, "u@example.com")` which under the hood triggers the line 70-74 InfoContext call.
       - Parse captured JSON; assert one record with `msg:"otp issued"`; assert `email` key present; assert `code` key ABSENT; assert no 6-digit numeric substring in entire buffer.

    2. `TestRequestCode_DevButInfoLevel(t *testing.T)`:
       - `devMode=true`, handler Level=LevelInfo.
       - Same call → buffer contains "otp issued" InfoContext line BUT not the "otp dev-mode echo" DebugContext line (it's at Debug, baseline is Info).
       - Assert no 6-digit substring in buffer.

    3. `TestRequestCode_DevAndDebugLevel_HandlerStillScrubs(t *testing.T)`:
       - `devMode=true`, handler = `observability.NewSlogJSONHandler(Config{Level:slog.LevelDebug,...})`.
       - Buffer captures both InfoContext + DebugContext lines BUT the DebugContext line's `code` attr is dropped by the piiScrubHandler (defense-in-depth).
       - Assert "otp dev-mode echo" message present in buffer; assert `code` key NOT in JSON; assert no 6-digit substring.

    Run tests → RED (line 70-74 still emits `code` unconditionally).

    **GREEN phase — fix otp.go:70-74 per D-13:**

    Replace the existing block:
    ```
    slog.InfoContext(ctx, "otp issued",
        "email", email,
        // Dev-mode logging only. В production не логируем code.
        "code", code,
    )
    ```

    With:
    ```
    slog.InfoContext(ctx, "otp issued", "email", email)
    if devMode {
        slog.DebugContext(ctx, "otp dev-mode echo", "email", email, "code", code)
        // DebugContext only emits at LOG_LEVEL=debug. Even at LevelDebug, the
        // pkg/observability slog handler's PIIDenyList drops "code" attr (D-12).
        // Defense-in-depth: gate at call-site AND drop at handler (D-13).
    }
    ```

    Run tests → GREEN. Confirm `go test ./services/backend/identity/internal/service/... -run "TestRequestCode_"` exits 0.

    Also commit-grep self-check: `grep -nE 'slog\.\w+Context\([^)]*"code"' services/backend/identity/internal/service/otp.go` returns the DebugContext line only (no other occurrences).
  </action>
  <verify>
    <automated>
      cd services/backend \
        && go test -race ./identity/internal/service/... -run "TestRequestCode_ProdNoLeak|TestRequestCode_DevButInfoLevel|TestRequestCode_DevAndDebugLevel_HandlerStillScrubs" \
        && grep -nE 'slog\.\w+Context\([^)]*"code"' identity/internal/service/otp.go | grep -v '^#' | wc -l | grep -qE '^[[:space:]]*1[[:space:]]*$' \
        && grep -q 'if devMode {' identity/internal/service/otp.go \
        && grep -q 'slog.DebugContext.*otp dev-mode echo' identity/internal/service/otp.go
    </automated>
  </verify>
  <done>
    otp.go:70-74 fixed per D-13 (InfoContext drops `code`; DebugContext emits only when devMode=true); 3 new tests GREEN; defense-in-depth verified (handler scrubs `code` even at LevelDebug); exactly one `slog.*Context(...,"code"...)` call remains in otp.go (the gated DebugContext).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire pkg/observability into 8 services + gateway main.go + scripts/pii_audit.sh + CI pii-audit job</name>
  <files>
    services/backend/identity/cmd/server/main.go,
    services/backend/activity-sync/cmd/server/main.go,
    services/backend/feed/cmd/server/main.go,
    services/backend/media/cmd/server/main.go,
    services/backend/messaging/cmd/server/main.go,
    services/backend/notifications/cmd/server/main.go,
    services/backend/realtime-gw/cmd/server/main.go,
    services/backend/social-graph/cmd/server/main.go,
    services/backend/gateway/cmd/server/main.go,
    scripts/pii_audit.sh,
    .github/workflows/backend-ci.yml,
    scripts/setup-branch-protection.sh
  </files>
  <read_first>
    - services/backend/identity/cmd/server/main.go (current bootstrap — copy verbatim shape per PATTERNS.md "Go: per-service MODIFY" → "8× cmd/server/main.go" subsection; replace line 52 only)
    - services/backend/activity-sync/cmd/server/main.go (verify same shape across all 8)
    - services/backend/gateway/cmd/server/main.go (gateway is the 9th — same pattern)
    - scripts/setup-branch-protection.sh (PATTERNS.md `scripts/pii_audit.sh` analog — boilerplate `set -euo pipefail` + idempotent header + tool-availability check)
    - .github/workflows/backend-ci.yml (existing job structure — extend with two new jobs per PATTERNS.md `backend-ci.yml` MODIFY section; D-18 cardinality-probe job lands in Plan 05-04, but we wire only `pii-audit` here)
    - .planning/phases/05-observability-backend/05-CONTEXT.md §decisions D-14 + D-32
    - .planning/phases/05-observability-backend/05-PATTERNS.md "Python smoke / Bash audit scripts" → "scripts/pii_audit.sh" subsection
  </read_first>
  <action>
    **Step 3.1 — Replace slog default in each of 9 main.go files** (8 services + gateway):

    For each `services/backend/<svc>/cmd/server/main.go`, find the line(s) matching:
    ```
    logger := slog.New(slog.NewJSONHandler(os.Stdout, &slog.HandlerOptions{Level: slog.LevelInfo}))
    slog.SetDefault(logger)
    ```

    Replace with (per D-32 + PATTERNS.md "Logger pattern: applies to all 8 services' main.go"):
    ```
    versionLDFlag := envOr("BUILD_VERSION", "dev")
    logger := observability.NewSlogJSONHandler(observability.Config{
        ServiceName: "<svc>",                       // hardcode per service: "identity", "activity-sync", ...
        Env:         envOr("ENV", "prod"),
        Version:     versionLDFlag,
        Level:       observability.ParseLevel(envOr("LOG_LEVEL", "info")),
    })
    slog.SetDefault(logger)
    ```

    Add the import: `"github.com/runningecosystem/backend/pkg/observability"` (use the actual module path from `services/backend/pkg/go.mod` — likely `github.com/runningecosystem/backend/pkg`; verify by reading `pkg/clientversion`'s import path).

    **The other middleware layers (Promhttp, OtelHTTP, SentryRecovery, DebugSession) are NOT wired in this plan** — Plan 05-04 adds Promhttp, Plan 05-05 adds OtelHTTP + SentryRecovery, Plan 05-06 adds DebugSession. This plan only swaps the slog default.

    Per-service ServiceName mapping (hardcoded constants — also used as Sentry tag in Plan 05-05 per RESEARCH §1.9):
    - `identity` (:8081), `activity-sync` (:8082), `feed` (:8083), `media` (:8084), `messaging` (:8085), `notifications` (:8086), `realtime-gw` (:8087), `social-graph` (:8088), `gateway` (port from existing main.go envOr).

    **Step 3.2 — Create scripts/pii_audit.sh** per D-14 + PATTERNS.md `pii_audit.sh` analog:

    Header per `scripts/setup-branch-protection.sh` style — `#!/usr/bin/env bash`, `set -euo pipefail`, phase anchor `# Phase 5 / OBS-06 / D-14 — grep audit for PII attr names in slog calls`.

    Body:
    ```
    # Match any slog.*Context(..., "<deny-key>", ...) anywhere in services/backend (excluding _test.go).
    # The regex enumerates D-12 keys explicitly so future renames stay visible.
    PII_KEYS='code|otp_code|otp|phone|phone_number|phoneNumber|tel|displayName|display_name|external_uuid|strava_external_id|garmin_external_id|lat|lon|latitude|longitude|coords|gps|location|dm_content|message_body|mapbox_token|strava_token|jwt|access_token|refresh_token|password'

    if grep -rnE "slog\\.\\w+Context\\([^)]*\"(${PII_KEYS})\"" \
        --include='*.go' --exclude='*_test.go' \
        services/backend/ ; then
        echo "❌ PII attribute leak detected — see grep hits above" >&2
        echo "    Defense-in-depth: handler still scrubs, but call-sites must use scrubbed alternatives." >&2
        echo "    Exception: identity/internal/service/otp.go DebugContext gated by 'if devMode' (D-13)" >&2
        exit 1
    fi
    echo "✓ PII grep audit clean"
    exit 0
    ```

    **EXCEPT** for the one legitimate `slog.DebugContext(..., "code", code)` at otp.go:70-80 (the gated devMode echo per D-13). Add an allowlist excerpt: `--exclude='otp.go'` is too broad. Instead, the grep regex requires the call NOT be inside an `if devMode` block — but grep is line-based and can't see context. Practical solution: filter out the specific known-legit line with a `grep -v` after the match:
    ```
    LEAK=$(grep -rnE "..." ... | grep -v 'identity/internal/service/otp.go.*DebugContext.*otp dev-mode echo' || true)
    if [ -n "$LEAK" ]; then echo "$LEAK"; exit 1; fi
    ```
    Document the allowlist in the script header. Future engineers adding a second exception must update both the grep filter AND the script header rationale.

    Minimum 30 lines including header + the explicit D-12 key listing + comments.

    **Step 3.3 — Add `pii-audit` job to `.github/workflows/backend-ci.yml`** per PATTERNS.md `backend-ci.yml` MODIFY:

    Append to the `jobs:` block (do NOT replace existing `test` / `lint` / `gosec` jobs):
    ```
    pii-audit:
      name: PII Audit (slog grep)
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - name: Run PII grep audit
          run: bash scripts/pii_audit.sh
    ```

    **Step 3.4 — Extend `scripts/setup-branch-protection.sh` required-status-checks** to include the new job per CICD-06 carry-forward:

    Read the current `setup-branch-protection.sh`. Find the `contexts:` array (or wherever the required checks are listed). Append the string `"PII Audit (slog grep)"`. Document the change inline with a comment `# Phase 5 / OBS-06 — pii-audit job blocks PR merge on leak`.

    **Smoke run from dev workstation:** `bash scripts/pii_audit.sh` exits 0 on current tree (after Task 2's D-13 fix landed the allowed DebugContext call).
  </action>
  <verify>
    <automated>
      cd services/backend \
        && go build ./... \
        && for svc in identity activity-sync feed media messaging notifications realtime-gw social-graph gateway; do \
              grep -q "observability.NewSlogJSONHandler" "$svc/cmd/server/main.go" || { echo "missing wire in $svc"; exit 1; }; \
              grep -q "ServiceName: \"$svc\"" "$svc/cmd/server/main.go" || { echo "wrong ServiceName in $svc"; exit 1; }; \
           done \
        && cd .. \
        && bash scripts/pii_audit.sh \
        && test $(wc -l < scripts/pii_audit.sh) -ge 30 \
        && grep -q "pii-audit:" .github/workflows/backend-ci.yml \
        && grep -q "PII Audit (slog grep)" .github/workflows/backend-ci.yml \
        && grep -q "PII Audit (slog grep)" scripts/setup-branch-protection.sh
    </automated>
  </verify>
  <done>
    All 9 main.go files use observability.NewSlogJSONHandler with correct ServiceName; pii_audit.sh exits 0 on current tree; CI gains `pii-audit` job; branch protection list updated to include the new required check. `go build ./...` from services/backend/ compiles cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code → slog.Default() | Every log call funnels through piiScrubHandler — single chokepoint for log-line redaction |
| log output (stdout) → docker logs | Container stdout is captured by docker daemon; Plan 05-06 ships via Alloy to Loki on sentry VPS |
| CI workflow → main branch | `pii-audit` job is a required check via branch protection (CICD-06 + this plan's update) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01-I | Info disclosure | PII attr leak in slog call site | mitigate | Defense-in-depth — D-13/D-14 cover call-site (otp.go gate + CI grep) and handler-level (PIIDenyList drop). Plan 05-04 adds runtime probe via cardinality_probe.py for metrics; Plan 05-06 adds pii_live_probe.py for runtime Loki sampling. |
| T-05-03-02-I | Info disclosure | OTP `code` field in production logs | mitigate | D-13 explicit fix: drop from InfoContext + DebugContext gated by devMode + handler still scrubs at LevelDebug |
| T-05-03-03-I | Info disclosure | Email in plaintext logs | mitigate | EmailHashKeys → SHA-256 first-8 hex; operational correlation preserved without plaintext |
| T-05-03-04-T | Tampering | CI grep audit bypass via clever attr name (e.g., `c0de`) | accept | D-14 grep is one defense layer; handler PIIDenyList is the actual security boundary. New attribute names with PII payload still emit unless added to deny-list — accepted residual risk; mitigated by code review |
| T-05-03-05-D | DoS | Handler scrub overhead on hot paths | accept | SHA-256 + map lookup is ≤1µs per attr; well under Go runtime noise floor |
| T-05-03-06-S | Spoofing | Forged request_id in context | accept | request_id is correlation aid, not auth; spoofed IDs simply produce unhelpful logs |

</threat_model>

<verification>
1. `go test -race ./services/backend/pkg/observability/...` exits 0 (all 10+ tests GREEN)
2. `go test -race ./services/backend/identity/internal/service/... -run "TestRequestCode_"` exits 0 (3 D-13 tests GREEN)
3. `go build ./...` from services/backend/ succeeds (all 9 services + gateway compile after main.go modifications)
4. `bash scripts/pii_audit.sh` exits 0
5. Run a synthetic emit: `(cd services/backend/identity && go run cmd/server/main.go &) && sleep 2; pkill -f 'go run cmd/server'` produces stdout JSON lines with `"service":"identity"`, `"env":"prod"`, `"version":"<sha>"`
6. CI `pii-audit` job appears in backend-ci.yml + is in branch protection required-checks
7. `grep -nE 'slog\.\w+Context\([^)]*"code"' services/backend/identity/internal/service/otp.go` returns exactly one match (the gated DebugContext per D-13)
</verification>

<success_criteria>
Plan 05-03 is complete when:
- [ ] pkg/observability package exists with slog_handler.go + pii_deny_list.go + tests
- [ ] D-13 fix landed in otp.go:70-74 + 3 no-leak tests GREEN
- [ ] All 9 main.go files swapped to observability.NewSlogJSONHandler with correct ServiceName
- [ ] scripts/pii_audit.sh exits 0 on current tree
- [ ] CI `pii-audit` job added + branch protection list updated
- [ ] `go build ./...` from services/backend/ succeeds
- [ ] No regression: existing tests (clientversion, featureflags, etc.) still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-observability-backend/05-03-SUMMARY.md` documenting: pkg/observability LOC (target ~400 across source + tests), D-13 fix verification (3 tests GREEN), 9 services wired, scripts/pii_audit.sh + CI gate state. Plan 05-05 unblocked (will extend pkg/observability with otel_init.go + sentry_init.go importing PIIDenyList from this plan).
</output>
