---
phase: 05-observability-backend
plan: 06
type: execute
wave: 4
depends_on: [05-04, 05-05, 05-07]
revised_at: 2026-05-19
revised_per: docs/DECISIONS/0010-sentry-saas-and-colocation.md
revision_summary: "Topology reshape per ADR-0010 (Sentry SaaS + Loki/Grafana/Prom colocated on srv1561293). Carrier swaps for executor to apply consistently: (a) `sentry.<sentry-ip>.sslip.io` → `o<org>.ingest.sentry.io` (SaaS) for Sentry events, OR `https://82.25.71.215:8443` (Caddy on srv1561293) for Loki/Grafana/Prom; (b) `:3100` direct push → `/loki/api/v1/push` Caddy-fronted on :8443; (c) `grafana.<sentry-ip>.sslip.io` → `https://82.25.71.215:8443/grafana/` (basicauth); (d) Sentry self-hosted topology references in §1 of the new docs/RUNBOOKS/observability.md MUST be rewritten to point at ADR-0010 + Sentry SaaS; sentry-stack deploy section (§11) replaced by observability-stack deploy section already shipped via Plan 05-07 (RUNBOOK §7); (e) acceptance walkthrough URLs swap correspondingly (Sentry UI = sentry.io/organizations/<slug>/, Grafana = https://82.25.71.215:8443/grafana/, the 3 probes unchanged); (f) Alloy `tls_config { insecure_skip_verify = true }` per R-03 mitigation since Caddy uses self-signed cert per D-37."
files_modified:
  - services/backend/pkg/observability/debug_session_middleware.go
  - services/backend/pkg/observability/debug_session_middleware_test.go
  - services/backend/pkg/featureflags/flags.go
  - services/backend/identity/cmd/server/main.go
  - services/backend/activity-sync/cmd/server/main.go
  - services/backend/feed/cmd/server/main.go
  - services/backend/media/cmd/server/main.go
  - services/backend/messaging/cmd/server/main.go
  - services/backend/notifications/cmd/server/main.go
  - services/backend/realtime-gw/cmd/server/main.go
  - services/backend/social-graph/cmd/server/main.go
  - services/backend/gateway/cmd/server/main.go
  - infra/ansible/roles/alloy-shipper/tasks/main.yml
  - infra/ansible/roles/alloy-shipper/templates/alloy-config.alloy.j2
  - infra/ansible/roles/alloy-shipper/templates/alloy.service.j2
  - infra/ansible/site.yml
  - scripts/pii_live_probe.py
  - docs/RUNBOOKS/sentry-ops.md
  - docs/RUNBOOKS/observability.md
  - docs/RUNBOOKS/deploy.md
  - docs/DECISIONS/0009-observability-architecture.md
  - docs/TELEMETRY.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [OBS-06, OBS-08]
must_haves:
  truths:
    - "DebugSessionMiddleware sets slog.LevelDebug for a request ONLY when X-Debug-Session=1 header AND JWT claim is_tester=true AND featureflag tester_debug_logs=ON for that user; missing any one of the three → LevelInfo (RESEARCH §1.8 — header-only is a debug-DoS vector, per D-22)"
    - "Featureflag tester_debug_logs is registered in pkg/featureflags with default=false (admin-controlled rollout via Phase 1 REL-03 admin UI per D-23)"
    - "Alloy shipper (replacing Promtail per RESEARCH §1.6 / §P20 — Promtail EOL 2026-03-02) is installed via systemd on the prod VPS (NOT containerized per D-29) and ships Docker container stdout to https://82.25.71.215:8443/loki/api/v1/push (AMENDED 2026-05-19 per ADR-0010 — was https://sentry.<sentry-ip>.sslip.io:3100; now Caddy-fronted :8443 with @allowed_loki matcher per D-36; Alloy uses tls_config insecure_skip_verify=true per D-37 for v1.0 self-signed cert)"
    - "Alloy config pairs discovery.docker + loki.source.docker components per RESEARCH §P21 (NOT loki.source.file over docker logs); alloy user is in the docker group so /var/run/docker.sock is readable (RESEARCH §P17)"
    - "scripts/pii_live_probe.py queries Loki query_range over the last 60 sec and regex-scans every log line for GPS coords, phone, displayName, external_uuid, and OTP — exits 0 with stdout '0 PII matches in N log lines' on clean capture; non-zero with offending excerpt on match (closes OBS-06 runtime row)"
    - "Final acceptance walkthrough signed off by user: Sentry UI accessible at https://sentry.io/organizations/<org-slug>/ (AMENDED 2026-05-19 per ADR-0010 — was sentry.<ip>.sslip.io; SaaS-hosted), 4 Grafana panels (P99 / error-rate / queue-depth / JWT-failures) populated within 5 min of smoke_metrics.py --load 60 at https://82.25.71.215:8443/grafana/ (Caddy basicauth), pii_live_probe.py green, cardinality_probe.py green, smoke_sentry.py green, Telegram P0 alert received in @running-ecosystem-alerts chat"
    - "ROADMAP §Phase 5 checkbox flipped from [ ] to [x] and 05-06-SUMMARY.md written with closeout (including explicit note that OBS-08 mobile-side UX + RU consent banner = Phase 17 territory)"
  artifacts:
    - path: "services/backend/pkg/observability/debug_session_middleware.go"
      provides: "DebugSessionMiddleware(ff featureflags.Store) func(http.Handler) http.Handler — reads X-Debug-Session header AND JWT is_tester claim AND tester_debug_logs featureflag; all three true → ctx carries slog.LevelDebug via WithLogLevel; any missing → passthrough at LevelInfo"
      contains: "DebugSessionMiddleware"
      min_lines: 90
    - path: "services/backend/pkg/observability/debug_session_middleware_test.go"
      provides: "TestDebugSessionMiddleware_AllThreeTrue (asserts ctx LogLevel=Debug when all 3 gates met) + TestDebugSessionMiddleware_DefaultsToInfo (3 sub-tests: no header / no JWT claim / featureflag off — each stays at LevelInfo)"
      contains: "TestDebugSessionMiddleware_AllThreeTrue"
      min_lines: 110
    - path: "infra/ansible/roles/alloy-shipper/tasks/main.yml"
      provides: "Idempotent Grafana Alloy install on prod VPS — add Grafana apt repo + GPG key, apt install alloy at pinned version, add alloy user to docker group (RESEARCH §P17), template /etc/alloy/config.alloy + override systemd unit, enable + start; creates: guards on each side-effect task"
      contains: "alloy-shipper"
      min_lines: 60
    - path: "infra/ansible/roles/alloy-shipper/templates/alloy-config.alloy.j2"
      provides: "Alloy config using discovery.docker + loki.source.docker pair per RESEARCH §P21 — NOT loki.source.file; writes to https://{{ sentry_caddy_host }}/loki/api/v1/push; labels env + cluster only (RESEARCH §P16 — request_id NEVER a Loki label)"
      contains: "loki.source.docker"
      min_lines: 40
    - path: "scripts/pii_live_probe.py"
      provides: "Queries Grafana Loki proxy datasource over last --duration seconds, regex-scans every log line against PII_PATTERNS (GPS / phone / displayName / external_uuid / OTP), prints 'N PII matches in M log lines'; sys.exit(1) on first match; stdlib-only urllib + json per smoke_otp.py precedent"
      contains: "PII_PATTERNS"
      min_lines: 100
    - path: "docs/DECISIONS/0009-observability-architecture.md"
      provides: "ADR-0009 documenting (§1) Sentry self-host topology + separate VPS isolation redline (D-01..D-05), (§2) slog choice (D-09..D-12), (§3) OTLP-to-Sentry (D-20..D-21 + RESEARCH §1.3), (§4) Telegram alerting (D-24..D-26 + RESEARCH §1.5), (§5) single prod-backend project + per-service tag (RESEARCH §1.9); uses ADR-0007 structure (Дата / Статус / Контекст / Решение / sub-decisions)"
      contains: "0009"
      min_lines: 80
    - path: "docs/RUNBOOKS/sentry-ops.md"
      provides: "Operational runbook — Sentry admin user creation, sentry-cli bootstrap (RESEARCH §1.2), Telegram bot setup (USER ACTION 2 recap), retention tuning knobs (RESEARCH §1.10), backup procedure, §Acceptance Walkthrough checklist (used by Task 6), P12-P21 pitfall reference appendix"
      contains: "Acceptance Walkthrough"
      min_lines: 120
    - path: "docs/RUNBOOKS/observability.md"
      provides: "Engineer-facing reference — slog usage conventions, metric naming rules (D-17), PII deny-list reference (D-12), cardinality budget rules (D-18), how to add new metrics safely, X-Debug-Session protocol description (RESEARCH §1.8 three-gate model)"
      contains: "PII deny-list"
      min_lines: 100
    - path: "docs/TELEMETRY.md"
      provides: "Skeleton (≤80 lines) — single section 'Opt-in Telemetry Event Allowlist' with placeholder table (event_name / description / PII risk / consent required); points to D-12 deny-list as master ruleset; documents that Phase 17 mobile-side populates it"
      contains: "Opt-in Telemetry"
      min_lines: 40
  key_links:
    - from: "9 services' cmd/server/main.go (identity, activity-sync, feed, media, messaging, notifications, realtime-gw, social-graph, gateway)"
      to: "services/backend/pkg/observability/debug_session_middleware.go"
      via: "Handler chain mounts DebugSessionMiddleware as OUTERMOST observability layer — placement is AFTER auth (so JWT claims are populated in r.Context()) and BEFORE business handlers; wraps the existing 4-layer stack from Plan 05-05 (Promhttp → SentryRecovery → OtelHTTP → clientversion)"
      pattern: "DebugSessionMiddleware"
    - from: "prod VPS alloy systemd unit (/etc/alloy/config.alloy)"
      to: "Loki on sentry VPS port 3100"
      via: "loki.write endpoint posts to https://82.25.71.215:8443/loki/api/v1/push (AMENDED 2026-05-19 per ADR-0010 + D-36 — Caddy @allowed_loki matcher replaces UFW :3100 rule; net security identical); Alloy tls_config insecure_skip_verify=true for v1.0 self-signed cert per D-37"
      pattern: "loki.source.docker"
    - from: "scripts/pii_live_probe.py"
      to: "Grafana Loki proxy datasource (sentry VPS)"
      via: "GET https://grafana.<sentry-ip>.sslip.io/api/datasources/proxy/<loki-id>/loki/api/v1/query_range?query={service=~\".+\"}&start=<now-60s>&end=<now>; regex-scan line content against PII_PATTERNS table"
      pattern: "PII_PATTERNS"
    - from: "Featureflag registration in pkg/featureflags/flags.go"
      to: "DebugSessionMiddleware ff.IsEnabled call"
      via: "Phase 1 REL-03 Postgres-backed store; admin UI flips per-user rollout; default=false ensures gate is OFF until explicit admin grant"
      pattern: "tester_debug_logs"
---

<objective>
Close Phase 5 with the final two seams + acceptance walkthrough.

**Backend seam — OBS-08 (D-22 / D-23):** Add `DebugSessionMiddleware` to `pkg/observability/` that elevates per-request log level to `slog.LevelDebug` only when ALL THREE conditions hold: `X-Debug-Session: 1` header, JWT claim `is_tester=true` (Phase 1 issues JWTs; this claim is added by `identity/internal/service/auth.go` issuePair upstream), and the `tester_debug_logs` featureflag is ON for that user (admin-controlled rollout via Phase 1 REL-03 store). Per RESEARCH §1.8 — header-only is a debug-DoS vector (anonymous attacker inflates Loki volume, leaks pre-auth debug state). Wire as the OUTERMOST observability layer in all 9 services' `cmd/server/main.go` (placement: after auth so JWT claims are in context, before business handlers).

**Log shipping — OBS-06 closure (D-27 / D-29 with RESEARCH §1.6 substitution):** New Ansible role `alloy-shipper` (NOT `promtail-shipper` — Promtail EOL 2026-03-02 per RESEARCH §P20). Installed via apt on the prod VPS, NOT the sentry VPS. systemd-managed (not containerized — independence from `sport-stack.service` per D-29). Ships container stdout via `discovery.docker` + `loki.source.docker` pair (RESEARCH §P21 — they must be used together) to Loki on sentry VPS at port 3100 (UFW-restricted to prod VPS source IP only per D-28).

**Runtime PII closure — OBS-06 runtime row:** New `scripts/pii_live_probe.py` — tails Loki via Grafana proxy datasource, regex-scans every captured log line against GPS / phone / displayName / external_uuid / OTP patterns. Exit 0 on clean capture. Plan 05-03 + 05-04 + 05-05 ensure PII never reaches the wire; this probe is the runtime audit that proves it.

**Documentation — phase-closing artifacts:** ADR-0009 (observability architecture), `sentry-ops.md` + `observability.md` runbooks, `deploy.md §11 sentry-stack deploy + §12 sentry rollback`, `TELEMETRY.md` skeleton (Phase 17 consumes).

**Final acceptance walkthrough (USER ACTION 4, autonomous=false):** Single task at the end — user runs the three probes, opens 4 Grafana panels, triggers a Telegram P0 alert, confirms RU consent banner is deferred to Phase 17, flips the ROADMAP §Phase 5 checkbox to [x], writes the 05-06-SUMMARY.md.

Purpose: This plan closes the last two OBS requirements (OBS-06 runtime + OBS-08 backend seam) and ships the documentation set + ADR that Phase 17 (CRASH-01..06 + mobile Settings toggle + RU consent banner) inherits as a dependency.

Output: `pkg/observability/debug_session_middleware.go` + tests, 9 service `main.go` files with `DebugSessionMiddleware` mounted as the outermost layer, Alloy log shipping live on prod VPS, `scripts/pii_live_probe.py` green, ADR-0009 + 3 runbook files committed, ROADMAP §Phase 5 checkbox flipped to [x], 05-06-SUMMARY.md written. After this plan: Phase 5 is CLOSED. Phase 6 (EDGE / DDoS) and Phase 17 (mobile observability) inherit the seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-observability-backend/05-CONTEXT.md
@.planning/phases/05-observability-backend/05-RESEARCH.md
@.planning/phases/05-observability-backend/05-VALIDATION.md
@.planning/phases/05-observability-backend/05-PATTERNS.md
@.planning/phases/05-observability-backend/05-04-SUMMARY.md
@.planning/phases/05-observability-backend/05-05-SUMMARY.md

# Source files the executor reads to copy patterns:
@services/backend/pkg/clientversion/middleware.go
@services/backend/pkg/clientversion/middleware_test.go
@services/backend/pkg/featureflags/featureflags.go
@services/backend/pkg/observability/slog_handler.go
@services/backend/pkg/observability/pii_deny_list.go
@services/backend/identity/cmd/server/main.go
@scripts/smoke_otp.py
@infra/ansible/roles/common/tasks/main.yml
@infra/ansible/roles/sport-stack/tasks/main.yml
@docs/RUNBOOKS/sops-edit.md
@docs/RUNBOOKS/deploy.md
@docs/DECISIONS/0007-v1.0-release-contract.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase + Plan 05-03/05-05 outputs. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From services/backend/pkg/featureflags/featureflags.go (Phase 1 REL-03):
  type Store interface {
      IsEnabled(ctx context.Context, flag string, userID string) bool
      // Register(name string, defaultEnabled bool) — see flags.go registration table
  }
  NewPostgresStore(pool *pgxpool.Pool, ttl time.Duration) *Store

From services/backend/pkg/observability/slog_handler.go (Plan 05-03 output):
  func NewSlogJSONHandler(cfg Config) *slog.Logger
  // ctx-attached log level helpers used by DebugSessionMiddleware:
  func WithLogLevel(ctx context.Context, level slog.Level) context.Context
  func LogLevelFromContext(ctx context.Context) slog.Level  // defaults to LevelInfo

From services/backend/pkg/observability/pii_deny_list.go (Plan 05-03 output):
  var PIIDenyList map[string]struct{}  // shared with OTel span processor per D-21

From Phase 1 identity service (JWT claims — already issued; this plan reads them):
  type Claims struct {
      UserID    string
      IsTester  bool   // Phase 1 issuer adds this; default false; admin flips via users.is_tester column
      ExpiresAt int64
  }
  func ClaimsFromContext(ctx context.Context) (*Claims, bool)  // returned by auth middleware upstream

From services/backend/pkg/clientversion/middleware.go (canonical middleware shape):
  func Middleware(next http.Handler, p Policy, log *slog.Logger) http.Handler
  // Outermost-wrap pattern; HTTP header parse + early-return graceful pass-through

Pinned Alloy version (RESEARCH §4): v1.5.0 (Grafana apt repo, NOT containerized per D-29)
Pinned sentry-cli version (RESEARCH §4): 2.40.0 (RESEARCH §P18 — pin to avoid JSON-output schema drift)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 TDD — DebugSessionMiddleware test scaffold + featureflag registration + minimal handler</name>
  <files>
    services/backend/pkg/observability/debug_session_middleware_test.go,
    services/backend/pkg/observability/debug_session_middleware.go,
    services/backend/pkg/featureflags/flags.go
  </files>
  <read_first>
    - services/backend/pkg/clientversion/middleware_test.go (canonical table-driven HTTP-handler test shape; reuse newLogger() + runRequest() + stubNext helpers)
    - services/backend/pkg/clientversion/middleware.go lines 25-75 (header-parse + early-return + context-attach idiom — DO NOT use slog.WarnContext on missing header here, this middleware is silent-pass-through per RESEARCH §1.8 to avoid logging a debug-DoS attempt as warning-floor noise)
    - services/backend/pkg/featureflags/featureflags.go (Store interface; IsEnabled signature — confirm `(ctx, flag, userID) bool`)
    - services/backend/pkg/observability/slog_handler.go (WithLogLevel / LogLevelFromContext helpers from Plan 05-03)
    - .planning/phases/05-observability-backend/05-RESEARCH.md §1.8 (three-gate threat model — header alone is debug-DoS)
    - .planning/phases/05-observability-backend/05-PATTERNS.md §debug_session_middleware.go (Adapt block)
  </read_first>
  <behavior>
    - Test 1 — TestDebugSessionMiddleware_AllThreeTrue: request with header X-Debug-Session=1, JWT claims with IsTester=true in ctx, fake Store.IsEnabled("tester_debug_logs", userID) → true → assert next handler observes LogLevelFromContext(ctx) == slog.LevelDebug
    - Test 2 sub-A — TestDebugSessionMiddleware_DefaultsToInfo/no_header: request WITHOUT X-Debug-Session header → next observes LogLevel=Info (default)
    - Test 2 sub-B — .../no_jwt_claim: header set, but no Claims in ctx OR IsTester=false → next observes LogLevel=Info
    - Test 2 sub-C — .../featureflag_off: header + claim both set, but Store.IsEnabled returns false → next observes LogLevel=Info
    - Test 3 — TestDebugSessionMiddleware_PreAuthPath: header set, no JWT in ctx (pre-auth path like /healthz, /metrics, /auth/request-code) → MUST NOT elevate; next observes LogLevel=Info (proves the "no JWT → no elevation, even if header set" guarantee from RESEARCH §1.8)
    - Test 4 — TestFeatureflag_TesterDebugLogs_DefaultFalse: register tester_debug_logs in flags.go default false; assert Store.IsEnabled returns false for an unknown user (rollout off by default per D-23)
  </behavior>
  <action>
    Write debug_session_middleware_test.go FIRST (red phase). Mirror clientversion/middleware_test.go structure: t.Parallel(), newLogger() helper, stubNext handler-spy with atomic.Int32 counter, runRequest() test helper that takes (headerSet bool, claims *auth.Claims, ffEnabled bool) and returns the LogLevel observed by stubNext.

    Then write the minimal middleware to make tests pass (green phase):
    - Signature: `func DebugSessionMiddleware(ff featureflags.Store) func(http.Handler) http.Handler` (returns a wrapper, matches Plan 05-05 chain composition pattern from D-32).
    - Logic per RESEARCH §1.8:
      1. If r.Header.Get("X-Debug-Session") != "1" → next.ServeHTTP(w, r); return.
      2. claims, ok := auth.ClaimsFromContext(r.Context()); if !ok || !claims.IsTester → next.ServeHTTP(w, r); return. (Covers pre-auth paths: no JWT means no Claims in ctx; can't elevate.)
      3. if !ff.IsEnabled(r.Context(), "tester_debug_logs", claims.UserID) → next.ServeHTTP(w, r); return.
      4. ctx := observability.WithLogLevel(r.Context(), slog.LevelDebug); next.ServeHTTP(w, r.WithContext(ctx)).
    - DO NOT emit slog at any branch (RESEARCH §1.8 — pre-auth attacker spamming the header should not also generate warn-floor log noise; the gate must be silent).
    - NO ELSE — middleware is silent pass-through on every failed gate.

    Register the featureflag in pkg/featureflags/flags.go (single file holds the flag registration table per Phase 1 REL-03 pattern):
    - Add entry: `tester_debug_logs` with default=false and description "Per-user debug-log elevation gate; coupled with X-Debug-Session header + JWT is_tester claim per OBS-08 / RESEARCH §1.8."
    - If flags.go does not yet exist (Phase 1 may have registration via direct DB seed), create flags.go with a `var KnownFlags = []FlagSpec{...}` table and call site that the existing PostgresStore consumes; mirror whatever Plan 01-XX SUMMARY documents as the registration seam.

    NO fenced multi-page code in the executor's response — keep all code inside the actual middleware + test files; the action above is directive prose.

    Per D-23: this only ships the BACKEND seam. Mobile-side Settings toggle + RU consent banner UX = Phase 17. Closeout SUMMARY must flag the split.
  </action>
  <verify>
    <automated>
      cd services/backend && go test -race -run 'TestDebugSessionMiddleware|TestFeatureflag_TesterDebugLogs' ./pkg/observability/... ./pkg/featureflags/...
    </automated>
  </verify>
  <done>
    debug_session_middleware.go exists with DebugSessionMiddleware(ff) wrapper enforcing 3-gate logic per RESEARCH §1.8; debug_session_middleware_test.go has TestDebugSessionMiddleware_AllThreeTrue + TestDebugSessionMiddleware_DefaultsToInfo (3 sub-tests) + TestDebugSessionMiddleware_PreAuthPath, all GREEN; pkg/featureflags/flags.go registers tester_debug_logs with default=false; `go test -race` exits 0; no slog.* emission anywhere in middleware (grep -c 'slog\.' debug_session_middleware.go returns 0 excluding import line).
  </done>
</task>


<task type="auto">
  <name>Task 2: Wire DebugSessionMiddleware into 9 services' cmd/server/main.go as OUTERMOST observability layer</name>
  <files>
    services/backend/identity/cmd/server/main.go,
    services/backend/activity-sync/cmd/server/main.go,
    services/backend/feed/cmd/server/main.go,
    services/backend/media/cmd/server/main.go,
    services/backend/messaging/cmd/server/main.go,
    services/backend/notifications/cmd/server/main.go,
    services/backend/realtime-gw/cmd/server/main.go,
    services/backend/social-graph/cmd/server/main.go,
    services/backend/gateway/cmd/server/main.go
  </files>
  <read_first>
    - services/backend/identity/cmd/server/main.go (canonical bootstrap; locate Plan 05-05's existing 4-layer chain — Promhttp → SentryRecovery → OtelHTTP → clientversion → mux; identify the auth middleware boundary so DebugSession can be placed AFTER auth populates JWT claims into r.Context())
    - .planning/phases/05-observability-backend/05-05-SUMMARY.md (Plan 05-05's middleware-chain shape after its modifications; confirms exact existing layer order before this plan adds the 5th layer)
    - .planning/phases/05-observability-backend/05-CONTEXT.md D-32 (handler chain shape; DebugSessionMiddleware position — between auth and business handlers, OUTERMOST of the observability bundle so it sees the JWT claims attached by auth)
    - services/backend/pkg/observability/debug_session_middleware.go (Task 1 output; confirm the wrapper signature func(http.Handler) http.Handler returned by DebugSessionMiddleware(ff))
    - services/backend/pkg/clientversion/middleware.go (already wired upstream — debug session sits OUTSIDE of clientversion so version-check happens first, but INSIDE of auth so JWT claims are available; if auth lives in identity's handler-level middleware not main.go, the placement note adjusts accordingly per service)
  </read_first>
  <action>
    For each of the 9 services (identity, activity-sync, feed, media, messaging, notifications, realtime-gw, social-graph, gateway), modify `cmd/server/main.go` to insert DebugSessionMiddleware into the existing observability chain established by Plan 05-05.

    **Chain placement (per D-32 + RESEARCH §1.8):** DebugSessionMiddleware is the OUTERMOST observability layer (wraps Plan 05-05's `rootHandler`). The 9th-service composition becomes:

      rootHandler = observability.DebugSessionMiddleware(flagStore)(
        observability.PromhttpMiddleware(serviceName,
          observability.SentryRecoveryMiddleware(
            observability.OtelHTTPMiddleware(versionedMux))))

    **Auth boundary:** The middleware reads `auth.ClaimsFromContext(r.Context())`. Confirm at read time that the auth middleware (Phase 1 / `pkg/auth/middleware.go` or equivalent) is mounted BEFORE this handler chain — i.e. claims are populated INSIDE r.Context() by the time the request reaches DebugSessionMiddleware. If auth currently lives WITHIN the business-handler mux (per-route), then DebugSessionMiddleware sees no claims for pre-auth paths and silently passes through at LevelInfo — that is the CORRECT behavior per RESEARCH §1.8 (pre-auth path = no debug elevation).

    **Featureflag wiring:** Each service already has `flagStore := featureflags.NewPostgresStore(pool, 30*time.Second)` from Phase 1 REL-03 (per CONTEXT.md code_context line; if any service lacks it, add per the identity main.go template). Pass `flagStore` to `DebugSessionMiddleware(flagStore)`.

    **Gateway nuance:** gateway is the edge proxy; it has /healthz + /metrics + reverse-proxy handlers. Wire DebugSessionMiddleware at the gateway main.go too, so debug elevation propagates downstream via the X-Debug-Session header (gateway forwards it to backend services); downstream services then re-evaluate the gate on their own auth context.

    **No behavior change to other layers** — Promhttp / SentryRecovery / OtelHTTP / clientversion stay identical to Plan 05-05 output. This task adds exactly ONE wrap layer per service.

    **Closeout sanity check** (per-service in commit body): grep that `observability.DebugSessionMiddleware` appears exactly once in each main.go, and that the wrap order places it OUTSIDE Promhttp (the request counter then sees the elevation in ctx for any downstream log emission, which is what we want — debug-elevated requests still get metric'd normally).
  </action>
  <verify>
    <automated>
      cd services/backend && go build ./... && \
      for svc in identity activity-sync feed media messaging notifications realtime-gw social-graph gateway; do \
        grep -q "observability.DebugSessionMiddleware" "$svc/cmd/server/main.go" || { echo "missing DebugSessionMiddleware in $svc"; exit 1; }; \
      done && \
      go test -race ./pkg/observability/...
    </automated>
  </verify>
  <done>
    All 9 main.go files mount DebugSessionMiddleware as the outermost observability layer (wrapping Plan 05-05's 4-layer chain); `go build ./...` exits 0; pkg/observability tests stay GREEN; grep confirms exactly one DebugSessionMiddleware reference per service; chain order documented in PR body matches D-32 shape.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ansible alloy-shipper role (NEW) — install + config + systemd + site.yml wiring</name>
  <files>
    infra/ansible/roles/alloy-shipper/tasks/main.yml,
    infra/ansible/roles/alloy-shipper/templates/alloy-config.alloy.j2,
    infra/ansible/roles/alloy-shipper/templates/alloy.service.j2,
    infra/ansible/site.yml
  </files>
  <read_first>
    - infra/ansible/roles/common/tasks/main.yml (apt-update + base-package pattern; legacy-docker.gpg heal block at lines 15-55 — mirror for Grafana apt repo + GPG key install)
    - infra/ansible/roles/sport-stack/tasks/main.yml lines 60-103 (systemd unit install + enable + smoke pattern; flush_handlers + reload-systemd notify; when: not ansible_check_mode guard)
    - .planning/phases/05-observability-backend/05-RESEARCH.md §1.6 (Alloy config skeleton — verbatim discovery.docker + loki.source.docker + loki.write blocks; pin Alloy v1.5.0)
    - .planning/phases/05-observability-backend/05-RESEARCH.md §P17 (alloy user must be added to docker group; UID 473 collision with root:docker GID 999 — fix via ansible.builtin.user with append: true)
    - .planning/phases/05-observability-backend/05-RESEARCH.md §P21 (discovery.docker + loki.source.docker MUST be paired; never hardcode container names)
    - .planning/phases/05-observability-backend/05-RESEARCH.md §P16 (Loki labels for slow-changing dims only — env + cluster; NEVER request_id; keeps stream cardinality bounded)
    - .planning/phases/05-observability-backend/05-PATTERNS.md §roles/alloy-shipper/ (Adapt block; Naming substitution note)
    - infra/ansible/site.yml current top-2-plays structure (append three plays per PATTERNS.md §site.yml — but only the alloy-shipper play for this task; sentry-prep + sentry bootstrap plays were authored by Plan 05-01 + 05-02)
    - infra/ansible/group_vars (look for sentry.yml authored by Plan 05-01 — pull sentry_caddy_host variable from there; this task only consumes it)
  </read_first>
  <action>
    Create `infra/ansible/roles/alloy-shipper/tasks/main.yml` with the following idempotent task sequence (Promtail → Alloy substitution per RESEARCH §P20):

    1. **Add Grafana apt key** — `ansible.builtin.get_url` fetches https://apt.grafana.com/gpg.key to `/etc/apt/keyrings/grafana.gpg`; mode 0644; `creates:` guard. Mirror common/tasks/main.yml legacy-docker.gpg heal block.
    2. **Add Grafana apt repo** — `ansible.builtin.apt_repository` adds `deb [signed-by=/etc/apt/keyrings/grafana.gpg] https://apt.grafana.com stable main` to `/etc/apt/sources.list.d/grafana.list`; update_cache: true on add only.
    3. **Install alloy at pinned version** — `ansible.builtin.apt: name=alloy=1.5.0* state=present` (per RESEARCH §4 v1.5.0 LTS-line; allow patch range with `=1.5.*`). NOT containerized per D-29.
    4. **Add alloy user to docker group** — `ansible.builtin.user: name=alloy groups=docker append=true` (RESEARCH §P17 — without this, loki.source.docker silently fails with permission denied on /var/run/docker.sock; alloy emits no logs for hours before operator notices). Register handler `notify: Restart alloy`.
    5. **Template /etc/alloy/config.alloy** from `alloy-config.alloy.j2` (Task content below). owner=alloy, group=alloy, mode=0640. Register handler `notify: Restart alloy`.
    6. **Template /etc/systemd/system/alloy.service.d/override.conf** from `alloy.service.j2` — only if we need to override anything beyond the apt-installed unit; this template's primary purpose is to add `EnvironmentFile=/etc/alloy/alloy.env` (if any env vars needed) and `Restart=always` hardening. Keep minimal — apt-installed unit is the canonical; we only override.
    7. **flush_handlers** — apply config + systemd-reload before enable.
    8. **Enable + start alloy** — `ansible.builtin.systemd: name=alloy enabled=true state=started`; `when: not ansible_check_mode`.
    9. **Smoke probe** — `ansible.builtin.command: journalctl -u alloy -n 50 --no-pager` then `register` + `failed_when: "'level=error' in result.stdout"`; also check `journalctl -u alloy -n 50 | grep -q 'level=info msg="started reader"'` per RESEARCH §P21 (one started-reader line per discovered container).
    10. **Define handlers/main.yml** — `Restart alloy` handler calls `systemd: name=alloy state=restarted daemon_reload=true`.

    **Template `alloy-config.alloy.j2`** — Jinja2 header comment + RESEARCH §1.6 skeleton verbatim:
    - `discovery.docker "containers" { host = "unix:///var/run/docker.sock" }` — discovers all running Docker containers (sport-stack services + sport-stack.service itself; ALL container stdout flows to Loki).
    - `loki.source.docker "containers" { host=…, targets=discovery.docker.containers.targets, forward_to=[loki.write.sentry_vps.receiver], labels={ env="{{ env_name }}", cluster="running-ecosystem" } }` — RESEARCH §P21 pair; RESEARCH §P16 labels limited to env+cluster (no request_id, no service per-container; Loki uses log content for filtering).
    - `loki.write "sentry_vps" { endpoint { url = "https://{{ sentry_caddy_host }}/loki/api/v1/push" } }` — Caddy on sentry VPS reverse-proxies /loki to the internal Loki :3100. UFW on sentry VPS allows source IP=prod-vps-ip/32 only per D-28.

    Note on port: If Caddy reverse-proxies, we use 443 to the public Caddy and Caddy routes /loki/* to the internal :3100. If we expose 3100 directly (no Caddy), use port 3100 explicitly. Plan 05-01 + 05-02 wired Caddy on sentry VPS — confirm at execution time whether the public push URL is :443 (via Caddy) or :3100 (direct); update the template URL accordingly. Default to the Caddy-fronted :443 path (simpler ACME, single TLS terminus).

    **Template `alloy.service.j2`** — minimal systemd override (apt unit is canonical):
    ```
    {# /etc/systemd/system/alloy.service.d/override.conf — Phase 5 / OBS-06.
       Adds restart-always hardening; the upstream apt unit handles binary path + user. #}
    [Service]
    Restart=always
    RestartSec=10
    ```
    NO additional ExecStart / User override — those come from the apt-installed unit (per D-29 "systemd not container" reasoning; we adopt upstream defaults except for hardening).

    **Modify `site.yml`** to append a third play (Plan 05-01 + 05-02 wrote the sentry-vps plays; this task adds the alloy play targeting prod VPS):
    ```yaml
    - name: Deploy Alloy log shipper to prod VPS (ships container stdout to Loki on sentry VPS per D-27)
      hosts: app_servers
      become: true
      roles: [alloy-shipper]
      tags: [alloy]
    ```
    Verify at write time that this play does not already exist (check for `roles: [alloy-shipper]` in site.yml first).

    **No SOPS dependency** — alloy config has no secrets (Loki push URL is not secret; UFW IP allowlist is the auth boundary per D-28). Skips the SOPS decrypt pattern used by sport-stack / sentry-prep roles.
  </action>
  <verify>
    <automated>
      cd infra/ansible && \
      ansible-playbook --syntax-check site.yml && \
      ansible-lint roles/alloy-shipper/ 2>&1 | tee /tmp/alloy-lint.txt && \
      grep -q "alloy-shipper" site.yml && \
      grep -q "loki.source.docker" roles/alloy-shipper/templates/alloy-config.alloy.j2 && \
      grep -q "discovery.docker" roles/alloy-shipper/templates/alloy-config.alloy.j2 && \
      grep -q "groups: docker" roles/alloy-shipper/tasks/main.yml
    </automated>
  </verify>
  <done>
    roles/alloy-shipper/tasks/main.yml + templates/alloy-config.alloy.j2 + templates/alloy.service.j2 all created; site.yml appends the alloy play targeting app_servers; ansible-playbook --syntax-check passes; ansible-lint emits no errors; config template pairs discovery.docker + loki.source.docker per RESEARCH §P21; alloy user added to docker group per §P17; ready for Wave 1 smoke-run via `ansible-playbook -i inventory/prod site.yml --tags alloy` (executed during Task 6 acceptance walkthrough, not by this task — this task only ships the role).
  </done>
</task>

<task type="auto">
  <name>Task 4: scripts/pii_live_probe.py — Loki tail + regex match counter (closes OBS-06 runtime row)</name>
  <files>
    scripts/pii_live_probe.py
  </files>
  <read_first>
    - scripts/smoke_otp.py (canonical smoke-script idiom — stdlib-only urllib + json; BASE env-overridable URL; req() helper with (status, body) tuple; must() assertion with checkmark; sys.exit(1) on first failure; 🎉 terminal print)
    - scripts/smoke_realtime_feed.py (similar shape; pattern for `--duration` arg parsing if present)
    - .planning/phases/05-observability-backend/05-CONTEXT.md D-12 (PII deny-list — but for log-LINE regex matching we test the CONTENTS not the attribute KEYS; the regex set is narrower: GPS lat/lon, phone, displayName values, external_uuid hex, OTP digits)
    - .planning/phases/05-observability-backend/05-RESEARCH.md §2 Validation Architecture (OBS-06 runtime row — assertion shape "0 PII matches in N log lines"; --duration 60 default)
    - .planning/phases/05-observability-backend/05-PATTERNS.md §scripts/pii_live_probe.py (Adapt block)
  </read_first>
  <action>
    Create `scripts/pii_live_probe.py` following the `smoke_otp.py` shape verbatim (stdlib-only, BASE env-overridable, `req()` + `must()` helpers, sys.exit(1) on failure, terminal celebration print).

    **Top-of-file header** (mirror smoke_otp.py): shebang + path comment + Phase 5 / OBS-06 anchor + idempotent re-run note.

    **Constants:**
    - `GRAFANA_BASE` env-overridable; default `https://grafana.<sentry-ip>.sslip.io` (read from env, no hardcode of sentry IP — sentry IP is filled in by Plan 05-01 USER ACTION).
    - `LOKI_DATASOURCE_ID` env-overridable; default `1` (Grafana's first datasource ID; resolve at runtime via /api/datasources if needed — but for v1.0 single datasource the ID is deterministic).
    - `DEFAULT_DURATION_SEC = 60`.
    - `PII_PATTERNS` — a `dict[str, re.Pattern]` keyed by PII category, value a compiled regex per RESEARCH §2 expectations:
      - "gps_coords": `r"\b-?\d{1,3}\.\d{3,}\b"` — matches anything with 3+ decimal places (GPS precision is ≥4dp; longitude/latitude both caught). False-positive risk: legitimate floats like prices may match — accept; the slog handler should never put any float into a log message anyway (numbers go in structured attrs, which the handler drops by KEY name — so a hit here indicates a PII bypass).
      - "phone": `r"\+?\d{10,15}"` — international phone format.
      - "external_uuid": `r"[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}"` — full UUIDv4 shape (external_uuid in Strava integration; also matches internal user_id but those should never be in log lines per D-18, so a hit is still a finding).
      - "otp_code": `r"\b\d{6}\b"` — 6-digit OTP. False-positive risk: zip codes, timestamps with 6 digits — accept; defense-in-depth (Plan 05-03 already drops by KEY; a regex hit here means somebody string-interpolated the code into a message).
      - "displayName_attr": `r"\"(displayName|display_name|name)\"\\s*:\\s*\"[A-Za-zА-Яа-яЁё]"` — JSON attr "displayName": "<text>"; catches structured field bypasses.

    **Argparse:** `--duration` int default 60; `--grafana-base` string default env GRAFANA_BASE; `--loki-datasource-id` int default 1.

    **Core logic:**
    1. Compute time range: `now = int(time.time() * 1e9)` (Loki uses ns); `start = now - duration * int(1e9)`.
    2. Build URL: `f"{grafana_base}/api/datasources/proxy/{loki_id}/loki/api/v1/query_range?query=" + urllib.parse.quote('{service=~".+"}') + f"&start={start}&end={now}&limit=5000"`.
    3. GET with auth — if `GRAFANA_API_KEY` env var present, attach `Authorization: Bearer <key>`; else attempt unauthenticated (Grafana sentry VPS uses basic auth per RESEARCH §1.4 — accept `BASIC_AUTH_USER` + `BASIC_AUTH_PASS` env vars and attach basic-auth header).
    4. Parse response JSON — Loki returns `{"data":{"result":[{"stream":{...labels...},"values":[[ns_ts,line], ...]}, ...]}}`.
    5. Iterate every value pair across every stream; for each `line`:
       - For each (name, pattern) in PII_PATTERNS: if pattern.search(line) → append to `matches` list as `{category, line_excerpt, stream_labels, timestamp}`.
       - Counter increment `total_lines`.
    6. Print `f"Scanned {total_lines} log lines across {len(streams)} streams over {duration}s window"`.
    7. If matches: print first 5 entries with category + line excerpt (≤120 chars) + stream service label; `sys.exit(1)` with summary `f"❌ {len(matches)} PII matches found — see excerpts above"`.
    8. Else: print `f"✓ 0 PII matches in {total_lines} log lines"` then `🎉 Phase 5 / OBS-06 runtime check passed.` + `sys.exit(0)`.

    **Error handling:** Loki query timeout → `urllib.error.URLError` → print friendly error + sys.exit(2) (distinct from PII finding); auth failures (401/403) → print "set GRAFANA_API_KEY or BASIC_AUTH_USER/PASS" + sys.exit(3).

    **No external deps** — stdlib only (`urllib.request`, `urllib.parse`, `urllib.error`, `json`, `re`, `sys`, `os`, `time`, `argparse`).

    **Reviewability:** keep total file <180 LOC; comment each PII_PATTERNS entry explaining the trade-off (false-positive risk vs detection coverage).
  </action>
  <verify>
    <automated>
      python3 -c "import ast; ast.parse(open('scripts/pii_live_probe.py').read())" && \
      python3 scripts/pii_live_probe.py --help 2>&1 | grep -q -- '--duration' && \
      grep -q "PII_PATTERNS" scripts/pii_live_probe.py && \
      grep -q "query_range" scripts/pii_live_probe.py && \
      grep -cE "^import |^from " scripts/pii_live_probe.py | awk '{ if ($1 < 12) exit 0; else { print "too many imports — likely external deps"; exit 1 } }'
    </automated>
  </verify>
  <done>
    scripts/pii_live_probe.py parses cleanly; --help shows --duration arg; PII_PATTERNS dict with all 5 categories present; query_range URL constructed; stdlib-only imports; matches smoke_otp.py shape (req-like helper, must-like asserter, sys.exit semantics); ready for Task 6 acceptance walkthrough invocation.
  </done>
</task>

<task type="auto">
  <name>Task 5: Documentation — ADR-0009 + sentry-ops.md + observability.md + deploy.md §11/§12 + TELEMETRY.md</name>
  <files>
    docs/DECISIONS/0009-observability-architecture.md,
    docs/RUNBOOKS/sentry-ops.md,
    docs/RUNBOOKS/observability.md,
    docs/RUNBOOKS/deploy.md,
    docs/TELEMETRY.md
  </files>
  <read_first>
    - docs/DECISIONS/0007-v1.0-release-contract.md (ADR shape reference — Дата / Статус / Контекст / Решение front-matter; numbered §-sections per sub-decision with bold **Принято:** lead-in; Канонические требования cross-link block; Russian-prose tone)
    - docs/RUNBOOKS/sops-edit.md (runbook shape — H1 title + Russian blurb + pinned-versions admonition + numbered H2 sections + bash code blocks with version-expectation comments + idempotency note)
    - docs/RUNBOOKS/deploy.md (existing structure; identify section 10 (last) so §11 + §12 append cleanly; provider-agnostic framing; > **Bold-label.** admonition pattern; § numbered H2 with H3 sub-steps)
    - .planning/phases/05-observability-backend/05-CONTEXT.md (D-XX rationale block — ADR sub-sections trace back to specific D-IDs)
    - .planning/phases/05-observability-backend/05-RESEARCH.md (Pitfalls P12-P21 → sentry-ops.md appendix; Q1-Q10 resolutions → ADR §1-§5)
    - .planning/phases/05-observability-backend/05-PATTERNS.md §docs/* sections (Adapt blocks for ADR + each runbook)
  </read_first>
  <action>
    Create five doc files (four NEW + one MODIFY) — Russian-language prose throughout to match existing docs style (sops-edit.md, deploy.md, ADR-0007 all Russian-mixed). All file sizes target close to artifact min_lines + headroom for clarity.

    **1. docs/DECISIONS/0009-observability-architecture.md (NEW ADR, ~180 lines):**
    Front-matter mirror ADR-0007 (lines 1-32 verbatim shape):
    - `# ADR-0009: Observability Architecture`
    - **Дата:** 2026-05-19 (or today's date at execution)
    - **Статус:** Accepted
    - **Контекст:** Phase 5 / OBS-01..08 (милестон v1.0 Production Readiness)
    - **Решение:** одностраничный roll-up пяти tightly-coupled sub-decisions.

    Sections (per RESEARCH executive summary, each with **Принято:** bold lead-in):
    - **§1 Sentry self-host topology** (D-01..D-05 + RESEARCH §1.1, §1.4) — separate VPS (isolation redline non-negotiable); pin getsentry/self-hosted@26.5.0; Caddy reverse-proxy in front of bundled nginx; sslip.io DNS; sentry+grafana on separate Caddy + ACME from app VPS.
    - **§2 slog choice + PII deny-list** (D-09..D-12 + D-13 OTP fix + D-14 CI grep audit) — `slog` stdlib over Zap (81 existing call sites); JSON-handler with email-hash + deny-list scrub; defense-in-depth at handler + call-site + CI; reference services/backend/pkg/observability/pii_deny_list.go as the single source of truth.
    - **§3 OTLP-to-Sentry tracing** (D-20..D-21 + RESEARCH §1.3) — OTLP/HTTP exporter to Sentry's `/api/<project_id>/integration/otlp/v1/traces`; X-Sentry-Auth header with sentry_key=<public_key>; SpanProcessor mirrors slog PII deny-list per D-21; OTel v1.32.0 pinned (all packages same minor version).
    - **§4 Telegram alerting** (D-24..D-26 + RESEARCH §1.5) — tiny Go alerter service on sentry VPS (50 LOC); token-bucket rate limit 25 msg/sec; Sentry generic webhook + Grafana contact point both go through it; PagerDuty/SMS rejected (cost + complexity for 2-dev team).
    - **§5 Single prod-backend project + per-service tag** (RESEARCH §1.9) — 1 Sentry project for all 9 services; discriminate via `setTag("service", svc)`; tag-based filtering preserves isolation without 8× dashboard surface; cross-service OTLP traces stay connected.
    - **§6 Log shipping via Grafana Alloy** (D-27/D-29 + RESEARCH §1.6 substitution) — Promtail EOL 2026-03-02; Alloy is documented successor; deployed via apt + systemd on prod VPS (not containerized; D-29 reasoning); UFW allowlist on Loki 3100 limits source to prod VPS only (D-28).
    - **§7 Tester-mode debug logging — backend seam only** (D-22/D-23 + RESEARCH §1.8 three-gate) — X-Debug-Session header + JWT is_tester claim + featureflag tester_debug_logs ALL required; pre-auth paths hard-skip; mobile-side Settings + RU consent banner DEFERRED TO PHASE 17.

    Closing block: **Канонические требования** with cross-links to REQUIREMENTS.md §OBS-01..08, ROADMAP.md §Phase 5, RESEARCH.md sections, CONTEXT.md D-01..D-32.

    **2. docs/RUNBOOKS/sentry-ops.md (NEW, ~250 lines):**
    Following sops-edit.md shape (Russian opening + pinned-versions admonition + numbered §):
    - §1 Environment — sentry-cli install (pinned 2.40.0 per RESEARCH §P18 — avoid JSON schema drift); SOPS_AGE_KEY_FILE setup; SENTRY_URL + SENTRY_AUTH_TOKEN env vars.
    - §2 Admin user creation — `docker exec sentry-self-hosted-web-1 sentry createuser --email <admin> --password <from SOPS> --superuser`; ADD `creates: /opt/sentry/.admin-created` Ansible guard per RESEARCH §P14.
    - §3 Project bootstrap — `sentry-cli projects create -o sentry prod-backend` loop for 4 projects; idempotent (HTTP 409 on re-create is fine).
    - §4 DSN extraction → SOPS — `sentry-cli projects list -o sentry --json | jq` to /tmp + manual paste into `sops edit .secrets/prod/sentry.yaml`.
    - §5 Telegram bot setup (USER ACTION 2 recap) — BotFather create @running-ecosystem-alerts-bot, /newbot prompts, capture token + chat_id; verify with `curl https://api.telegram.org/bot<TOKEN>/getMe`.
    - §6 Retention tuning knobs (RESEARCH §1.10) — SENTRY_EVENT_RETENTION_DAYS env; SENTRY_RETENTION_DAYS_TRANSACTIONS; ClickHouse partition_drop_after; Snuba kafka.message.max.bytes; alarm threshold at df 60% per RESEARCH §P13.
    - §7 Backup procedure — `docker exec sentry-postgres pg_dump`; ClickHouse `BACKUP DATABASE`; Kafka skipped (event volume tier; restore from Postgres + ClickHouse suffices); cron weekly.
    - §8 Pitfall reference appendix — quick lookup table mapping P12..P21 from RESEARCH.md to symptom + remediation.
    - §9 **Acceptance Walkthrough** — checklist USER ACTION 4 (Task 6) follows:
      1. `curl -fsSI https://sentry.<sentry-ip>.sslip.io/auth/login/` → HTTP 200
      2. Login as admin; confirm 4 projects visible
      3. Run `python3 scripts/smoke_metrics.py --load 60`
      4. Open Grafana → 4 dashboards (P99, error-rate, queue-depth, JWT-failures) → confirm panels populated within 5 min (screenshot each)
      5. `python3 scripts/pii_live_probe.py --duration 60` → exit 0, "0 PII matches"
      6. `python3 scripts/cardinality_probe.py` → exit 0
      7. `python3 scripts/smoke_sentry.py` → Sentry receives synthetic event within 30s
      8. Trigger P0 in staging-backend project → Telegram message arrives in @running-ecosystem-alerts chat (screenshot)
      9. Explicit user confirmation: "OBS-08 mobile UX + RU consent banner = Phase 17" (no work in Phase 5)
      10. Flip ROADMAP §Phase 5 checkbox [ ] → [x]
      11. Commit + write 05-06-SUMMARY.md

    **3. docs/RUNBOOKS/observability.md (NEW, ~180 lines):**
    Engineer-facing reference (mirror sops-edit.md shape):
    - §1 slog usage conventions — `slog.InfoContext(ctx, "event happened", "key", value)` preferred; never `fmt.Sprintf` PII into the message; reference D-10 + D-12.
    - §2 Metric naming rules — D-17 standard set; namespace (http_*, jwt_*, nats_*, db_*, external_api_*); bucket choices for histograms (P50/P95/P99 derivable); D-18 cardinality budget.
    - §3 PII deny-list reference — D-12 table verbatim; how to add new entries (PR to `pkg/observability/pii_deny_list.go` + update this doc).
    - §4 Cardinality budget — D-18 forbidden label list; allowed bucketed labels; `scripts/cardinality_probe.py` is the CI gate.
    - §5 How to add new metrics safely — checklist: namespace prefix, no user_id in labels, route label = templated path not raw URL (Pitfall #6), bucketed cohorts only.
    - §6 X-Debug-Session protocol description — RESEARCH §1.8 three-gate model: header + JWT claim + featureflag ALL required; pre-auth paths skip; mobile UX = Phase 17.
    - §7 Loki label hygiene — RESEARCH §P16 — labels for slow-changing dims only (env, cluster); request_id INSIDE log JSON body, NOT as Loki label; warning sign: loki_ingester_streams_created_total >10/sec.

    **4. docs/RUNBOOKS/deploy.md §11 + §12 (MODIFY — append two sections):**
    Read current deploy.md to identify last section number, then APPEND:
    - **§11 Sentry-stack deploy** — analog of existing §5 routine deploy, swap sport-stack→sentry-stack: `ansible-playbook -i inventory/sentry site.yml --tags sentry`; check `systemctl status sentry-stack.service` on sentry VPS; verify Caddy ACME with `curl -vI https://sentry.<sentry-ip>.sslip.io`. **§11.1 Alloy log shipper deploy** — `ansible-playbook -i inventory/prod site.yml --tags alloy`; verify `systemctl status alloy` on prod VPS; verify logs flowing via `curl 'https://grafana.<sentry-ip>.sslip.io/api/datasources/proxy/<id>/loki/api/v1/query?query={cluster=\"running-ecosystem\"}'`.
    - **§12 Sentry-stack rollback** — analog of existing §6 (sport-stack rollback): pin previous Sentry tag in roles/sentry-prep/defaults/main.yml; re-run `ansible-playbook -i inventory/sentry site.yml --tags sentry`; document `make sentry-rollback` if a corresponding Make target is desired (planner leaves Make target authoring to Plan 05-01/05-02 inheritors — this section only documents the manual ansible-playbook flow). DO NOT touch sport-stack rollback — that stayed in §6.

    **5. docs/TELEMETRY.md (NEW skeleton, ≤80 lines):**
    Minimal opt-in telemetry event allowlist skeleton (Phase 17 consumes):
    - H1 `# Mobile Telemetry — Opt-in Event Allowlist (Phase 17 consumes)`
    - Opening paragraph (Russian): "Phase 5 ships the backend Sentry seam; mobile-side `apps/mobile-rn/src/observability/sentry.ts` PII-strip middleware + Settings consent UI = Phase 17 (CRASH-01..06). This file is the contract Phase 17 implements."
    - Section "## Allowlist Table" — placeholder Markdown table with columns `event_name | description | PII risk | consent required`. Empty rows with TODO comments.
    - Section "## Reference" — point to D-12 PII deny-list (single source of truth — mobile reuses same list); RESEARCH §1.8 X-Debug-Session protocol; Phase 17 plan-bundle TBD.
    - NO content beyond skeleton — Phase 17 fills it.
  </action>
  <verify>
    <automated>
      test -f docs/DECISIONS/0009-observability-architecture.md && \
      test -f docs/RUNBOOKS/sentry-ops.md && \
      test -f docs/RUNBOOKS/observability.md && \
      test -f docs/TELEMETRY.md && \
      grep -q "ADR-0009" docs/DECISIONS/0009-observability-architecture.md && \
      grep -q "Acceptance Walkthrough" docs/RUNBOOKS/sentry-ops.md && \
      grep -q "PII deny-list" docs/RUNBOOKS/observability.md && \
      grep -q "Opt-in" docs/TELEMETRY.md && \
      grep -E "^## (§|\\\\\\$|11|Sentry-stack)" docs/RUNBOOKS/deploy.md | grep -q "11" && \
      [ $(wc -l < docs/DECISIONS/0009-observability-architecture.md) -ge 80 ] && \
      [ $(wc -l < docs/RUNBOOKS/sentry-ops.md) -ge 120 ] && \
      [ $(wc -l < docs/RUNBOOKS/observability.md) -ge 100 ] && \
      [ $(wc -l < docs/TELEMETRY.md) -ge 40 ] && \
      [ $(wc -l < docs/TELEMETRY.md) -le 80 ]
    </automated>
  </verify>
  <done>
    All five doc files present at the expected line counts; ADR-0009 has §1-§7 sub-sections + Канонические требования cross-link block; sentry-ops.md has §Acceptance Walkthrough checklist that Task 6 references; observability.md covers D-12 + D-17 + D-18 + RESEARCH §1.8; deploy.md gains §11 + §12; TELEMETRY.md is a skeleton ≤80 lines pointing at D-12 + Phase 17.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: USER ACTION 4 — Final acceptance walkthrough + ROADMAP closeout</name>
  <files>
    .planning/ROADMAP.md,
    .planning/phases/05-observability-backend/05-06-SUMMARY.md
  </files>
  <read_first>
    - docs/RUNBOOKS/sentry-ops.md §Acceptance Walkthrough (the 11-step checklist authored by Task 5; this task EXECUTES that checklist with the user)
    - .planning/phases/05-observability-backend/05-VALIDATION.md (per-requirement verification map — OBS-06 runtime row + OBS-08 row + Alerts row + Logs row all flip to ✅ here)
    - .planning/ROADMAP.md §Phase 5 (locate the [ ] checkbox to flip to [x])
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY.md structure for closeout artifact)
    - .planning/phases/05-observability-backend/05-04-SUMMARY.md + 05-05-SUMMARY.md (sibling closeouts; mirror tone + section headers for 05-06-SUMMARY.md)
  </read_first>
  <what-built>
    By this point (post-Task 5), all six Phase 5 plans have shipped artifacts:
    - 05-01: Sentry VPS provisioned + sentry-prep Ansible role + DNS + Caddy ACME
    - 05-02: Sentry self-hosted installed + 4 projects + DSNs in SOPS + Telegram alerter
    - 05-03: slog JSON handler + PII deny-list + OTP fix + CI grep audit
    - 05-04: Prom client_golang /metrics + 4 Grafana dashboards + cardinality probe
    - 05-05: OTLP traces + sentry-go SDK + span PII scrub
    - 05-06 (this plan, Tasks 1-5): DebugSessionMiddleware + Alloy log shipper + pii_live_probe + docs/ADR
    Everything is now wired; this checkpoint is the end-to-end runtime acceptance.
  </what-built>
  <how-to-verify>
    Execute the 11-step **Acceptance Walkthrough** checklist from `docs/RUNBOOKS/sentry-ops.md §9` verbatim. Each step's pass/fail is recorded in the 05-06-SUMMARY.md evidence block.

    1. **Sentry UI reachable** — `curl -fsSI https://sentry.<sentry-ip>.sslip.io/auth/login/` returns HTTP 200; cert issuer = Let's Encrypt or ZeroSSL (RESEARCH §P19 fallback); open browser and confirm Sentry login page renders.
    2. **Admin login + 4 projects visible** — Login as admin (password from `sops -d .secrets/prod/sentry.yaml | grep SENTRY_ADMIN_PASSWORD`); Project sidebar shows `prod-backend`, `staging-backend`, `prod-mobile`, `staging-mobile`.
    3. **Generate synthetic load** — `python3 scripts/smoke_metrics.py --load 60` from a dev workstation with BASE_URL pointing at prod app VPS; expect 60-sec sustained traffic across the 9 services.
    4. **4 Grafana panels populated** — Open `https://grafana.<sentry-ip>.sslip.io`, navigate to the 4 dashboards (P99 latency, error rate, NATS queue depth, JWT validation failures); within 5 min of load start, EACH panel shows non-empty data. SCREENSHOT each panel and save to `.planning/phases/05-observability-backend/evidence/grafana-{p99,errors,queue,jwt}.png`.
    5. **PII live probe green** — `python3 scripts/pii_live_probe.py --duration 60` exits 0 with stdout "0 PII matches in N log lines"; copy exact stdout into SUMMARY evidence.
    6. **Cardinality probe green** — `python3 scripts/cardinality_probe.py` exits 0 with stdout "PASS: 9 services scraped, 0 forbidden labels, max <N> series on <metric>"; copy exact stdout.
    7. **Sentry envelope smoke green** — `python3 scripts/smoke_sentry.py` exits 0; Sentry UI shows the synthetic event within 30s tagged `service=test` + `env=staging`.
    8. **Telegram alert E2E** — In Sentry UI, manually create a P0 alert rule on `staging-backend` triggered by `event.type:error message:"test alert"`; send a POST to the staging-backend DSN envelope endpoint with a test error; within 60s Telegram chat @running-ecosystem-alerts receives the formatted alert (via alerter service from Plan 05-02). SCREENSHOT the chat message → save to `.planning/phases/05-observability-backend/evidence/telegram-p0.png`.
    9. **OBS-08 deferral confirmation** — User explicitly confirms in the walkthrough: "RU consent banner UX + mobile Settings toggle UI = Phase 17 territory; Phase 5 ships only the backend seam (DebugSessionMiddleware + tester_debug_logs featureflag + JWT is_tester claim coupling)." Record this confirmation verbatim in 05-06-SUMMARY.md.
    10. **ROADMAP checkbox flip** — Edit `.planning/ROADMAP.md`; locate `### Phase 5:` section; flip the closing `[ ]` checkbox → `[x]`. Stage the file.
    11. **Write 05-06-SUMMARY.md** — Following `$HOME/.claude/get-shit-done/templates/summary.md` structure: outcome (what shipped), artifacts (8 must_have artifacts with paths + LOC), evidence block (steps 1-8 stdout/screenshots), pinned deps (Alloy 1.5.0; sentry-cli 2.40.0; sentry-go 0.46.2; OTel 1.32.0; client_golang 1.20.5; getsentry/self-hosted 26.5.0), follow-ups (Phase 17 inherits: mobile SDK install, Settings toggle, RU consent banner; v1.1 inherits: PagerDuty if team grows, real <brand>.com domain, secret rotation per CONCERNS HIGH).

    **Failure handling:** If any step 1-8 fails, capture the failure mode in SUMMARY.md "Issues encountered" section but DO NOT proceed to step 10 (ROADMAP flip). Spawn a `/gsd-plan-phase 5 --gaps` follow-up plan to close the gap.
  </how-to-verify>
  <resume-signal>
    Type `approved` after all 11 steps pass and ROADMAP §Phase 5 checkbox is flipped to [x]. Reply with `blocked: <step-N>` + brief description if any step fails — the orchestrator will route to `--gaps` planner for closure.
  </resume-signal>
  <verify>
    <automated>
      grep -E "^- \[x\].*Phase 5" .planning/ROADMAP.md || grep -E "Phase 5.*\[x\]" .planning/ROADMAP.md && \
      test -f .planning/phases/05-observability-backend/05-06-SUMMARY.md && \
      grep -q "OBS-08" .planning/phases/05-observability-backend/05-06-SUMMARY.md && \
      grep -q "Phase 17" .planning/phases/05-observability-backend/05-06-SUMMARY.md && \
      grep -qE "Alloy.*1\.5\.0|sentry-cli.*2\.40\.0" .planning/phases/05-observability-backend/05-06-SUMMARY.md
    </automated>
  </verify>
  <done>
    All 11 walkthrough steps signed off in 05-06-SUMMARY.md evidence block; ROADMAP §Phase 5 checkbox = [x]; SUMMARY.md mentions OBS-08 mobile deferral to Phase 17 + pinned dep versions; 4 Grafana screenshots + 1 Telegram screenshot saved to evidence/; Phase 5 is CLOSED.
  </done>
</task>

</tasks>


<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request edge → backend services | Any client can send arbitrary headers, including `X-Debug-Session: 1`. Without JWT+featureflag coupling, this would be a debug-DoS amplifier (RESEARCH §1.8). |
| Prod VPS Docker socket → Alloy process | Alloy reads /var/run/docker.sock to discover containers; UID/GID alignment via docker-group membership (RESEARCH §P17) bounds blast radius to "what Docker exposes" — Alloy is not root but holds docker-group privilege. |
| Prod VPS Alloy → Sentry VPS Loki :3100 | Outbound HTTPS POST to Loki on sentry VPS; auth is by source-IP allowlist on sentry VPS UFW (D-28) — no application-level auth on Loki itself (single-tenant). |
| Grafana Loki proxy datasource → pii_live_probe.py | Probe reads logs via Grafana API; basic auth or API-key gated; probe has read-only access; mis-set credentials = probe fails closed (exit 3), not silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01-T | Tampering | `X-Debug-Session: 1` header spoofed by attacker on a pre-auth endpoint (`/auth/request-code`, `/healthz`, `/metrics`) | mitigate | DebugSessionMiddleware requires JWT claim `is_tester=true` via `auth.ClaimsFromContext` — pre-auth paths have no claims in ctx → gate silently denies; no log emission at gate failure (RESEARCH §1.8 — would otherwise be log-volume amplifier); Task 1 Test 3 (TestDebugSessionMiddleware_PreAuthPath) enforces this regression-resistant. |
| T-05-06-02-E | Elevation of Privilege | Anonymous attacker elevates request to debug-log level via header-only path | mitigate | Three-gate coupling: header + JWT is_tester=true + featureflag tester_debug_logs ON; attacker must compromise a tester account (rotation-bounded blast radius) AND admin must have rolled out the featureflag for that user; defense-in-depth means flipping featureflag OFF revokes all debug elevation instantly via Phase 1 admin UI. |
| T-05-06-03-I | Information Disclosure | PII slips into production logs despite Plan 05-03 handler scrub (e.g., engineer string-interpolates phone into a message body, bypassing attribute-key deny-list) | mitigate | scripts/pii_live_probe.py runs over 60-sec Loki window in CI + at acceptance walkthrough; regex-matches against GPS/phone/displayName/external_uuid/OTP patterns in LINE CONTENT (not just attribute keys); first match = exit 1; complements D-14 CI grep audit (attr-key level) with runtime check (content level). |
| T-05-06-04-I | Information Disclosure | Loki ingests PII as a label (e.g., engineer adds `request_id` as Loki label causing stream-cardinality explosion AND attaches PII into label value) | mitigate | RESEARCH §P16 — Alloy config templates `labels = { env, cluster }` only; documented in observability.md §7; loki_ingester_streams_created_total >10/sec is the early warning sign documented in P16. |
| T-05-06-05-D | DoS | Attacker spams X-Debug-Session: 1 on a public endpoint to inflate Loki storage costs | mitigate | Three-gate coupling silently denies anonymous + non-tester requests; even if attacker compromises a tester account, featureflag rollout (admin-controlled) can be turned off instantly; Loki retention budget per RESEARCH §1.10 has 3-orders-of-magnitude headroom for current scale; debug elevation does NOT increase Loki label cardinality (still env+cluster only — only log line VOLUME would grow, and Loki rejects burst above ingester rate per P16). |
| T-05-06-06-D | DoS | Alloy fails silently (docker socket permission denied per P17) → all log shipping stops without alarm | mitigate | Task 3 step 4 adds alloy user to docker group; Task 3 step 9 smoke probe asserts `journalctl -u alloy` shows "started reader" lines per RESEARCH §P21; acceptance walkthrough Task 6 step 8 verifies log flow end-to-end via Loki query (any silent failure = pii_live_probe runs over zero data = sus output that doesn't match expected line count). |
| T-05-06-07-S | Spoofing | Forged log lines pushed to Loki :3100 from non-prod-VPS source | accept (with UFW) | UFW allowlist on sentry VPS restricts source IP to prod-vps-ip/32 only per D-28; bypass would require either (a) IP spoofing on the carrier network (impractical for TCP), or (b) compromising the prod VPS itself (in which case attacker already has all the logs). Application-level Loki auth deferred — UFW is sufficient for v1.0 closed beta scale. |
| T-05-06-08-R | Repudiation | Operator silently disables tester_debug_logs featureflag mid-incident, claims "we couldn't see the bug" | accept | Featureflag store has Postgres audit trail via Phase 1 REL-03 (default; Plan 01-XX SUMMARY); admin actions are timestamped + user-attributed; v1.1 may add signed audit log if 2-dev team grows. |

</threat_model>

<verification>
1. `cd services/backend && go test -race ./pkg/observability/... ./pkg/featureflags/...` exits 0 — DebugSessionMiddleware tests + featureflag registration green (Plan 05-03 + 05-04 + 05-05 + 05-06 combined).
2. `cd services/backend && go build ./...` exits 0 — all 9 services compile with DebugSessionMiddleware mounted in chain.
3. `cd infra/ansible && ansible-playbook --syntax-check site.yml` exits 0 — site.yml includes alloy play; alloy-shipper role syntactically valid.
4. `ansible-lint roles/alloy-shipper/` emits no errors.
5. `python3 -c "import ast; ast.parse(open('scripts/pii_live_probe.py').read())"` succeeds; `python3 scripts/pii_live_probe.py --help` lists `--duration`.
6. All five doc files exist with min_lines satisfied; ADR-0009 has 7 sub-sections; sentry-ops.md has §Acceptance Walkthrough; observability.md covers PII deny-list + cardinality budget + X-Debug-Session protocol; deploy.md has §11 + §12; TELEMETRY.md is a ≤80-line skeleton.
7. **Manual (Task 6 walkthrough):** All 11 acceptance steps pass; 4 Grafana panels populated within 5 min of synthetic load; pii_live_probe + cardinality_probe + smoke_sentry all green; Telegram P0 alert received; OBS-08 mobile deferral explicitly confirmed; ROADMAP §Phase 5 checkbox = [x]; 05-06-SUMMARY.md committed with evidence block.
8. After Plan 05-06: Phase 5 is CLOSED. Phase 17 inherits the OBS-08 mobile-UX dependency; Phase 6 (EDGE) inherits the sentry-VPS Caddy + UFW patterns.
</verification>

<success_criteria>
Plan 05-06 is complete when:
- [ ] DebugSessionMiddleware + 3-gate enforcement tests GREEN (Task 1)
- [ ] tester_debug_logs featureflag registered default=false in pkg/featureflags/flags.go
- [ ] All 9 services' main.go mount DebugSessionMiddleware as outermost observability layer (Task 2)
- [ ] alloy-shipper Ansible role + site.yml play merged; ansible --syntax-check + ansible-lint clean (Task 3)
- [ ] scripts/pii_live_probe.py parses + --help exposes --duration; PII_PATTERNS table complete (Task 4)
- [ ] 5 doc files shipped: ADR-0009, sentry-ops.md, observability.md, deploy.md §11+§12, TELEMETRY.md skeleton (Task 5)
- [ ] Acceptance walkthrough (Task 6) — all 11 steps pass; user signs off; ROADMAP §Phase 5 checkbox = [x]
- [ ] 05-06-SUMMARY.md written with: artifact LOC table, evidence block (step stdouts + screenshot refs), pinned dep versions (Alloy 1.5.0 + sentry-cli 2.40.0 + sentry-go 0.46.2 + OTel 1.32.0 + client_golang 1.20.5 + getsentry/self-hosted 26.5.0), Phase 17 follow-up list (mobile SDK install + Settings toggle + RU consent banner)
- [ ] OBS-06 + OBS-08 rows in VALIDATION.md flip to ✅
</success_criteria>

<output>
After completion, create `.planning/phases/05-observability-backend/05-06-SUMMARY.md` documenting Phase 5 closeout. Required sections:

1. **Outcome** — Phase 5 closed; 8 ROADMAP success criteria achieved; OBS-01..08 status per VALIDATION.md final state.
2. **Artifacts** — must_have artifact paths + final LOC per file + test count for the Go files.
3. **Evidence block** — verbatim stdout from steps 5-7 (pii_live_probe, cardinality_probe, smoke_sentry); screenshot refs for steps 4 + 8 (Grafana + Telegram).
4. **Pinned dep versions** — Alloy 1.5.0, sentry-cli 2.40.0, sentry-go 0.46.2, OTel 1.32.0 (all packages same minor), client_golang 1.20.5, getsentry/self-hosted 26.5.0.
5. **Phase 17 inheritance** — explicit list: (a) mobile Sentry SDK install + DSN consumption (DSNs already in SOPS), (b) Settings toggle UI for X-Debug-Session, (c) RU consent banner UX, (d) PII-strip middleware in apps/mobile-rn/src/observability/sentry.ts mirroring backend D-12 deny-list.
6. **Deferred to v1.1** — PagerDuty integration (D-24 escape hatch), real `<brand>.com` domain replacing sslip.io (D-04 + D-05), secret rotation per CONCERNS HIGH (POSTGRES_PASSWORD, JWT_SECRET, MINIO_ROOT_* — Plan 05-06 introduces NEW Sentry/Telegram secrets which are NOT in the rotation backlog because they were generated offline + sops --set directly per CONCERNS discipline).
7. **Issues encountered** (if any) — failure modes from walkthrough; gap closure plan reference if `--gaps` had to spin up.

Phase 5 is then closed. The next phase (Phase 6 — EDGE / DDoS / WAF — per ROADMAP) inherits the Caddy + UFW patterns from sentry VPS bootstrap (Plan 05-01); Phase 17 inherits the OBS-08 mobile-UX dependency.
</output>
