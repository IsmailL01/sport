---
plan_id: 03-03
phase: 3
phase_slug: infrastructure-as-code
wave: 3
depends_on: [03-02]
files_modified:
  - docs/RUNBOOKS/deploy.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
requirements: [INFRA-01, INFRA-07]
autonomous: false
estimated_duration: "60 min"

must_haves:
  truths:
    - "RUNBOOK provider-agnostic per D-25 NEW — no `hetzner`/`digitalocean`/`aws`/`gcp` literals (verified by negative grep in Task 1)"
    - "Prod cutover MUST teardown existing manual `/opt/sport/deploy.sh`-launched compose stack via explicit `docker compose down --remove-orphans` SSH step BEFORE Ansible play binds port 443 (B4 mitigation — preserves zero-downtime invariant)"
    - "Cutover Task 2 step 3.5 acceptance: `ssh deploy@<prod-ip> 'docker ps --filter name=caddy --format \"{{.Names}}\"'` returns empty post-teardown — verifies manual stack stopped"
    - "ROADMAP §Phase 3 + REQUIREMENTS.md INFRA-* reflect pivot reality: INFRA-02 + INFRA-04 deferred v1.1; INFRA-06 moved to Phase 5; criterion `6 Go service systemd units` → `8 Go service containers under sport-stack.service umbrella`"
    - "deploy.md §9 measured timing table populated programmatically via sed substitution from 03-02-timing.log + 03-03-timing.log (W5 fix preserved — no executor-typed placeholders)"
    - "[informational] D-22 (drop Terraform) reflected in RUNBOOK §3 (\"Spin up a Linux VPS at your provider of choice\" — not Terraform-driven)"
    - "[informational] D-23 (single-VPS topology = existing prod) reflected in RUNBOOK §3 (no staging spinup; staging deferred v1.1)"
    - "[informational] /opt/sport/deploy.sh stays as documented fallback in RUNBOOK §6 Rollback ONLY; §5 Routine deploy is Ansible-only"
  behaviors:
    - "After cutover (Task 2): `ssh deploy@<prod-ip> 'systemctl is-active sport-stack.service'` returns `active` AND `BASE_URL=https://<prod-ip>.sslip.io python services/backend/scripts/smoke_otp.py` returns exit 0 AND `docker ps --filter name=caddy` shows ONLY `caddy:2.8-alpine` under sport-stack umbrella (no stale standalone container outside)"
    - "After Task 2 step 7: `! grep -q '<fill' docs/RUNBOOKS/deploy.md` exit 0 (all `<fill ...>` placeholders replaced via programmatic sed — W5 fix preserved)"
    - "After Task 3: `grep -q '8 Go service containers' .planning/ROADMAP.md && ! grep -q '6 Go service systemd units' .planning/ROADMAP.md` exit 0 (criterion 6→8 fix + old wording purged)"
    - "After Task 4: `grep -c '\\*\\*DEFERRED to v1.1\\*\\*' .planning/REQUIREMENTS.md` ≥ 2 (INFRA-02 + INFRA-04); `grep -c '\\*\\*MOVED to Phase 5\\*\\*'` ≥ 1 (INFRA-06)"
  forbidden:
    - "No provider-specific names в RUNBOOK (verified by Task 1 negative grep: `! grep -qi 'hetzner\\|digitalocean\\|aws\\|gcp' docs/RUNBOOKS/deploy.md`)"
    - "No `--amend` on cutover commits — every cutover step gets its own commit для audit trail"
    - "No skipping the explicit teardown step (Task 2 step 3.5) — port 443 double-bind kills cutover deterministically"
    - "No hand-typed wall-clock numbers in deploy.md §9 — values MUST come from sed substitution against `/usr/bin/time -p` real-line (W5 fix)"
    - "No new `<fill ...>` placeholders left in deploy.md after Task 2 step 7 (`! grep -q '<fill'` gate)"
---

<objective>
## Цель плана 03-03 (post-pivot — Wave 3, was 03-04)

Закрыть Phase 3 тремя действиями: (a) написать provider-agnostic `docs/RUNBOOKS/deploy.md` (9 sections per D-25 NEW), (b) выполнить production cutover с explicit `docker compose down` существующего manual stack BEFORE Ansible UP (B4 mitigation), (c) обновить `.planning/ROADMAP.md` §Phase 3 и `.planning/REQUIREMENTS.md` §INFRA-01..07 чтобы отразить pivot reality (INFRA-02/04 deferred v1.1, INFRA-06 moved to Phase 5, criterion `6 Go service systemd units` → `8 Go service containers`).

**Purpose:** Wave 3 закрывает Phase 3 целиком. После Task 2 production традуется с manual `/opt/sport/deploy.sh` → Ansible-managed `sport-stack.service` zero-downtime (managed via explicit teardown + reup). RUNBOOK становится canonical onboarding doc для будущих devs / другого VPS provider. ROADMAP + REQUIREMENTS отражают что закрыто vs deferred — следующая фаза (Phase 4 — CI/CD) видит чистую картину.

**Pivot context:** Этот план был ранее 03-04 (cutover). После 2026-05-17 pivot renumbered to 03-03 и расширен: RUNBOOK становится provider-agnostic (D-25 NEW), теряет Terraform sections (D-22), теряет staging/sentry mentions (D-23), gains explicit teardown step (B4 mitigation surfaced as Task 2 step 3.5).

**Output:**
- `docs/RUNBOOKS/deploy.md` (NEW — 9 sections, provider-agnostic, measured timing populated programmatically)
- Live cutover на prod completed: `sport-stack.service` active, smoke 200, no stale standalone Caddy container
- `.planning/ROADMAP.md` §Phase 3 updated (criteria edited per pivot)
- `.planning/REQUIREMENTS.md` §INFRA-01..07 updated (deferral markers added)
- `.planning/phases/03-infrastructure-as-code/03-03-timing.log` (cutover timing)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-infrastructure-as-code/03-CONTEXT.md
@.planning/phases/03-infrastructure-as-code/03-RESEARCH.md
@.planning/phases/03-infrastructure-as-code/03-PATTERNS.md
@.planning/phases/03-infrastructure-as-code/03-01-PLAN.md
@.planning/phases/03-infrastructure-as-code/03-02-PLAN.md
@.planning/phases/03-infrastructure-as-code/03-VALIDATION.md
@docs/RUNBOOKS/sops-edit.md
@services/backend/docker-compose.prod.yml
@services/backend/scripts/smoke_otp.py

<interfaces>
<!-- Outputs from Plan 03-02 (Wave 2) that this plan consumes: -->

From `infra/ansible/roles/sport-stack/` (Plan 03-02 Task 1 output):
- Full role tree (7 files) deployed and dry-run-verified on prod
- `sport-stack.service.j2` template ready to land at `/etc/systemd/system/sport-stack.service` on cutover

From `.planning/phases/03-infrastructure-as-code/03-02-timing.log` (Plan 03-02 Task 2 output):
- `/usr/bin/time -p` output with `real <seconds>` line
- `INFRA-07 VERDICT:` line derived programmatically via awk

From `infra/ansible/group_vars/prod.yml` + `infra/ansible/inventory/prod/hosts.yml`:
- `caddy_host: "148-253-214-156.sslip.io"` (либо updated)
- `prod-app-01` host with `ansible_host` IP

From existing `services/backend/docker-compose.prod.yml`:
- `gateway` service uses `caddy:2.8-alpine` and binds host ports 80:80, 443:443 (L280-282)
- Same compose file is mounted at `/opt/sport/services/backend/docker-compose.prod.yml` on prod by both manual deploy.sh AND Ansible — same source of truth, no divergence

From existing `docs/RUNBOOKS/sops-edit.md`:
- 9-section structure used as template for deploy.md (same conventions: numbered sections, Russian narrative + English commands)
- §9 Deploy script lives there pre-Phase-3 — Phase 3 deploy.md references it as legacy fallback only

From existing `/opt/sport/deploy.sh` on prod VPS:
- Wraps SOPS decrypt → docker compose up
- Stays in place (NOT deleted) — pure fallback after Plan 03-03 cutover; deprecation note in deploy.md SUMMARY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/RUNBOOKS/deploy.md — 9 sections, provider-agnostic (D-25)</name>
  <files>
    docs/RUNBOOKS/deploy.md
  </files>
  <read_first>
    .planning/phases/03-infrastructure-as-code/03-CONTEXT.md (D-22 + D-23 + D-24 + D-25 + D-26 NEW; KEPT D-04 + D-13 + D-16 + D-18 + D-21),
    docs/RUNBOOKS/sops-edit.md (structural template — 9 numbered sections, mixed RU narrative + EN commands; §1 Environment Setup pattern для dev workstation onboarding),
    .planning/phases/03-infrastructure-as-code/03-01-PLAN.md (Task 0 user-action — VPS provisioning contract; Task 2 — DEV_B keys delivery flow),
    .planning/phases/03-infrastructure-as-code/03-02-PLAN.md (sport-stack role contract; timing log format),
    services/backend/scripts/smoke_otp.py (BASE_URL pattern для §6 smoke verification)
  </read_first>
  <action>
    Create `docs/RUNBOOKS/deploy.md` с 9 numbered sections. Structure mirrors `docs/RUNBOOKS/sops-edit.md` (RU narrative for body text, EN identifiers/commands/code). Provider-agnostic throughout per D-25 NEW.

    **Section structure (each section is `## <N>. <Title>`):**

    **§1 — Overview**
    - One-paragraph framing: Phase 3 deploy seam = Ansible-driven prod deploy от any SSH-accessible Linux VPS. Manual `/opt/sport/deploy.sh` остаётся как documented emergency fallback (§6 Rollback). Provider choice не влияет (D-22 — Terraform deferred to v1.1 if migrating to cloud-API provider; D-25 — RUNBOOK provider-agnostic).
    - List of artifacts deploy'ит: 8 Go services + Postgres+TimescaleDB + Redis + NATS + MinIO + containerized Caddy:2.8-alpine = 13 containers under one `sport-stack.service` systemd umbrella (D-04 KEPT).
    - Cross-ref to `docs/RUNBOOKS/sops-edit.md` для SOPS edit flow.

    **§2 — Dev workstation setup**
    - Install commands: `brew install ansible age sops python3` (macOS) или `apt install ansible age sops python3` (Linux dev).
    - Ansible Galaxy collections: `ansible-galaxy collection install community.sops community.docker community.general ansible.posix`.
    - Age key location: `~/.config/sops/age/keys.txt` (XDG path per D-13 KEPT). Document `SOPS_AGE_KEY_FILE` env var export + direnv hint (`~/.envrc` containing `export SOPS_AGE_KEY_FILE=$HOME/.config/sops/age/keys.txt`) — closes Phase 2 follow-up #3.
    - Smoke-test decrypt: `sops -d --extract '["JWT_SECRET"]' .secrets/prod/shared.yaml >/dev/null && echo OK`.

    **§3 — VPS provisioning (provider-agnostic per D-25)**
    - **Note explicitly:** "Spin up a Linux VPS at your provider of choice. Ubuntu 22.04 LTS or 24.04 LTS recommended. Minimum 4 GB RAM, 2 vCPU, 40 GB SSD. Get public IP + SSH-user credentials with sudo access."
    - No mention of Hetzner, DigitalOcean, AWS, GCP, Linode — provider-agnostic per D-25 NEW.
    - Pre-flight checklist: VPS reachable via SSH from dev workstation IP; sudo works without password ИЛИ password-prompted (Ansible bootstrap pass handles either via `--ask-become-pass` if needed).
    - DNS: sslip.io for v1.0 closed beta (D-18 KEPT) — works on any public IP, no DNS provider needed for closed beta.
    - **NO Terraform.** v1.0 has no cloud-API automation; if migrating to cloud-API provider in v1.1, reintroduce Terraform с appropriate provider — write ADR explaining motivation.

    **§4 — First-time bootstrap (Plan 03-01 + Plan 03-02 walkthrough)**
    - Step 1: `git clone <repo>` on dev workstation.
    - Step 2: Run Plan 03-01 user-action checkpoint — confirm VPS IP + SSH user + sudo access (update `infra/ansible/inventory/prod/hosts.yml` `ansible_host`).
    - Step 3: First bootstrap pass with existing admin SSH user (likely `root` if cloud-init Linux): `cd infra/ansible && ansible-playbook -i inventory/prod --user root --extra-vars ansible_user=root site.yml`.
    - Step 4: After bootstrap creates `deploy` user, switch to deploy: `ansible-playbook -i inventory/prod site.yml`.
    - Step 5: Verify: `ssh deploy@<vps-ip> 'systemctl is-active sport-stack.service'` returns `active`.
    - Step 6: Smoke: `BASE_URL=https://<vps-ip-dashes>.sslip.io python services/backend/scripts/smoke_otp.py` returns exit 0.

    **§5 — Routine deploy (after code change)**
    - Single command: `cd infra/ansible && ansible-playbook -i inventory/prod site.yml --tags sport-stack`
    - This is the canonical deploy command — NOT manual `/opt/sport/deploy.sh`.
    - Idempotency: 2nd-run shows `changed=0` если код не менялся; если менялся — `changed=N` для соответствующих tasks.

    **§6 — Rollback (manual fallback ONLY)**
    - This section documents the legacy `/opt/sport/deploy.sh` shape as emergency fallback.
    - Procedure: `git checkout <prev-tag>` локально → `ansible-playbook ... --tags sport-stack` → если migration drift, `docker compose run --rm migrations -- down 1` (golang-migrate one-step rollback).
    - **Note:** Phase 4 (CICD-04) wires CI-driven rollback drill — until then, manual fallback documented here is the only path.
    - **Deprecation note для /opt/sport/deploy.sh:** stays usable as emergency fallback после Plan 03-03 cutover; full removal scheduled for Phase 4 (CI/CD).

    **§7 — Failure modes**
    | Symptom | Likely cause | Resolution |
    |---------|--------------|------------|
    | `community.sops.load_vars` exit 1 | `SOPS_AGE_KEY_FILE` not set OR age key missing | Re-export env var; recover key per `sops-edit.md §7` |
    | `SSH: connection refused` | VPS unreachable / dev IP not in UFW allow-list | Verify VPS public IP; verify `dev_admin_ips` в group_vars/all.yml contains current dev workstation IP |
    | `ACME issue failed` в Caddy logs | Let's Encrypt rate limit / DNS A record missing | Wait 1h for rate-limit reset; verify sslip.io subdomain resolves correctly via `dig <vps-ip-dashes>.sslip.io` |
    | `migrations` container exit 1 | Schema drift / SQL syntax error в new migration | `ssh deploy@<vps> 'docker compose logs migrations'`; resolve manually then re-run Ansible |
    | `port 443 already in use` during deploy | Existing manual `/opt/sport/deploy.sh` stack still running (cutover blocker) | Run explicit teardown: `ssh deploy@<vps> 'cd /opt/sport/services/backend && docker compose -f docker-compose.prod.yml down --remove-orphans'`. Then retry Ansible. |

    **§8 — Provider-specific notes (placeholder for v1.1)**
    - Section header + one paragraph: "RUNBOOK currently provider-agnostic per D-25. If specific provider-side automation needed (e.g. Hetzner Cloud Firewall API, AWS Security Groups, DigitalOcean Spaces для backup), add subsection here. v1.0 keeps generic Linux VPS posture."
    - Empty subsection placeholders: `### 8.1 If using Hetzner Cloud (placeholder)`, `### 8.2 If using DigitalOcean (placeholder)`, `### 8.3 If using AWS EC2 (placeholder)` — each содержит `_(not yet documented — provider-agnostic v1.0 per D-25)_`.

    **§9 — Measured timing log**
    - Table with `<fill>` placeholders that Task 2 fills programmatically via sed:
    ```markdown
    | Date | Env | Deploy type | Wall-clock real (sec) | Verdict | Notes |
    |------|-----|-------------|----------------------|---------|-------|
    | 2026-05-17 | prod | First clean Ansible-deploy (Plan 03-02) | <fill 03-02 deploy time> | <fill 03-02 verdict> | sport-stack role landed; existing manual stack still running in parallel during Plan 03-02 |
    | 2026-05-17 | prod | Cutover (Plan 03-03 — manual stack down + Ansible UP) | <fill prod cutover time> | <fill cutover verdict> | Zero-downtime cutover; manual /opt/sport/deploy.sh deprecated |
    ```

    **§10 — Cross-references**
    - `docs/RUNBOOKS/sops-edit.md` — SOPS edit/decrypt/rotate workflow
    - `docs/SECRETS.md` — token inventory + rotation playbooks
    - `.planning/phases/02-secrets-and-config-hardening/02-04-SUMMARY.md` — Phase 2 follow-ups (DEV_B keys, direnv)
    - `.planning/phases/03-infrastructure-as-code/03-01-PLAN.md`, `03-02-PLAN.md`, `03-03-PLAN.md`, `03-CONTEXT.md` — Phase 3 source of truth
    - `services/backend/docker-compose.prod.yml` — production stack file (canonical)
    - `infra/ansible/site.yml` — Ansible entry playbook

    **Required negative-presence (verified by Task acceptance):**
    - `! grep -qi 'hetzner\|digitalocean\|aws\|gcp' docs/RUNBOOKS/deploy.md` — exit 0 (no provider literals in main body; the §8 placeholder subsections contain provider names inside `### 8.x If using <provider>` header but these are explicitly marked as v1.1 placeholders).

    Wait — the `<provider>` names appearing в §8 subsection headers WILL hit the negative grep. Resolution: use case-insensitive grep with explicit allow-list for §8 headers OR rephrase §8 subsections to use neutral wording like `### 8.1 Provider A (placeholder)`, `### 8.2 Provider B (placeholder)`. Use the neutral form to satisfy the negative grep cleanly.

    **Revised §8 subsections:**
    ```
    ### 8.1 Provider A (placeholder)
    _(not yet documented — provider-agnostic v1.0 per D-25)_

    ### 8.2 Provider B (placeholder)
    _(not yet documented — provider-agnostic v1.0 per D-25)_

    ### 8.3 Provider C (placeholder)
    _(not yet documented — provider-agnostic v1.0 per D-25)_
    ```

    INFRA-07 reference: include explicit `> Closes **INFRA-07** (`<60min fresh-deploy timing`). Measured timing recorded в §9.` callout at top of §1.
  </action>
  <verify>
    <automated>
      test -f docs/RUNBOOKS/deploy.md \
      && grep -c '^## [0-9]\+\. ' docs/RUNBOOKS/deploy.md | awk '$1 >= 9 { exit 0 } { exit 1 }' \
      && grep -q "INFRA-07" docs/RUNBOOKS/deploy.md \
      && grep -q "provider-agnostic" docs/RUNBOOKS/deploy.md \
      && grep -q "sport-stack.service" docs/RUNBOOKS/deploy.md \
      && grep -q "ansible-playbook" docs/RUNBOOKS/deploy.md \
      && grep -q "<fill prod cutover time>" docs/RUNBOOKS/deploy.md \
      && grep -q "<fill 03-02 deploy time>" docs/RUNBOOKS/deploy.md \
      && (! grep -qi 'hetzner\|digitalocean\|gcp' docs/RUNBOOKS/deploy.md) \
      && (! grep -qiE '\baws\b' docs/RUNBOOKS/deploy.md)
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/RUNBOOKS/deploy.md` with 10 numbered sections (§1..§10 — §10 is Cross-references; §1..§9 are content sections).
    - At least 9 `## <N>. <Title>` section headers present.
    - `INFRA-07` reference в text (anchored callout at top of §1 or in §9 timing table caption).
    - Phrase `provider-agnostic` appears at least once (in §1 Overview or §3 VPS provisioning).
    - Phrase `sport-stack.service` appears (§5 Routine deploy command line OR §4 verification step).
    - `ansible-playbook` command form appears in §4 and §5.
    - `<fill prod cutover time>` and `<fill 03-02 deploy time>` placeholders present in §9 timing table (Task 2 will sed-replace).
    - **Negative-presence:** no `hetzner`, `digitalocean`, `gcp`, or word-boundary `aws` literals anywhere in file (case-insensitive). §8 subsections use neutral `Provider A/B/C (placeholder)` form.
  </acceptance_criteria>
  <done>
    Provider-agnostic deploy RUNBOOK landed. 9 content sections + cross-references. `<fill>` placeholders in §9 ready для Task 2 programmatic sed-substitution. Negative-presence enforced (D-25 NEW honored).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Production cutover — explicit manual-stack teardown (step 3.5, B4 mitigation) → Ansible UP → smoke → sed-fill timing</name>
  <what-built>
    Plan 03-02 Task 2 либо measured timing in dry-run only (если manual stack still occupied port 443), либо deployed to prod in parallel mode. This task performs the actual production cutover: teardown manual stack → run full Ansible site.yml → measure cutover wall-clock → live smoke → programmatic sed-fill of `<fill>` placeholders in `docs/RUNBOOKS/deploy.md §9`.

    **CRITICAL (B4 mitigation):** Explicit `docker compose down --remove-orphans` of existing manual stack BEFORE Ansible play binds port 443. Without this step, port 443 double-bind (manual `caddy:2.8-alpine` + Ansible-managed `caddy:2.8-alpine` via sport-stack.service) kills cutover deterministically.
  </what-built>
  <how-to-verify>
    **Step 1: Backup current prod state (audit trail)**
    ```
    VPS_IP=$(grep ansible_host infra/ansible/inventory/prod/hosts.yml | awk '{print $2}')
    ssh deploy@${VPS_IP} 'cd /opt/sport/services/backend && docker compose ps > /tmp/pre-cutover-ps.txt 2>&1'
    scp deploy@${VPS_IP}:/tmp/pre-cutover-ps.txt .planning/phases/03-infrastructure-as-code/03-03-pre-cutover-ps.txt
    ```

    **Step 2: Sanity check — sport-stack.service is loaded but inactive after Plan 03-02**
    ```
    ssh deploy@${VPS_IP} 'systemctl status sport-stack.service' || true
    # Expect: "loaded; disabled" OR "loaded; enabled; inactive (dead)" — unit file present but не running.
    # If "active (running)" — Plan 03-02 already started it; skip step 3.5 and proceed to step 4
    # (sport-stack is already up; cutover happened during 03-02). If "could not be found" — Plan 03-02
    # did not run; STOP and complete Plan 03-02 first.
    ```

    **Step 3.5 (B4 mitigation — CRITICAL): Explicit teardown of manual stack BEFORE Ansible UP**
    ```
    ssh deploy@${VPS_IP} 'cd /opt/sport/services/backend && docker compose -f docker-compose.prod.yml down --remove-orphans'
    ```
    **Acceptance gate** (MUST pass before step 4):
    ```
    ssh deploy@${VPS_IP} 'docker ps --filter name=caddy --format "{{.Names}}"' | head -1
    ```
    Expected: empty stdout (no Caddy container running). If any Caddy container still listed → repeat teardown с `docker compose -f docker-compose.prod.yml kill && docker compose -f docker-compose.prod.yml rm -f` until empty. **DO NOT proceed to step 4 with port 443 occupied.**

    **Step 4: Ansible cutover with timing capture**
    ```
    cd infra/ansible
    /usr/bin/time -p ansible-playbook -i inventory/prod site.yml 2>&1 \
      | tee ../../.planning/phases/03-infrastructure-as-code/03-03-timing.log
    ```

    **Step 5: Verify systemd unit active**
    ```
    ssh deploy@${VPS_IP} 'systemctl is-active sport-stack.service'
    ```
    Expected: `active`. If anything else → check logs `ssh deploy@${VPS_IP} 'journalctl -u sport-stack.service -n 100 --no-pager'`.

    **Step 6: Live smoke probe (BASE_URL via sslip.io)**
    ```
    BASE_URL="https://${VPS_IP//./-}.sslip.io" python services/backend/scripts/smoke_otp.py
    ```
    Expected: full smoke pass (`🎉 OTP smoke passed.`), exit 0. If Caddy ACME still issuing, retry after 30-60s (up to 5 minutes).

    **Step 7: Programmatic sed-fill placeholders in deploy.md §9 (W5 fix — NO hand-typed numbers)**
    ```
    CUTOVER_REAL=$(grep '^real' .planning/phases/03-infrastructure-as-code/03-03-timing.log | awk '{print $2}')
    DEPLOY_02_REAL=$(grep '^real' .planning/phases/03-infrastructure-as-code/03-02-timing.log | awk '{print $2}')

    # Derive verdicts programmatically (B3-aligned pattern)
    CUTOVER_VERDICT=$(awk -v s="$CUTOVER_REAL" 'BEGIN { secs = s + 0; if (secs > 3600) print "FAIL"; else print "PASS" }')
    DEPLOY_02_VERDICT=$(awk -v s="$DEPLOY_02_REAL" 'BEGIN { secs = s + 0; if (secs > 3600) print "FAIL"; else print "PASS" }')

    sed -i.bak "s|<fill prod cutover time>|${CUTOVER_REAL}|g" docs/RUNBOOKS/deploy.md
    sed -i.bak "s|<fill 03-02 deploy time>|${DEPLOY_02_REAL}|g" docs/RUNBOOKS/deploy.md
    sed -i.bak "s|<fill cutover verdict>|${CUTOVER_VERDICT}|g" docs/RUNBOOKS/deploy.md
    sed -i.bak "s|<fill 03-02 verdict>|${DEPLOY_02_VERDICT}|g" docs/RUNBOOKS/deploy.md
    rm docs/RUNBOOKS/deploy.md.bak
    ```

    **Step 8: Acceptance — verify no `<fill` placeholders left**
    ```
    ! grep -q '<fill' docs/RUNBOOKS/deploy.md
    ```
    Expected exit 0. If FAIL → some `<fill ...>` placeholder still present; investigate why sed did not replace (likely a typo in placeholder text vs sed pattern).

    **Step 9: Verify existing manual stack gone (no stale Caddy outside sport-stack)**
    ```
    ssh deploy@${VPS_IP} 'docker ps --format "{{.Names}}\t{{.Image}}" | grep caddy'
    ```
    Expected: ONLY the Caddy container spawned by `sport-stack.service` (named `re_gateway` per docker-compose.prod.yml L274 `container_name: re_gateway`). No standalone caddy:2.8-alpine outside sport-stack umbrella.

    **Step 10: Commit cutover artifacts**
    ```
    git add docs/RUNBOOKS/deploy.md \
            .planning/phases/03-infrastructure-as-code/03-03-timing.log \
            .planning/phases/03-infrastructure-as-code/03-03-pre-cutover-ps.txt
    git commit -m "feat(03-03): production cutover to Ansible-managed sport-stack.service (B4 teardown applied)"
    ```
  </how-to-verify>
  <resume-signal>
    Type "prod cutover green, smoke 200" если:
    - `systemctl is-active sport-stack.service` returns `active`
    - smoke_otp.py returns exit 0
    - `! grep -q '<fill' docs/RUNBOOKS/deploy.md` exit 0
    - no stale Caddy container outside sport-stack umbrella

    ИЛИ "cutover deferred: <reason>" if blocked (port 443 still occupied, ACME failure persisting >5min, migration drift) — orchestrator carries forward to follow-up plan.
  </resume-signal>
  <acceptance_criteria>
    - `ssh deploy@<prod-ip> 'systemctl is-active sport-stack.service'` returns `active` (exit 0)
    - Live smoke `BASE_URL=https://<prod-ip-dashes>.sslip.io python services/backend/scripts/smoke_otp.py` returns exit 0 (HTTP 200 on all endpoints checked by smoke_otp.py)
    - `! grep -q '<fill' docs/RUNBOOKS/deploy.md` exit 0 (W5 fix — all placeholders replaced programmatically via sed against `/usr/bin/time -p` output)
    - `ssh deploy@<prod-ip> 'docker ps --filter name=caddy --format "{{.Names}}"'` returns ONLY containers managed by sport-stack.service (specifically `re_gateway` per compose container_name) — no stale standalone `caddy:2.8-alpine` outside (verifies cutover happened cleanly)
    - `.planning/phases/03-infrastructure-as-code/03-03-timing.log` committed; contains `/usr/bin/time -p` `real` line
    - `.planning/phases/03-infrastructure-as-code/03-03-pre-cutover-ps.txt` committed (pre-cutover audit trail)
  </acceptance_criteria>
  <done>
    Production cutover complete. Manual `/opt/sport/deploy.sh` stack torn down via explicit `docker compose down --remove-orphans` (B4 mitigation applied). Ansible-managed `sport-stack.service` active, smoke 200. RUNBOOK §9 timing table populated programmatically via sed (W5 fix preserved). Pre-cutover and cutover timing artifacts committed for audit trail.
  </done>
</task>

<task type="auto">
  <name>Task 3: Patch .planning/ROADMAP.md §Phase 3 — reflect pivot reality (criteria edits + 6→8 fix + deferral markers)</name>
  <files>
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/ROADMAP.md (existing §Phase 3 — find current criterion 1..7 wording),
    .planning/phases/03-infrastructure-as-code/03-CONTEXT.md (PIVOT NOTICE + D-22 + D-23 + D-24 + D-25 — drives all edits)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` §Phase 3 in place. The phase section has success criteria 1-7 (pre-pivot) — apply the following targeted edits:

    1. **Criterion 1 (containers under systemd)** — replace `6 Go service systemd units` → `8 Go service containers under sport-stack.service umbrella`. The "6 → 8" carries forward earlier revision (current code on `feat/cursona-redesign` ships 8 services); the wording shift from `systemd units` → `containers under sport-stack.service umbrella` reflects D-04 KEPT (single umbrella unit, not per-service units).

    2. **Criterion 2 (Terraform / cloud-API provisioning)** — prepend `**Deferred to v1.1** (per 2026-05-17 pivot to provider-agnostic VPS — D-22):` to the existing criterion text. Do not delete the criterion; mark as deferred so reader sees what was original scope.

    3. **Criterion 4 (TF state backend)** — prepend `**Deferred to v1.1** (no Terraform in v1.0 per D-22):` to existing text.

    4. **Criterion 5 (network firewall)** — reword to mention UFW (D-24 NEW) — drop any Hetzner Cloud Firewall mention; final wording: `UFW (OS-level) firewall rules explicit; no \`0.0.0.0/0\` except 443 + 22 dev-IPs-only. NATS 4222 / Postgres 5432 / Redis 6379 / MinIO 9000 internal-only.`

    5. **Criterion 6 (Sentry separate VPS)** — prepend `**Moved to Phase 5** (per D-23 — sentry-prep role belongs in Phase 5):` to existing text.

    6. **Top-level phase list** (above Phase 3 details) — find phase 3 entry в top-level list and append ` — Re-Planned 2026-05-17 (5→3 plans, pivot)` to the existing line.

    7. **Cross-cutting constraints subsection** (если присутствует) — drop lines mentioning Object Storage / cx42 / Hetzner Cloud Firewall (all SUPERSEDED per pivot). Keep lines:
       - SOPS via `delegate_to: localhost` (D-12)
       - Caddy stays containerized в compose (D-16)
       - `docker compose` space-form в systemd (D-04 RESEARCH correction)
       - Add UFW (D-24 NEW) line: `OS-level UFW firewall per D-24 (NEW post-pivot — replaces cloud-side firewall)`.

    **Use Edit tool for in-place modifications** — read ROADMAP.md first, then apply each edit as a discrete `Edit` call. Do not rewrite the entire ROADMAP.md.
  </action>
  <verify>
    <automated>
      grep -q '8 Go service containers' .planning/ROADMAP.md \
      && (! grep -q '6 Go service systemd units' .planning/ROADMAP.md) \
      && grep -q 'Deferred to v1.1' .planning/ROADMAP.md \
      && grep -q 'Moved to Phase 5' .planning/ROADMAP.md \
      && grep -q 'UFW' .planning/ROADMAP.md \
      && grep -q 'Re-Planned 2026-05-17' .planning/ROADMAP.md \
      && (! grep -qi 'Hetzner Storage Box.*Terraform state' .planning/ROADMAP.md) \
      && (! grep -qi 'Hetzner Cloud Firewall' .planning/ROADMAP.md)
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '8 Go service containers' .planning/ROADMAP.md` exit 0 (positive — new wording landed)
    - `! grep -q '6 Go service systemd units' .planning/ROADMAP.md` exit 0 (negative — old wording purged)
    - `grep -q 'Deferred to v1.1' .planning/ROADMAP.md` exit 0 (at least 2 occurrences — criteria 2 and 4)
    - `grep -q 'Moved to Phase 5' .planning/ROADMAP.md` exit 0 (criterion 6)
    - `grep -q 'UFW' .planning/ROADMAP.md` exit 0 (criterion 5 reworded)
    - `grep -q 'Re-Planned 2026-05-17' .planning/ROADMAP.md` exit 0 (top-level phase list line marker)
    - `! grep -qi 'Hetzner Storage Box.*Terraform state' .planning/ROADMAP.md` (W4 fix carried — Object Storage wording gone since deferred)
    - `! grep -qi 'Hetzner Cloud Firewall' .planning/ROADMAP.md` (criterion 5 reworded to UFW)
  </acceptance_criteria>
  <done>
    ROADMAP §Phase 3 reflects post-pivot reality. Original criteria preserved with deferral markers for audit trail. Criterion 6→8 fix applied. UFW (D-24 NEW) cited. Hetzner-specific wording purged where superseded.
  </done>
</task>

<task type="auto">
  <name>Task 4: Patch .planning/REQUIREMENTS.md §Phase 3 INFRA-01..07 (deferral markers + 6→8 fix + UFW wording)</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md (existing §Phase 3 INFRA-* section),
    .planning/phases/03-infrastructure-as-code/03-CONTEXT.md (PIVOT NOTICE + D-22 + D-23 + D-24 + D-25)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` §Phase 3 INFRA-01..07 in place:

    1. **INFRA-01** — keep, reword to:
       - Replace `6 Go service` → `8 Go service containers`
       - Replace `Hetzner Cloud Firewall` → `UFW (OS-level firewall per D-24)`
       - Add cross-ref: `(per D-04 + D-24 post-pivot)`

    2. **INFRA-02 (Terraform provisioning)** — prepend `**DEFERRED to v1.1**` with reason: `Reason: 2026-05-17 pivot to provider-agnostic VPS (D-22 NEW) — no cloud-API to call. Reintroduce if migrating to cloud-API provider в v1.1.`

    3. **INFRA-03 (Environments)** — keep, reword environments list:
       - From: `dev / staging / prod` (or similar) → `dev / prod; staging deferred to v1.1 (per D-23)`

    4. **INFRA-04 (TF state backend)** — prepend `**DEFERRED to v1.1**` with reason: `Reason: no Terraform in v1.0 per D-22; revisit when cloud-API provider chosen.`

    5. **INFRA-05** — keep, reword: replace `Network firewall rules` (or similar) → `UFW (OS-level) firewall rules per D-24 NEW post-pivot — same effective ports policy as original Hetzner Cloud Firewall (SUPERSEDED).`

    6. **INFRA-06 (Sentry separate VPS)** — prepend `**MOVED to Phase 5**` with reason: `Reason: per D-23 post-pivot — sentry-prep role belongs in Phase 5 observability scope; Phase 3 no longer provisions sentry-01.`

    7. **INFRA-07 (<60min deploy)** — keep unchanged (still in scope; closed by Plan 03-02 Task 2 + Plan 03-03 Task 2 timing measurement).

    Use Edit tool for in-place modifications.
  </action>
  <verify>
    <automated>
      grep -c '\*\*DEFERRED to v1.1\*\*' .planning/REQUIREMENTS.md | awk '$1 >= 2 { exit 0 } { exit 1 }' \
      && grep -c '\*\*MOVED to Phase 5\*\*' .planning/REQUIREMENTS.md | awk '$1 >= 1 { exit 0 } { exit 1 }' \
      && grep -q '8 Go service containers' .planning/REQUIREMENTS.md \
      && grep -q 'UFW' .planning/REQUIREMENTS.md \
      && grep -q 'staging deferred to v1.1' .planning/REQUIREMENTS.md \
      && grep -q 'INFRA-07' .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\*\*DEFERRED to v1.1\*\*' .planning/REQUIREMENTS.md` returns ≥ 2 (INFRA-02 + INFRA-04)
    - `grep -c '\*\*MOVED to Phase 5\*\*' .planning/REQUIREMENTS.md` returns ≥ 1 (INFRA-06)
    - `grep -q '8 Go service containers' .planning/REQUIREMENTS.md` exit 0 (positive — old `6 Go service` wording purged in INFRA-01)
    - `grep -q 'UFW' .planning/REQUIREMENTS.md` exit 0 (positive — INFRA-01 + INFRA-05 reworded)
    - `grep -q 'staging deferred to v1.1' .planning/REQUIREMENTS.md` exit 0 (INFRA-03 environments list updated)
    - `grep -q 'INFRA-07' .planning/REQUIREMENTS.md` exit 0 (INFRA-07 kept intact)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md §Phase 3 INFRA-01..07 reflects pivot reality. Deferred items explicitly marked (not silently deleted) for audit trail. Phase 4 (CI/CD) planning sees clean picture of what's in scope vs deferred.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing manual `/opt/sport/deploy.sh` stack ↔ Ansible-managed sport-stack.service | Both bind host port 443; cutover requires explicit teardown of manual stack BEFORE Ansible binds. Port 443 is the shared resource. |
| Cutover wall-clock measurement ↔ deploy.md §9 timing table | Programmatic sed-substitution from `/usr/bin/time -p` `real` line — no human-typed numbers (W5 fix). |
| RUNBOOK §6 manual fallback ↔ §5 routine deploy | Two deploy paths during transition window; RUNBOOK explicitly demarcates manual as emergency-only post-cutover. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-PIV-08 | Denial of Service | Zero-downtime cutover broken if existing manual `/opt/sport/deploy.sh`-launched `caddy:2.8-alpine` still bound to port 443 when Ansible play tries to bring up sport-stack.service (which itself spawns Caddy via docker compose). Double-bind → systemd start failure → prod down. | HIGH | mitigate | Task 2 step 3.5 explicit `ssh deploy@<prod-ip> 'docker compose down --remove-orphans'` before step 4 Ansible play. Acceptance gate asserts `docker ps --filter name=caddy` empty post-step-3.5 (MUST pass before step 4). |
| T-03-PIV-AGN-01 | Tampering | Two-deploy-paths confusion (manual `/opt/sport/deploy.sh` AND Ansible) during transition window. Devs could push code via wrong path → state divergence. | MEDIUM | mitigate | RUNBOOK §6 Rollback section explicitly documents manual fallback as emergency ONLY; §5 Routine deploy is Ansible-only. Plan 03-03 SUMMARY records explicit deprecation date for `/opt/sport/deploy.sh` (full removal: Phase 4 CI/CD scope). |
| T-03-PIV-12 | Information Disclosure | RUNBOOK leaks provider-specific names → reveals deployment provider to public reader (e.g. on GitHub) | LOW | mitigate | Task 1 negative grep `! grep -qi 'hetzner\|digitalocean\|gcp' && ! grep -qiE '\baws\b'` enforces provider-agnostic content per D-25 NEW. §8 placeholder subsections use neutral `Provider A/B/C` form. |
| T-03-PIV-13 | Tampering | Hand-typed wall-clock numbers in deploy.md §9 could drift from actual measurement | MEDIUM | mitigate | W5 fix: Task 2 step 7 derives `<fill ...>` values programmatically via `sed` against `/usr/bin/time -p` real-line. Step 8 acceptance gate (`! grep -q '<fill'`) blocks commit if any placeholder unfilled. |

**block_on:** high
**HIGH-severity threats:** T-03-PIV-08 (mitigate, B4 step 3.5 explicit teardown) — all mitigated.
</threat_model>

<verification>
- `docs/RUNBOOKS/deploy.md` exists with ≥9 numbered sections; INFRA-07 cited; no provider-specific literals (negative grep).
- After Task 2 cutover: `sport-stack.service` active; live smoke 200; no stale Caddy container outside sport-stack umbrella.
- `.planning/phases/03-infrastructure-as-code/03-03-timing.log` committed with `/usr/bin/time -p` output; `<fill ...>` placeholders in deploy.md §9 replaced programmatically (W5 fix — `! grep -q '<fill'` exit 0).
- `.planning/ROADMAP.md` §Phase 3 reflects post-pivot reality: `8 Go service containers` wording landed; `6 Go service systemd units` purged; criteria 2 + 4 marked Deferred v1.1; criterion 6 marked Moved to Phase 5; UFW cited.
- `.planning/REQUIREMENTS.md` §INFRA-01..07: INFRA-02 + INFRA-04 marked DEFERRED to v1.1; INFRA-06 marked MOVED to Phase 5; INFRA-01 reworded with 8 containers + UFW.
</verification>

<success_criteria>
- INFRA-01 (CLOSED): sport-stack.service systemd umbrella unit running on prod with all 13 containers; verified via `systemctl is-active sport-stack.service` returns `active` post-cutover.
- INFRA-07 (CLOSED): <60min fresh-deploy timing measured; verdict derived programmatically; recorded в `docs/RUNBOOKS/deploy.md §9` (sed-filled).
- Phase 3 (POST-PIVOT) CLOSED: ROADMAP success criteria 1, 3, 5, 7 (post-pivot 4 criteria total) satisfied. Deferred items (2, 4, 6) explicitly marked.
- RUNBOOK delivered (D-25): provider-agnostic; INFRA-07 cited; 9 sections + cross-references.
- Cutover applied (B4 mitigation): manual stack torn down explicitly; sport-stack.service running; zero stale Caddy.
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-as-code/03-03-SUMMARY.md` per template.

**Required SUMMARY content:**
- Cutover timing copied verbatim from `03-03-timing.log` (real line + awk-derived verdict).
- Live smoke pass evidence post-cutover (HTTP 200 endpoints).
- Pre/post-cutover `docker ps` diff (showing manual stack gone, sport-stack-managed containers present).
- ROADMAP + REQUIREMENTS patches summary: which lines changed, deferral markers added, criterion 6→8 fix applied.
- `/opt/sport/deploy.sh` deprecation note + planned removal date (Phase 4 CI/CD scope per RUNBOOK §6 footer).
- Decisions cited: D-22 (no Terraform), D-23 (single-VPS prod only), D-24 (UFW), D-25 (RUNBOOK provider-agnostic), D-26 (no hetzner SOPS slot — already enforced in Plan 03-02).
- Pivot summary: Phase 3 closes with 3 plans (was 5 pre-pivot); 4 success criteria CLOSED (was 7 pre-pivot, 3 deferred); RUNBOOK provider-agnostic; sport-stack.service umbrella applied; ROADMAP + REQUIREMENTS now reflect pivot reality.
- Carry-forward для Phase 4 (CI/CD):
  - Wire `ansible-playbook -i inventory/prod site.yml --tags sport-stack` into GitHub Actions on tagged release (CICD-01)
  - Rollback drill with real migration in path (CICD-04)
  - Full removal of `/opt/sport/deploy.sh` (cleanup pass)
- Phase 2 follow-up status: #3 (direnv) — CLOSED (documented in deploy.md §2); #5 (DEV_B keys) — status from Plan 03-01 Task 2 carry-forward
</output>
