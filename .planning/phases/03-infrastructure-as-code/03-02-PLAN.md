---
plan_id: 03-02
phase: 3
phase_slug: infrastructure-as-code
wave: 2
depends_on: [03-01]
files_modified:
  - infra/ansible/ansible.cfg
  - infra/ansible/site.yml
  - infra/ansible/inventory/dev/hosts.yml
  - infra/ansible/inventory/staging/hosts.yml
  - infra/ansible/inventory/prod/hosts.yml
  - infra/ansible/inventory/sentry/hosts.yml
  - infra/ansible/group_vars/all.yml
  - infra/ansible/group_vars/staging.yml
  - infra/ansible/group_vars/prod.yml
  - infra/ansible/group_vars/sentry.yml
  - infra/ansible/roles/common/tasks/main.yml
  - infra/ansible/roles/common/handlers/main.yml
  - infra/ansible/roles/common/defaults/main.yml
  - infra/ansible/roles/common/files/sshd_config_overrides
  - infra/ansible/roles/docker/tasks/main.yml
  - infra/ansible/roles/docker/defaults/main.yml
  - infra/ansible/roles/sport-stack/tasks/main.yml
  - infra/ansible/roles/sentry-prep/tasks/main.yml
requirements: [INFRA-01, INFRA-03]
autonomous: false
estimated_duration: "2 h"

must_haves:
  truths:
    - "Inventory split: infra/ansible/inventory/{dev,staging,prod,sentry}/ per CONTEXT D-03"
    - "dev/ inventory указывает на localhost — Ansible НЕ управляет dev (быстрый local docker-compose loop сохранён)"
    - "Staging/prod/sentry inventory hosts читают IPs из Terraform outputs (Wave 1) — не hardcoded в repo"
    - "Deploy user `deploy` (НЕ root) с narrow sudoers: `/bin/systemctl, /usr/bin/docker, /usr/bin/docker compose, /bin/shred /run/sport.env` per CONTEXT D-20"
    - "SSH hardening: PermitRootLogin no + PasswordAuthentication no — enforced через /etc/ssh/sshd_config.d/99-hardening.conf per CONTEXT D-19"
    - "SSH authorized_keys для deploy user провижится из group_vars/all.yml `dev_ssh_pubkeys` map (Ismail + DEV_B once delivered)"
    - "Docker Engine + Compose plugin (НЕ legacy docker-compose) — официальный docs.docker.com apt repo, deploy user в docker group per RESEARCH Pattern 1"
    - "Caddy остаётся в docker-compose как `caddy:2.8-alpine` контейнер — НИКОГДА apt-installed на host (port 443 conflict per RESEARCH correction D-16)"
    - "Wave 2 НЕ деплоит app stack — только база (common + docker). sport-stack role + Caddyfile template — Wave 3a."
    - "Stub роли `sport-stack` и `sentry-prep` существуют только чтобы satisfy `ansible-playbook --syntax-check site.yml` в Wave 2; Wave 3 (03-03a / 03-03b) полностью overwrites их (B2 fix — устраняет syntax-check fail на forward-ref roles)."
    - "`sport_repo_url` group_var sourced from `git remote get-url origin` at Wave 0 setup time, NOT hardcoded в Plan 03-03a Task 1 git clone step (W2 fix)"
    - "[informational] D-13 (SOPS_AGE_KEY_FILE direnv) — документируется в Plan 03-04 deploy.md §1, не реализуется здесь"
    - "[informational] D-18 (sslip.io DNS) — используется автоматически через IP-based subdomain (148-253-214-156.sslip.io); reflected in group_vars/<env>.yml caddy_host (но реальный Caddyfile рендерится в Wave 3a)"
  behaviors:
    - "После Task 1: `cd infra/ansible && ansible-playbook --syntax-check site.yml -i inventory/staging` exit 0 — БЕЗ filter'ов через `--tags`, потому что stub-роли существуют (B2 fix)"
    - "После Task 2 (common+docker roles, dry-run check on staging): `ansible-playbook site.yml -i inventory/staging --check --diff --tags=common,docker` exit 0, changes detected на fresh staging-app-01"
    - "После Task 3 (live apply на staging): SSH через `ssh deploy@<staging-app-01-ipv4>` работает с dev_a ssh key; `ssh root@<staging-app-01-ipv4>` отказывается с 'permission denied' (PermitRootLogin no)"
    - "После Task 3: повторный `ansible-playbook site.yml -i inventory/staging` показывает `changed=0` (idempotent)"
    - "После Task 3: на staging-app-01 `docker compose version` показывает >= 2.32, `docker --version` >= 24.0; `sudo -u deploy docker ps` работает (deploy в docker group)"
  forbidden:
    - "Не запускать common/docker role на dev inventory (localhost) — dev остаётся untouched"
    - "Не использовать legacy `docker-compose` (dash) — только plugin form `docker compose` (space) per RESEARCH Pitfall 4"
    - "Не добавлять `caddy` role с apt-key/apt-repo/apt-install в Wave 2 — Caddy остаётся в compose (RESEARCH correction D-16)"
    - "Не давать deploy user полный sudo — только узкий allow-list из 4 команд (D-20)"
    - "Не оставлять root SSH открытым после apply (PermitRootLogin no обязательно)"
    - "Не commit'ить SSH private keys или ansible-vault'едные секреты — все секреты через SOPS (Phase 2 inheritance)"
    - "Не оставлять site.yml ссылки на не-существующие роли (B2 — stubs обязательны до того как Wave 3 overwrites their real implementations)"
---

<objective>
## Цель плана 03-02

Развернуть Ansible scaffold с 4-inventory структурой (dev/staging/prod/sentry), создать роли `common` (базовые пакеты, deploy user, SSH hardening) и `docker` (Docker Engine + Compose plugin), создать STUB-роли `sport-stack` + `sentry-prep` (чтобы syntax-check site.yml проходил — Wave 3 их overwrites), и применить common/docker к staging-app-01 (создан в Wave 1). После Wave 2 staging-app-01 имеет: deploy user с SSH access от dev workstations, SSH hardened (no root, no password auth), Docker Engine ready.

**Purpose:** Wave 2 закрывает INFRA-03 (inventory structure) и часть INFRA-01 (Docker installed — Caddy/Postgres/Redis/NATS/MinIO/8 services уже есть в docker-compose.prod.yml; Wave 3a их wraps в systemd). Wave 3a (sport-stack role) и Wave 3b (sentry-prep role) обе зависят от готового deploy user + Docker — этот план их provisioning'ит.

**Output:**
- `infra/ansible/` scaffold: ansible.cfg, site.yml, 4 inventory + 4 group_vars + 2 базовых roles (common, docker) + 2 stub roles (sport-stack, sentry-prep) — B2 fix
- Staging-app-01 ready для Wave 3a (sport-stack role)
- Sentry-01 base prep будет в Plan 03-03b (этот план только common + docker base; sentry-01 получит общий common+docker через `hosts: app_servers:sentry` в Wave 3)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-infrastructure-as-code/03-CONTEXT.md
@.planning/phases/03-infrastructure-as-code/03-RESEARCH.md
@.planning/phases/03-infrastructure-as-code/03-PATTERNS.md
@.planning/phases/03-infrastructure-as-code/03-VALIDATION.md
@.planning/phases/03-infrastructure-as-code/03-01-PLAN.md
@.planning/phases/02-secrets-and-config-hardening/02-04-SUMMARY.md
@docs/RUNBOOKS/sops-edit.md

<interfaces>
<!-- Wave 1 (Plan 03-01) предоставляет Terraform outputs для inventory IP wiring -->

From infra/terraform/outputs.tf (created in Plan 03-01 Task 2):
- output "prod_app_ipv4"     = hcloud_server.prod_app_01.ipv4_address    (148.253.214.156)
- output "staging_app_ipv4"  = hcloud_server.staging_app_01.ipv4_address  (dynamic)
- output "sentry_ipv4"       = hcloud_server.sentry_01.ipv4_address       (dynamic)

Use `./infra/terraform/tf-wrap.sh output -raw staging_app_ipv4` to read values during Task 1.

From .planning/phases/02-secrets-and-config-hardening/02-04-SUMMARY.md (Phase 2 follow-ups):
- #5: DEV_B age pubkey + SSH pubkey delivery (carry-over to Plan 03-02 user-action checkpoint).
- #6: ~/.envrc (direnv) для SOPS_AGE_KEY_FILE — defer documentation to Plan 03-04 deploy.md.

From CONTEXT D-19 + D-20 (already locked):
- ansible_user = deploy (НЕ root)
- sudoers: `deploy ALL=(ALL) NOPASSWD: /bin/systemctl, /usr/bin/docker, /usr/bin/docker compose, /bin/shred /run/sport.env`
- PermitRootLogin no + PasswordAuthentication no

Stub roles created в этом плане (B2 fix — overwritten in Wave 3):
- infra/ansible/roles/sport-stack/tasks/main.yml — single ansible.builtin.debug task; Plan 03-03a Task 1 полностью overwrites this file (a также добавляет defaults/handlers/templates/3 additional task files)
- infra/ansible/roles/sentry-prep/tasks/main.yml — single ansible.builtin.debug task; Plan 03-03b Task 1 полностью overwrites this file (плюс defaults/handlers/2 templates)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ansible scaffold — ansible.cfg, site.yml, 4 inventory + 4 group_vars + sshd_config_overrides + 2 stub roles (B2 fix)</name>
  <files>
    infra/ansible/ansible.cfg,
    infra/ansible/site.yml,
    infra/ansible/inventory/dev/hosts.yml,
    infra/ansible/inventory/staging/hosts.yml,
    infra/ansible/inventory/prod/hosts.yml,
    infra/ansible/inventory/sentry/hosts.yml,
    infra/ansible/group_vars/all.yml,
    infra/ansible/group_vars/staging.yml,
    infra/ansible/group_vars/prod.yml,
    infra/ansible/group_vars/sentry.yml,
    infra/ansible/roles/common/files/sshd_config_overrides,
    infra/ansible/roles/sport-stack/tasks/main.yml,
    infra/ansible/roles/sentry-prep/tasks/main.yml
  </files>
  <read_first>
    .planning/phases/03-infrastructure-as-code/03-RESEARCH.md (lines 222-280 — directory structure; lines 717-741 — sample site.yml),
    .planning/phases/03-infrastructure-as-code/03-PATTERNS.md (lines 207-238 — site.yml stub; lines 481-525 — common role excerpt),
    .planning/phases/03-infrastructure-as-code/03-CONTEXT.md (D-03 inventory layout, D-19, D-20),
    services/backend/gateway/Caddyfile.prod (для понимания каких caddy_host значений ожидаем в group_vars)
  </read_first>
  <action>
    1. `infra/ansible/ansible.cfg` — runtime config:
       ```ini
       [defaults]
       inventory             = ./inventory
       roles_path            = ./roles
       collections_paths     = ~/.ansible/collections
       host_key_checking     = True
       retry_files_enabled   = False
       stdout_callback       = yaml
       log_path              = /tmp/ansible-sport.log  ; НЕ commit'ится; для no_log debug only

       [ssh_connection]
       pipelining            = True
       ssh_args              = -o ControlMaster=auto -o ControlPersist=60s
       ```

    2. `infra/ansible/site.yml` — entry playbook (per RESEARCH Code Examples L717-741):
       ```yaml
       ---
       - name: Bootstrap base packages and SSH hardening
         hosts: app_servers:sentry
         become: true
         gather_facts: true
         roles:
           - common
           - docker

       - name: Deploy sport-stack to app servers
         hosts: app_servers
         become: true
         roles:
           - sport-stack   # STUB в этом плане (B2 fix); Plan 03-03a Wave 3a полностью overwrites
         tags: [deploy]

       - name: Prep sentry VPS for Phase 5
         hosts: sentry
         become: true
         roles:
           - sentry-prep   # STUB в этом плане (B2 fix); Plan 03-03b Wave 3b полностью overwrites
         tags: [sentry]
       ```

       **B2 fix:** `--tags` НЕ фильтрует `--syntax-check` — Ansible проверяет, что все роли существуют до того как смотрит teги. Поэтому create stub-роли в шагах 6-7 ниже, чтобы `ansible-playbook --syntax-check site.yml -i inventory/staging` exit 0 БЕЗ `--tags` filter.

    3. **Inventory files** — все 4 в YAML format:

       `infra/ansible/inventory/dev/hosts.yml`:
       ```yaml
       all:
         children:
           app_servers:
             hosts:
               localhost:
                 ansible_connection: local
                 ansible_python_interpreter: "{{ ansible_playbook_python }}"
           sentry:
             hosts: {}   # пусто — dev не имеет Sentry
       ```
       **Important:** dev/ inventory не активирует Ansible-managed services (CONTEXT D-03). Local docker-compose loop сохраняется. Playbook commands вроде `ansible-playbook site.yml -i inventory/dev` no-op'ятся для остальных hosts.

       `infra/ansible/inventory/staging/hosts.yml`:
       ```yaml
       all:
         children:
           app_servers:
             hosts:
               staging-app-01:
                 ansible_host: REPLACE_FROM_TF_OUTPUT_staging_app_ipv4
                 ansible_user: deploy
                 ansible_python_interpreter: /usr/bin/python3
       ```
       Заменить `REPLACE_FROM_TF_OUTPUT_staging_app_ipv4` на реальный IP из `./infra/terraform/tf-wrap.sh output -raw staging_app_ipv4`. **Note для решения о Terraform dynamic inventory:** v1.0 closed beta использует static YAML inventory — Plan 03-04 docs deploy.md документирует "ручное обновление при scale-out". v1.1 может перейти на `hetznercloud.hcloud.hcloud` dynamic inventory plugin.

       `infra/ansible/inventory/prod/hosts.yml`:
       ```yaml
       all:
         children:
           app_servers:
             hosts:
               prod-app-01:
                 ansible_host: 148.253.214.156
                 ansible_user: deploy
                 ansible_python_interpreter: /usr/bin/python3
       ```
       **Initial cutover note:** При первом Ansible run на prod (Plan 03-04 Wave 4), `deploy` user ещё не существует на 148.253.214.156. Plan 03-04 включает bootstrap pass с `ansible_user: root` для создания deploy user, потом переключение на deploy. Cutover sequence — в Plan 03-04, не здесь.

       `infra/ansible/inventory/sentry/hosts.yml`:
       ```yaml
       all:
         children:
           sentry:
             hosts:
               sentry-01:
                 ansible_host: REPLACE_FROM_TF_OUTPUT_sentry_ipv4
                 ansible_user: deploy
                 ansible_python_interpreter: /usr/bin/python3
       ```
       Заменить аналогично через `./infra/terraform/tf-wrap.sh output -raw sentry_ipv4`.

    4. **Group vars files:**

       `infra/ansible/group_vars/all.yml`:
       ```yaml
       ---
       # Shared across all envs (dev/staging/prod/sentry).
       ansible_user: deploy
       deploy_user: deploy
       deploy_group: deploy

       # SSH authorized_keys для deploy user. Phase 2 follow-up #5: DEV_B pubkey
       # будет добавлен позже когда delivered (user-action checkpoint Task 2 ниже).
       dev_ssh_pubkeys:
         dev_a: "ssh-ed25519 AAAA...REPLACE_WITH_REAL_PUBKEY... dev_a@workstation"
         # dev_b: "ssh-ed25519 AAAA...PENDING_DELIVERY... dev_b@workstation"   # uncomment after delivery

       # Caddy ACME email (override per env if needed; для closed beta = один email)
       caddy_acme_email: "REPLACE_WITH_OPS_EMAIL"

       # W2 fix: sport_repo_url sourced from `git remote get-url origin` (не hardcoded в Plan 03-03a Task 1 git clone step).
       # Wave 0 orchestrator runs: REPO_URL="$(git remote get-url origin)" && sed -i.bak "s|sport_repo_url:.*|sport_repo_url: \"$REPO_URL\"|" infra/ansible/group_vars/all.yml && rm infra/ansible/group_vars/all.yml.bak
       sport_repo_url: "REPLACE_FROM_GIT_REMOTE_ORIGIN_URL"
       ```

       `infra/ansible/group_vars/staging.yml`:
       ```yaml
       ---
       env_name: staging
       caddy_host: "REPLACE_FROM_TF_OUTPUT_staging_app_ipv4-with-dashes.sslip.io"
       # Пример: 195-201-32-100.sslip.io если IP = 195.201.32.100
       ```

       `infra/ansible/group_vars/prod.yml`:
       ```yaml
       ---
       env_name: prod
       caddy_host: "148-253-214-156.sslip.io"
       ```
       Совпадает с существующим `services/backend/gateway/Caddyfile.prod` строка 18/27.

       `infra/ansible/group_vars/sentry.yml`:
       ```yaml
       ---
       env_name: sentry
       caddy_host: "sentry.REPLACE_FROM_TF_OUTPUT_sentry_ipv4-with-dashes.sslip.io"
       # Per CONTEXT D-18: sentry получает свой subdomain через own VPS IP
       ```

    5. `infra/ansible/roles/common/files/sshd_config_overrides` — drop-in для /etc/ssh/sshd_config.d/:
       ```
       # /etc/ssh/sshd_config.d/99-hardening.conf — Phase 3 D-19
       PermitRootLogin no
       PasswordAuthentication no
       ChallengeResponseAuthentication no
       UsePAM yes
       X11Forwarding no
       PrintMotd no
       AcceptEnv LANG LC_*
       Subsystem sftp /usr/lib/openssh/sftp-server
       ```

    6. **B2 fix — stub role `sport-stack`** (overwritten in Wave 3a Plan 03-03a Task 1):
       `infra/ansible/roles/sport-stack/tasks/main.yml`:
       ```yaml
       ---
       - name: "stub — full implementation in Plan 03-03a (sport-stack)"
         ansible.builtin.debug:
           msg: "Plan 03-02 stub — overwritten by Plan 03-03a в Wave 3."
       ```
       Цель: satisfy `ansible-playbook --syntax-check site.yml` который parsing'ует все роли. После Plan 03-03a Task 1 файл будет полностью overwritten реальной orchestration (см. 03-03a-PLAN.md interfaces section).

    7. **B2 fix — stub role `sentry-prep`** (overwritten in Wave 3b Plan 03-03b Task 1):
       `infra/ansible/roles/sentry-prep/tasks/main.yml`:
       ```yaml
       ---
       - name: "stub — full implementation in Plan 03-03b (sentry-prep)"
         ansible.builtin.debug:
           msg: "Plan 03-02 stub — overwritten by Plan 03-03b в Wave 3."
       ```
       Same rationale. Plan 03-03b Task 1 полностью overwrites.

    8. **W2 fix — populate `sport_repo_url`:**
       Wave 0 orchestrator step (executor runs):
       ```bash
       REPO_URL="$(git remote get-url origin)"
       sed -i.bak "s|sport_repo_url:.*|sport_repo_url: \"$REPO_URL\"|" infra/ansible/group_vars/all.yml
       rm -f infra/ansible/group_vars/all.yml.bak
       ```
       Verify: `grep sport_repo_url infra/ansible/group_vars/all.yml` показывает реальный URL (`https://github.com/<org>/sport.git` или git@github.com:... — whatever `git remote get-url origin` returns), не `REPLACE_FROM_GIT_REMOTE_ORIGIN_URL`.
  </action>
  <verify>
    <automated>
      test -f infra/ansible/ansible.cfg \
      && test -f infra/ansible/site.yml \
      && test -f infra/ansible/inventory/dev/hosts.yml \
      && test -f infra/ansible/inventory/staging/hosts.yml \
      && test -f infra/ansible/inventory/prod/hosts.yml \
      && test -f infra/ansible/inventory/sentry/hosts.yml \
      && test -f infra/ansible/group_vars/all.yml \
      && test -f infra/ansible/group_vars/staging.yml \
      && test -f infra/ansible/group_vars/prod.yml \
      && test -f infra/ansible/group_vars/sentry.yml \
      && test -f infra/ansible/roles/common/files/sshd_config_overrides \
      && test -f infra/ansible/roles/sport-stack/tasks/main.yml \
      && test -f infra/ansible/roles/sentry-prep/tasks/main.yml \
      && grep -q "ansible_connection: local" infra/ansible/inventory/dev/hosts.yml \
      && grep -q "ansible_host: 148.253.214.156" infra/ansible/inventory/prod/hosts.yml \
      && grep -q "PermitRootLogin no" infra/ansible/roles/common/files/sshd_config_overrides \
      && grep -q "PasswordAuthentication no" infra/ansible/roles/common/files/sshd_config_overrides \
      && grep -q "ansible_user: deploy" infra/ansible/group_vars/all.yml \
      && grep -q "^sport_repo_url:" infra/ansible/group_vars/all.yml \
      && grep -q "stub" infra/ansible/roles/sport-stack/tasks/main.yml \
      && grep -q "stub" infra/ansible/roles/sentry-prep/tasks/main.yml \
      && cd infra/ansible && ansible-inventory -i inventory/staging --list 2>&1 | grep -q "staging-app-01" \
      && ansible-playbook --syntax-check site.yml -i inventory/staging
    </automated>
  </verify>
  <acceptance_criteria>
    - 13 файлов созданы (1 cfg + 1 site.yml + 4 inventory + 4 group_vars + 1 sshd config drop-in + 2 stub roles for sport-stack + sentry-prep)
    - dev/ inventory указывает на localhost (ansible_connection: local)
    - prod/ inventory hardcode'ит 148.253.214.156 (известно из Wave 1)
    - staging/sentry inventory содержат REPLACE_FROM_TF_OUTPUT placeholders (заполняются live'ом из tf-wrap.sh output)
    - sshd_config_overrides содержит PermitRootLogin no + PasswordAuthentication no
    - `ansible-inventory --list` парсит inventory без ошибок
    - **B2 fix:** `ansible-playbook --syntax-check site.yml -i inventory/staging` exit 0 БЕЗ `--tags` filter (stub-роли существуют → forward-refs резолвятся)
    - **W2 fix:** `group_vars/all.yml` содержит `sport_repo_url:` line (после Wave 0 sed pass — реальный git remote URL, не placeholder)
  </acceptance_criteria>
  <done>
    Ansible scaffold готов. Inventory структура INFRA-03 закрыта. Group vars содержат deploy_user, caddy_host placeholders + sport_repo_url для Wave 3a Jinja2 templating. SSH hardening config готов для common role в Task 2. Stub-роли для sport-stack + sentry-prep удовлетворяют syntax-check (B2 fix).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: USER ACTION — DEV_B age pubkey + SSH pubkey delivery (Phase 2 follow-up #5)</name>
  <what-built>
    Task 1 создал group_vars/all.yml с `dev_ssh_pubkeys` map где dev_b закомментирован pending delivery.
  </what-built>
  <how-to-verify>
    Phase 2 carry-over follow-up #5: DEV_B age pubkey (для SOPS rotate-recipients) + DEV_B SSH pubkey (для deploy user authorized_keys на VPS).

    Действия:

    1. **DEV_B генерирует age key** (на своей workstation):
       ```
       age-keygen -o ~/.config/sops/age/keys.txt
       ```
       Копирует **public** часть (строка `# public key:`) — это age pubkey формата `age1...`.

    2. **DEV_B генерирует SSH key** (или использует существующий):
       ```
       ssh-keygen -t ed25519 -C "dev_b@workstation"
       ```
       Копирует содержимое `~/.ssh/id_ed25519.pub`.

    3. **Передаёт обе строки** Ismail'у через safe channel (in-person / Signal / Keybase).

    4. **После получения** (выполняет Ismail):

       (a) Обновить `.sops.yaml` — добавить DEV_B age pubkey в `creation_rules[].age` (через запятую):
       ```yaml
       creation_rules:
         - path_regex: \.secrets/.*\.yaml$
           age: >-
             age1ph7d4a62n9ngghvt5lzgh4eywfayzgrzx9mq6rfzpgp9sme0eg0snl33my,
             age1<DEV_B_PUBKEY>
       ```

       (b) Rotate recipients для всех существующих SOPS files (per Phase 2 RUNBOOK sops-edit.md §6 "Rotate-recipients"):
       ```
       for f in .secrets/**/*.yaml; do sops updatekeys -y "$f"; done
       ```

       (c) Обновить `infra/ansible/group_vars/all.yml` — расскомментировать dev_b в `dev_ssh_pubkeys`:
       ```yaml
       dev_ssh_pubkeys:
         dev_a: "ssh-ed25519 AAAA... dev_a@workstation"
         dev_b: "ssh-ed25519 AAAA... dev_b@workstation"   # ← реальный pubkey из DEV_B
       ```

    5. Verify:
       ```
       grep -c "age1" .sops.yaml    # должно показать минимум 2
       sops -d .secrets/dev/shared.yaml > /dev/null    # decrypt с dev_a key всё ещё работает
       grep dev_b infra/ansible/group_vars/all.yml | grep -v "^#"    # uncommented line найдена
       ```

    **Если DEV_B keys ещё не готовы:** пропустить этот checkpoint, продолжать Task 3 только с dev_a в authorized_keys. DEV_B будет добавлен позже отдельным маленьким plan'ом (или в Plan 03-04 Task X — закрывается там же где документация). НЕ блокировать Wave 3 ради DEV_B.
  </how-to-verify>
  <resume-signal>
    Type "dev_b keys added" если delivery completed и all.yml обновлён.
    ИЛИ "skip dev_b — solo dev_a for now" если DEV_B не готов — Wave 3 продолжается только с dev_a. (Carry-forward: re-open follow-up #5 в Plan 03-04 SUMMARY.)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: common + docker roles — apply на staging-app-01, verify idempotency</name>
  <files>
    infra/ansible/roles/common/tasks/main.yml,
    infra/ansible/roles/common/handlers/main.yml,
    infra/ansible/roles/common/defaults/main.yml,
    infra/ansible/roles/docker/tasks/main.yml,
    infra/ansible/roles/docker/defaults/main.yml
  </files>
  <read_first>
    .planning/phases/03-infrastructure-as-code/03-PATTERNS.md (lines 484-535 — common role + docker role stubs),
    .planning/phases/03-infrastructure-as-code/03-RESEARCH.md (lines 564-577 — Don't Hand-Roll table; lines 632-640 — Pitfall 4 docker compose plugin),
    .planning/phases/03-infrastructure-as-code/03-CONTEXT.md (D-19 SSH hardening, D-20 narrow sudoers),
    infra/ansible/roles/common/files/sshd_config_overrides (Task 1 output — drop-in уже создан)
  </read_first>
  <action>
    1. `infra/ansible/roles/common/defaults/main.yml`:
       ```yaml
       ---
       common_packages:
         - curl
         - gnupg
         - ca-certificates
         - python3
         - python3-apt
         - rsync
         - age   # для SOPS interop (если когда-то нужно decrypt'ить on remote; v1.0 — no)
       ```

    2. `infra/ansible/roles/common/tasks/main.yml` (CIS-aligned, ~30 lines):
       ```yaml
       ---
       - name: Apt update + base packages
         ansible.builtin.apt:
           name: "{{ common_packages }}"
           update_cache: true
           cache_valid_time: 3600
         tags: [common]

       - name: Ensure deploy group exists
         ansible.builtin.group:
           name: "{{ deploy_group }}"
           state: present
         tags: [common]

       - name: Create deploy user (non-root per CONTEXT D-20)
         ansible.builtin.user:
           name: "{{ deploy_user }}"
           group: "{{ deploy_group }}"
           shell: /bin/bash
           create_home: true
           state: present
         tags: [common]

       - name: Install authorized SSH keys для devs (D-19)
         ansible.posix.authorized_key:
           user: "{{ deploy_user }}"
           state: present
           key: "{{ item.value }}"
         loop: "{{ dev_ssh_pubkeys | dict2items }}"
         loop_control:
           label: "{{ item.key }}"
         tags: [common]

       - name: Install SSH hardening drop-in
         ansible.builtin.copy:
           src: sshd_config_overrides
           dest: /etc/ssh/sshd_config.d/99-hardening.conf
           owner: root
           group: root
           mode: '0644'
         notify: restart ssh
         tags: [common]

       - name: Narrow sudoers для deploy user (D-20)
         ansible.builtin.copy:
           dest: /etc/sudoers.d/deploy
           content: |
             {{ deploy_user }} ALL=(ALL) NOPASSWD: /bin/systemctl, /usr/bin/docker, /usr/bin/docker compose, /bin/shred /run/sport.env
           owner: root
           group: root
           mode: '0440'
           validate: '/usr/sbin/visudo -cf %s'
         tags: [common]
       ```

    3. `infra/ansible/roles/common/handlers/main.yml`:
       ```yaml
       ---
       - name: restart ssh
         ansible.builtin.systemd:
           name: ssh
           state: restarted
       ```

    4. `infra/ansible/roles/docker/defaults/main.yml`:
       ```yaml
       ---
       docker_apt_release_channel: stable
       docker_packages:
         - docker-ce
         - docker-ce-cli
         - containerd.io
         - docker-buildx-plugin
         - docker-compose-plugin   # CRITICAL: plugin form (docker compose, space) per RESEARCH Pitfall 4
       ```

    5. `infra/ansible/roles/docker/tasks/main.yml` (docs.docker.com/engine/install/ubuntu — official method):
       ```yaml
       ---
       - name: Apt deps для Docker repository
         ansible.builtin.apt:
           name: [ca-certificates, curl, gnupg]
           update_cache: true
           cache_valid_time: 3600
         tags: [docker]

       - name: Make /etc/apt/keyrings directory
         ansible.builtin.file:
           path: /etc/apt/keyrings
           state: directory
           mode: '0755'
         tags: [docker]

       - name: Download Docker apt key
         ansible.builtin.get_url:
           url: https://download.docker.com/linux/ubuntu/gpg
           dest: /etc/apt/keyrings/docker.asc
           mode: '0644'
           force: false
         tags: [docker]

       - name: Add Docker apt repository
         ansible.builtin.apt_repository:
           repo: "deb [arch={{ ansible_architecture | replace('aarch64','arm64') | replace('x86_64','amd64') }} signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} {{ docker_apt_release_channel }}"
           state: present
           filename: docker
           update_cache: true
         tags: [docker]

       - name: Install Docker Engine + Compose plugin
         ansible.builtin.apt:
           name: "{{ docker_packages }}"
           state: present
         tags: [docker]

       - name: Enable + start docker.service
         ansible.builtin.systemd:
           name: docker
           enabled: true
           state: started
         tags: [docker]

       - name: Add deploy user в docker group
         ansible.builtin.user:
           name: "{{ deploy_user }}"
           groups: [docker]
           append: true
         tags: [docker]

       - name: Verify docker compose (NOT docker-compose dash) installed
         ansible.builtin.command: docker compose version
         register: compose_check
         changed_when: false
         failed_when: compose_check.rc != 0
         tags: [docker]
       ```

    6. **Pre-flight**: обновить `infra/ansible/inventory/staging/hosts.yml` ansible_host placeholder реальным IP из `./infra/terraform/tf-wrap.sh output -raw staging_app_ipv4`. Если DEV_B keys ещё не доставлены (Task 2 skipped) — group_vars/all.yml содержит только dev_a в `dev_ssh_pubkeys` — это OK, Task 3 рабочий с одним dev'ом.

    7. **First-pass apply** на staging-app-01 (initial bootstrap — ansible_user = root для создания deploy user, потом переключение):

       **Bootstrap challenge:** Fresh Hetzner VPS из Terraform имеет SSH access только для `root` (Hetzner cloud-init копирует SSH pubkeys в root's authorized_keys). Deploy user ещё не существует. Нужен one-time bootstrap pass.

       Команды:
       ```
       # Bootstrap pass: root user, no deploy user yet
       ansible-playbook -i inventory/staging --tags=common,docker \
         --user root --extra-vars "ansible_user=root" \
         site.yml

       # После bootstrap deploy user существует — повторный idempotent apply под deploy user
       ansible-playbook -i inventory/staging --tags=common,docker site.yml
       ```

    8. **Verify idempotency** — повторный run без изменений на VPS:
       ```
       ansible-playbook -i inventory/staging --tags=common,docker site.yml
       # Expected output: "changed=0" в PLAY RECAP
       ```

    9. **Verify SSH hardening live:**
       ```
       ssh root@<staging-ip>     # должен FAIL с 'Permission denied' (PermitRootLogin no)
       ssh -p 22 deploy@<staging-ip> "whoami && id"     # должен показать 'deploy' + uid + docker group
       ssh deploy@<staging-ip> "docker compose version"   # должен показать v2.32+
       ```

    10. **Documentation hint** для Plan 03-04: первый prod cutover (Plan 03-04) повторит тот же bootstrap pattern: `ansible-playbook --user root` first run, потом deploy user. На существующей 148.253.214.156 root SSH вероятно уже работает с Ismail'овским pubkey.
  </action>
  <verify>
    <automated>
      cd infra/ansible \
      && ansible-playbook --syntax-check -i inventory/staging site.yml \
      && test -f roles/common/tasks/main.yml \
      && test -f roles/docker/tasks/main.yml \
      && grep -q "docker-compose-plugin" roles/docker/defaults/main.yml \
      && grep -qE 'NOPASSWD:\s+/bin/systemctl.*?/usr/bin/docker.*?/bin/shred' roles/common/tasks/main.yml \
      && grep -q "ansible.posix.authorized_key" roles/common/tasks/main.yml \
      && grep -q "src: sshd_config_overrides" roles/common/tasks/main.yml \
      && ansible-playbook -i inventory/staging --tags=common,docker --check --diff site.yml 2>&1 | grep -qE "PLAY RECAP|ok="
    </automated>
  </verify>
  <acceptance_criteria>
    - Files exist: roles/common/tasks/main.yml, handlers/main.yml, defaults/main.yml; roles/docker/tasks/main.yml, defaults/main.yml
    - `ansible-playbook --syntax-check` exit 0 (для full site.yml, не только with `--tags` — B2 fix wraps stubs)
    - `ansible-playbook --tags=common,docker --check` exit 0, показывает changes которые **were** made (на live staging-app-01 после real apply)
    - W1 fix: sudoers pattern grep matches `NOPASSWD:\s+/bin/systemctl.*?/usr/bin/docker.*?/bin/shred` (matches command pattern, not username `deploy` which может быть templated через `{{ deploy_user }}`)
    - Live SSH test (manual): `ssh root@<staging>` отказывается; `ssh deploy@<staging>` работает с dev_a key; `docker compose version` >= 2.32
    - Idempotent: повторный `ansible-playbook --tags=common,docker` показывает `changed=0`
  </acceptance_criteria>
  <done>
    INFRA-01 partial (common+docker base) и INFRA-03 (inventory) закрыты. Staging-app-01 готов для sport-stack role (Wave 3a). Deploy user + Docker Engine + Compose plugin установлены; SSH hardened. Wave 3a и 3b разблокированы.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation → staging-app-01 SSH | SSH key auth, deploy user (после bootstrap); root login disabled after common role applies |
| Internet → staging-app-01 SSH (port 22) | Hetzner firewall ограничивает 22/TCP до `var.dev_admin_ips` (Wave 1) — defense layer 1; sshd hardening — defense layer 2 |
| deploy user → root via sudo | Narrow sudoers — только 4 explicitly listed commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Elevation of Privilege | Root SSH остаётся открытым после cutover | HIGH | mitigate | `roles/common/files/sshd_config_overrides` enforces `PermitRootLogin no` + `PasswordAuthentication no` + handler restart ssh. После Task 3 idempotent run на staging — root SSH отказывает. Verify через manual `ssh root@<ip>` → permission denied. |
| T-03-08 | Elevation of Privilege | Deploy user имеет полный NOPASSWD sudo | HIGH | mitigate | Sudoers drop-in `/etc/sudoers.d/deploy` whitelist'ит только 4 команды: `/bin/systemctl /usr/bin/docker /usr/bin/docker compose /bin/shred /run/sport.env`. `visudo -cf` валидация в Ansible copy task — sudoers поломки rejected. |
| T-03-09 | Tampering | Docker apt-key чейн compromised | MEDIUM | mitigate | Используется officially documented docs.docker.com keyring (`/etc/apt/keyrings/docker.asc` signed-by), `get_url force: false` (не пересоздаёт key если уже есть — стабильность). Альтернатива (Docker official Ansible role) добавила бы зависимость без security benefit. |
| T-03-10 | Information Disclosure | DEV_B keys компрометированы при delivery | MEDIUM | accept_risk | Out-of-band human delivery (Task 2) — Signal/Keybase + photo verify. Закрытие риска: rotate-on-departure procedure в docs/SECRETS.md (Phase 2 SEC-07). |
| T-03-11 | Denial of Service | Bootstrap pass с `--user root` остаётся в shell history / Ansible log | LOW | accept_risk | One-time bootstrap; log_path `/tmp/ansible-sport.log` не commit'ится (.gitignore). После bootstrap deploy user — root pubkey остаётся в `/root/.ssh/authorized_keys` (Hetzner cloud-init); Task 3 НЕ удаляет (если удалить → break recovery). Documented для Plan 03-04 closing pass. |

**block_on:** high
**HIGH-severity threats:** T-03-07 (mitigate), T-03-08 (mitigate) — обе mitigated.
</threat_model>

<verification>
- `ansible-playbook --syntax-check site.yml -i inventory/staging` exit 0 (full file, не только with `--tags` — B2 stub-роли purr forward refs)
- `ansible-inventory --list` парсит все 4 inventory без ошибок
- Bootstrap pass на staging-app-01 успешный: deploy user создан, docker-ce + compose plugin установлены, SSH hardening enforce'но
- Idempotency verify: 2nd-run `ansible-playbook --tags=common,docker` показывает `changed=0` в PLAY RECAP
- Live test: `ssh root@<staging>` rejected; `ssh deploy@<staging> "docker compose version"` returns v2.32+
- group_vars/staging.yml + sentry.yml имеют real IP'ы (filled from terraform output -raw)
- group_vars/all.yml имеет real `sport_repo_url` value (filled from `git remote get-url origin` — W2 fix)
- Sentry-01 ещё НЕ tracked — он будет provisioning'иться в Wave 3b (Plan 03-03b) через те же common+docker roles (т.к. site.yml `hosts: app_servers:sentry` purr оба группы через common+docker)
</verification>

<success_criteria>
- INFRA-03: `infra/ansible/inventory/{dev,staging,prod,sentry}/` структура существует, ansible-inventory парсит без ошибок
- INFRA-01 partial: Docker Engine + Compose plugin (latest stable, plugin form) установлены на staging-app-01 через `roles/docker/`. Postgres+TimescaleDB+Redis+NATS+MinIO+Caddy уже в docker-compose.prod.yml; Wave 3a wraps в systemd. Прямой apt-install этих сервисов НЕ происходит (per RESEARCH correction D-16 для Caddy — keep в compose).
- staging-app-01 готов принять sport-stack role (Wave 3a)
- Wave 3a и 3b разблокированы — оба зависят от common+docker base + deploy user
- Phase 2 follow-up #5 либо закрыт (DEV_B keys delivered) либо carry-forward'ом помечен в SUMMARY
- B2 + W1 + W2 checker-fix acceptance: full-file syntax-check passes, sudoers grep matches command pattern (не username), sport_repo_url populated from git remote.
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-as-code/03-02-SUMMARY.md` per template.

**Required SUMMARY content:**
- Bootstrap pass timing на fresh staging-app-01 (для INFRA-07 baseline)
- Verify SSH hardening evidence: output `ssh root@<staging>` (rejected), `ssh deploy@<staging>` (accepted)
- Idempotency proof: 2nd-run `changed=0` в PLAY RECAP
- DEV_B keys status: либо added (closes Phase 2 follow-up #5), либо deferred (carry-forward note для Plan 03-04)
- Decisions cited: D-03 (inventory layout), D-19 (SSH hardening enforced), D-20 (narrow sudoers verified)
- RESEARCH correction cited: D-16 (Caddy НЕ apt-install в Wave 2; остаётся в compose, Wave 3a через template-only role) — explicit reminder для Plan 03-03a executor
- group_vars/staging.yml + sentry.yml real-IP populating evidence (tf output snapshots)
- B2 closure: stub-роли для sport-stack + sentry-prep present; `ansible-playbook --syntax-check site.yml` (full, без `--tags`) passes
- W1 closure: sudoers grep matches command pattern (`NOPASSWD:\s+/bin/systemctl.*?/usr/bin/docker.*?/bin/shred`), не hardcoded username
- W2 closure: `grep sport_repo_url infra/ansible/group_vars/all.yml` shows real URL from `git remote get-url origin`
</output>
</output>
