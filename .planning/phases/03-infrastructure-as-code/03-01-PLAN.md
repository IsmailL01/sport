---
plan_id: 03-01
phase: 3
phase_slug: infrastructure-as-code
wave: 1
depends_on: []
files_modified:
  - infra/ansible/ansible.cfg
  - infra/ansible/site.yml
  - infra/ansible/inventory/dev/hosts.yml
  - infra/ansible/inventory/prod/hosts.yml
  - infra/ansible/group_vars/all.yml
  - infra/ansible/group_vars/prod.yml
  - infra/ansible/roles/common/tasks/main.yml
  - infra/ansible/roles/common/handlers/main.yml
  - infra/ansible/roles/common/defaults/main.yml
  - infra/ansible/roles/common/files/sshd_config_overrides
  - infra/ansible/roles/docker/tasks/main.yml
  - infra/ansible/roles/docker/defaults/main.yml
  - infra/ansible/roles/ufw/tasks/main.yml
  - infra/ansible/roles/ufw/defaults/main.yml
  - infra/ansible/roles/sport-stack/tasks/main.yml
requirements: [INFRA-01, INFRA-03, INFRA-05]
autonomous: false
estimated_duration: "2 h"

must_haves:
  truths:
    - "Inventory split: infra/ansible/inventory/{dev,prod}/ per CONTEXT D-03 (PARTIAL post-pivot — staging/sentry dropped per D-23)"
    - "dev/ inventory указывает на localhost — Ansible НЕ управляет dev (быстрый local docker-compose loop сохранён)"
    - "prod/ inventory hardcode'ит current VPS IP (148.253.214.156 by default; user может override через Task 0 pre-flight) — НЕТ Terraform outputs feeding inventory (D-22 — no Terraform)"
    - "Deploy user `deploy` (НЕ root) с narrow sudoers: `/bin/systemctl, /usr/bin/docker, /usr/bin/docker compose, /bin/shred /run/sport.env` per CONTEXT D-20"
    - "SSH hardening: PermitRootLogin no + PasswordAuthentication no — enforced через /etc/ssh/sshd_config.d/99-hardening.conf per CONTEXT D-19"
    - "SSH authorized_keys для deploy user провижится из group_vars/all.yml `dev_ssh_pubkeys` map (Ismail + DEV_B once delivered)"
    - "Docker Engine + Compose plugin (НЕ legacy docker-compose) — официальный docs.docker.com apt repo, deploy user в docker group per RESEARCH Pattern 1"
    - "Caddy остаётся в docker-compose как `caddy:2.8-alpine` контейнер — НИКОГДА apt-installed на host (port 443 conflict per CONTEXT D-16 — KEPT post-pivot)"
    - "UFW (community.general.ufw) replaces Hetzner Cloud Firewall per D-24 (NEW post-pivot) — same effective ports policy: allow 443+22(dev-IPs), deny 80/4222/5432/6379/9000 ingress"
    - "Wave 1 НЕ деплоит app stack — только база (common + docker + ufw). sport-stack role full impl — Wave 2 (Plan 03-02)."
    - "Stub роль `sport-stack` существует только чтобы satisfy `ansible-playbook --syntax-check site.yml` в Wave 1; Wave 2 (Plan 03-02) полностью overwrites её."
    - "`sport_repo_url` group_var sourced from `git remote get-url origin` at Wave 0 setup time, NOT hardcoded в Plan 03-02 Task 1 git clone step"
    - "[informational] D-13 (SOPS_AGE_KEY_FILE direnv) — документируется в Plan 03-03 deploy.md §1, не реализуется здесь"
    - "[informational] D-18 (sslip.io DNS) — используется автоматически через IP-based subdomain (148-253-214-156.sslip.io); reflected в group_vars/prod.yml caddy_host"
    - "[informational] D-26 (NEW post-pivot) — no SOPS slot для VPS access (SSH key-based, no token). 3 per-env SOPS files unchanged: shared.yaml, mapbox.yaml, oauth.yaml"
  behaviors:
    - "После Task 1: `cd infra/ansible && ansible-playbook --syntax-check site.yml -i inventory/prod` exit 0 — БЕЗ filter'ов через `--tags`, потому что stub-роль существует"
    - "После Task 3 (common+docker+ufw roles, dry-run check on prod): `ansible-playbook site.yml -i inventory/prod --check --diff --tags=common,docker,ufw` exit 0, changes detected на existing prod VPS"
    - "После Task 3 (live apply на prod): SSH через `ssh deploy@148.253.214.156` работает с dev_a ssh key; `ssh root@148.253.214.156` отказывается с 'permission denied' (PermitRootLogin no)"
    - "После Task 3: повторный `ansible-playbook site.yml -i inventory/prod` показывает `changed=0` (idempotent)"
    - "После Task 3: на prod VPS `docker compose version` показывает >= 2.32, `docker --version` >= 24.0; `sudo -u deploy docker ps` работает (deploy в docker group)"
    - "После Task 3: `sudo ufw status` показывает 'Status: active' с rules для 443/22 allow + 80/4222/5432/6379/9000 deny incoming"
  forbidden:
    - "Не запускать common/docker/ufw role на dev inventory (localhost) — dev остаётся untouched"
    - "Не использовать legacy `docker-compose` (dash) — только plugin form `docker compose` (space) per RESEARCH Pitfall 4"
    - "Не добавлять `caddy` role с apt-key/apt-repo/apt-install в Wave 1 — Caddy остаётся в compose (D-16 KEPT post-pivot)"
    - "Не давать deploy user полный sudo — только узкий allow-list из 4 команд (D-20)"
    - "Не оставлять root SSH открытым после apply (PermitRootLogin no обязательно)"
    - "Не commit'ить SSH private keys или ansible-vault'едные секреты — все секреты через SOPS (Phase 2 inheritance)"
    - "Не оставлять site.yml ссылки на не-существующие роли (stub sport-stack обязателен до того как Wave 2 overwrites real impl)"
    - "Не enable'ить UFW ДО того как allow 22/TCP от dev_admin_ips — иначе Ansible сам себя залочит вне SSH (UFW gotcha)"
    - "НЕ создавать `infra/terraform/` tree, `.secrets/<env>/hetzner.yaml` slot, `inventory/staging/`, `inventory/sentry/`, `sentry-prep` role — все они SUPERSEDED post-pivot (CONTEXT §PIVOT NOTICE)"
---

<objective>
## Цель плана 03-01 (post-pivot — was 03-02)

Развернуть Ansible scaffold с 2-inventory структурой (dev/prod), создать роли `common` (базовые пакеты, deploy user, SSH hardening), `docker` (Docker Engine + Compose plugin), `ufw` (OS-level firewall per D-24 NEW), создать STUB-роль `sport-stack` (чтобы syntax-check site.yml проходил — Wave 2 Plan 03-02 её overwrites), и применить common/docker/ufw к существующему prod VPS. После Wave 1 prod VPS имеет: deploy user с SSH access от dev workstations, SSH hardened (no root, no password auth), Docker Engine + Compose plugin готовы, UFW активен и enforce'ит ports policy.

**Purpose:** Wave 1 (post-pivot) закрывает INFRA-03 (inventory structure — dev+prod only), INFRA-05 (UFW OS-level firewall per D-24 NEW), и часть INFRA-01 (Docker installed — Caddy/Postgres/Redis/NATS/MinIO/8 services уже есть в docker-compose.prod.yml; Wave 2 их wraps в systemd). Wave 2 (sport-stack role) зависит от готового deploy user + Docker + UFW base — этот план их provisioning'ит.

**Pivot context:** Это план был ранее 03-02. После 2026-05-17 pivot (см. `03-CONTEXT.md §PIVOT NOTICE`) renumbered to 03-01, поскольку оригинальный 03-01 (Terraform scaffold) удалён вместе с целым `infra/terraform/` tree (commit `00bcb39`). Также добавлена новая `ufw` role (D-24) и dropped staging/sentry inventory groups (D-23) + dropped sentry-prep stub role.

**Output:**
- `infra/ansible/` scaffold: ansible.cfg, site.yml, 2 inventory + 2 group_vars + 3 базовых roles (common, docker, ufw) + 1 stub role (sport-stack)
- Existing prod VPS ready для Wave 2 (sport-stack role)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-infrastructure-as-code/03-CONTEXT.md
@.planning/phases/03-infrastructure-as-code/03-RESEARCH.md
@.planning/phases/03-infrastructure-as-code/03-PATTERNS.md
@.planning/phases/02-secrets-and-config-hardening/02-04-SUMMARY.md
@docs/RUNBOOKS/sops-edit.md

<interfaces>
<!-- No prior plans within Phase 3 (this is Wave 1 post-pivot). Phase 2 outputs already locked. -->

From .planning/phases/02-secrets-and-config-hardening/02-04-SUMMARY.md (Phase 2 follow-ups):
- #5: DEV_B age pubkey + SSH pubkey delivery (carry-over to Plan 03-01 Task 2 user-action checkpoint).
- #6: ~/.envrc (direnv) для SOPS_AGE_KEY_FILE — defer documentation to Plan 03-03 deploy.md §1.

From CONTEXT D-19 + D-20 (KEPT post-pivot):
- ansible_user = deploy (НЕ root)
- sudoers: `deploy ALL=(ALL) NOPASSWD: /bin/systemctl, /usr/bin/docker, /usr/bin/docker compose, /bin/shred /run/sport.env`
- PermitRootLogin no + PasswordAuthentication no

From CONTEXT D-24 (NEW post-pivot — UFW replaces Hetzner Cloud Firewall):
- Allow ingress: 443/TCP from 0.0.0.0/0, 22/TCP from `{{ dev_admin_ips }}`
- Deny ingress (default): 80, 4222 (NATS — D-07), 5432 (Postgres), 6379 (Redis), 9000 (MinIO — served via Caddy at `s3.<vps-ip>.sslip.io` per D-08)
- Egress allow all

Stub role created в этом плане (overwritten in Wave 2 Plan 03-02):
- infra/ansible/roles/sport-stack/tasks/main.yml — single ansible.builtin.debug task; Plan 03-02 Task 1 полностью overwrites this file (плюс defaults/handlers/templates/3 additional task files)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: USER PRE-FLIGHT — confirm current VPS public IP + SSH user + sudo access</name>
  <what-built>
    Pre-flight для Ansible inventory. Ansible runs SSH to a provider-agnostic VPS (D-25 post-pivot) — нужно знать current public IP + SSH user that has sudo. По умолчанию используем `148.253.214.156` + текущего SSH user из существующего deploy seam (`docs/RUNBOOKS/sops-edit.md §Deploy script`). Если эти значения изменились — user должен подтвердить.
  </what-built>
  <how-to-verify>
    User должен подтвердить (или предоставить актуальные значения):

    1. **Current VPS public IP** — по умолчанию `148.253.214.156` (из существующего `services/backend/gateway/Caddyfile.prod` + `services/backend/scripts/smoke_otp.py` defaults). Если IP изменился — user provides new value.

    2. **Current SSH user that has sudo** — обычно `root` (Hetzner/typical cloud-init Linux VPS) или собственный admin user если уже custom-provisioned. Wave 1 первый apply на prod использует этот SSH user через `--user <name>` flag; после `common` role создаст `deploy` user, переключение на `deploy` для последующих runs.

    3. **Path to current SSH pubkey** — обычно `~/.ssh/id_ed25519.pub`. Будет вставлен в `infra/ansible/group_vars/all.yml` → `dev_ssh_pubkeys.dev_a` (Task 1 ниже).

    Если user не отвечает в течение нескольких минут (autonomous mode per persistent no-questions instruction), Ansible предположит дефолты: `ansible_host: 148.253.214.156`, `ansible_user: deploy` (после bootstrap; первый bootstrap pass через `--user root`), и `~/.ssh/id_ed25519.pub` для dev_a pubkey.

    **Action на executor**:
    - Если defaults подтверждены — proceed to Task 1.
    - Если изменения — update `infra/ansible/inventory/prod/hosts.yml` `ansible_host` value + `infra/ansible/group_vars/all.yml` `dev_ssh_pubkeys` map.
  </how-to-verify>
  <resume-signal>
    Type "defaults OK" если 148.253.214.156 + ~/.ssh/id_ed25519.pub валидны.
    ИЛИ provide actual values как: "IP=<x.x.x.x>, ssh_user=<name>, pubkey=$(cat ~/.ssh/<file>.pub)".
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1: Ansible scaffold — ansible.cfg, site.yml, 2 inventory + 2 group_vars + sshd_config_overrides + 1 stub role</name>
  <files>
    infra/ansible/ansible.cfg,
    infra/ansible/site.yml,
    infra/ansible/inventory/dev/hosts.yml,
    infra/ansible/inventory/prod/hosts.yml,
    infra/ansible/group_vars/all.yml,
    infra/ansible/group_vars/prod.yml,
    infra/ansible/roles/common/files/sshd_config_overrides,
    infra/ansible/roles/sport-stack/tasks/main.yml
  </files>
  <read_first>
    .planning/phases/03-infrastructure-as-code/03-RESEARCH.md (§Architecture Patterns §Recommended Project Structure; §Pattern 1; §UFW NEW section в SUPERSEDED banner),
    .planning/phases/03-infrastructure-as-code/03-CONTEXT.md (PIVOT NOTICE + D-03 PARTIAL + D-19 + D-20 + D-24 NEW + D-26 NEW),
    services/backend/gateway/Caddyfile.prod (для понимания каких caddy_host значений ожидаем в group_vars),
    docs/RUNBOOKS/sops-edit.md (§Deploy script — existing SSH user / sudo pattern на prod)
  </read_first>
  <action>
    1. `infra/ansible/ansible.cfg` — runtime config:
       ```ini
       [defaults]
       inventory             = ./inventory
       roles_path            = ./roles
       collections_paths     = ~/.ansible/collections
       host_key_checking     = True
       retry_files_enabled   = False
       stdout_callback       = yaml
       log_path              = /tmp/ansible-sport.log  ; НЕ commit'ится; для no_log debug only

       [ssh_connection]
       pipelining            = True
       ssh_args              = -o ControlMaster=auto -o ControlPersist=60s
       ```

    2. `infra/ansible/site.yml` — entry playbook (post-pivot — drop sentry play, keep app_servers only):
       ```yaml
       ---
       - name: Bootstrap base packages + SSH hardening + UFW
         hosts: app_servers
         become: true
         gather_facts: true
         roles:
           - common
           - docker
           - ufw   # NEW post-pivot per D-24 — OS-level firewall (replaces Hetzner Cloud Firewall)

       - name: Deploy sport-stack to app servers
         hosts: app_servers
         become: true
         roles:
           - sport-stack   # STUB в этом плане; Plan 03-02 Wave 2 полностью overwrites
         tags: [deploy]
       ```

       **Note:** Stub `sport-stack` обязателен — `--syntax-check` не фильтрует через `--tags`, парсит все роли упомянутые в playbook. Stub удовлетворяет syntax check.

    3. **Inventory files** — оба в YAML format:

       `infra/ansible/inventory/dev/hosts.yml`:
       ```yaml
       all:
         children:
           app_servers:
             hosts:
               localhost:
                 ansible_connection: local
                 ansible_python_interpreter: "{{ ansible_playbook_python }}"
       ```
       **Important:** dev/ inventory не активирует Ansible-managed services (CONTEXT D-03 PARTIAL post-pivot). Local docker-compose loop сохраняется. Playbook commands `ansible-playbook site.yml -i inventory/dev` no-op'ятся для остальных hosts.

       `infra/ansible/inventory/prod/hosts.yml`:
       ```yaml
       all:
         children:
           app_servers:
             hosts:
               prod-app-01:
                 ansible_host: 148.253.214.156   # default; user can override via Task 0 pre-flight
                 ansible_user: deploy
                 ansible_python_interpreter: /usr/bin/python3
       ```
       **Initial cutover note:** При первом Ansible run на prod (Plan 03-03 Wave 3), `deploy` user ещё может не существовать (если existing manual deploy seam использует `root` или другого admin user). Plan 03-03 включает bootstrap pass с `--user root` (или user-supplied) для создания deploy user, потом переключение на deploy. Cutover sequence — в Plan 03-03, не здесь.

       **DROPPED post-pivot:** `inventory/staging/hosts.yml` (D-23 — staging deferred to v1.1), `inventory/sentry/hosts.yml` (D-23 — sentry deferred to Phase 5).

    4. **Group vars files** (post-pivot — drop staging.yml + sentry.yml):

       `infra/ansible/group_vars/all.yml`:
       ```yaml
       ---
       # Shared across all envs (dev/prod).
       ansible_user: deploy
       deploy_user: deploy
       deploy_group: deploy

       # SSH authorized_keys для deploy user. Phase 2 follow-up #5: DEV_B pubkey
       # будет добавлен позже когда delivered (user-action checkpoint Task 2 ниже).
       dev_ssh_pubkeys:
         dev_a: "ssh-ed25519 AAAA...REPLACE_WITH_REAL_PUBKEY... dev_a@workstation"
         # dev_b: "ssh-ed25519 AAAA...PENDING_DELIVERY... dev_b@workstation"   # uncomment after delivery

       # Dev admin IPs для UFW 22/TCP allow-list (per D-24 NEW post-pivot).
       # Add /32 IPs (с CIDR) для каждой dev workstation. Default placeholder — заменяется на real IPs.
       dev_admin_ips:
         - "REPLACE_WITH_DEV_A_IP/32"
         # - "REPLACE_WITH_DEV_B_IP/32"

       # Caddy ACME email (override per env if needed; для closed beta = один email)
       caddy_acme_email: "REPLACE_WITH_OPS_EMAIL"

       # sport_repo_url sourced from `git remote get-url origin` (не hardcoded в Plan 03-02 Task 1 git clone step).
       # Wave 0 orchestrator runs: REPO_URL="$(git remote get-url origin)" && sed -i.bak "s|sport_repo_url:.*|sport_repo_url: \"$REPO_URL\"|" infra/ansible/group_vars/all.yml && rm infra/ansible/group_vars/all.yml.bak
       sport_repo_url: "REPLACE_FROM_GIT_REMOTE_ORIGIN_URL"
       ```

       `infra/ansible/group_vars/prod.yml`:
       ```yaml
       ---
       env_name: prod
       caddy_host: "148-253-214-156.sslip.io"
       ```
       Совпадает с существующим `services/backend/gateway/Caddyfile.prod` строка 18/27. Если current VPS IP отличается от 148.253.214.156 (Task 0 user-action) — обновить здесь на `<ip-with-dashes>.sslip.io`.

       **DROPPED post-pivot:** `group_vars/staging.yml`, `group_vars/sentry.yml` (per D-23).

    5. `infra/ansible/roles/common/files/sshd_config_overrides` — drop-in для /etc/ssh/sshd_config.d/:
       ```
       # /etc/ssh/sshd_config.d/99-hardening.conf — Phase 3 D-19
       PermitRootLogin no
       PasswordAuthentication no
       ChallengeResponseAuthentication no
       UsePAM yes
       X11Forwarding no
       PrintMotd no
       AcceptEnv LANG LC_*
       Subsystem sftp /usr/lib/openssh/sftp-server
       ```

    6. **Stub role `sport-stack`** (overwritten in Wave 2 Plan 03-02 Task 1):
       `infra/ansible/roles/sport-stack/tasks/main.yml`:
       ```yaml
       ---
       - name: "stub — full implementation in Plan 03-02 (sport-stack)"
         ansible.builtin.debug:
           msg: "Plan 03-01 stub — overwritten by Plan 03-02 в Wave 2."
       ```
       Цель: satisfy `ansible-playbook --syntax-check site.yml` который parsing'ует все роли. После Plan 03-02 Task 1 файл будет полностью overwritten реальной orchestration (см. 03-02-PLAN.md interfaces section).

       **DROPPED post-pivot:** stub role `sentry-prep` НЕ нужен — `sentry` host group dropped из `site.yml` (D-23 — sentry deferred to Phase 5).

    7. **Populate `sport_repo_url`:** Wave 0 orchestrator step (executor runs):
       ```bash
       REPO_URL="$(git remote get-url origin)"
       sed -i.bak "s|sport_repo_url:.*|sport_repo_url: \"$REPO_URL\"|" infra/ansible/group_vars/all.yml
       rm -f infra/ansible/group_vars/all.yml.bak
       ```
       Verify: `grep sport_repo_url infra/ansible/group_vars/all.yml` показывает реальный URL (`https://github.com/<org>/sport.git` или git@github.com:... — whatever `git remote get-url origin` returns), не `REPLACE_FROM_GIT_REMOTE_ORIGIN_URL`.
  </action>
  <verify>
    <automated>
      test -f infra/ansible/ansible.cfg \
      && test -f infra/ansible/site.yml \
      && test -f infra/ansible/inventory/dev/hosts.yml \
      && test -f infra/ansible/inventory/prod/hosts.yml \
      && test -f infra/ansible/group_vars/all.yml \
      && test -f infra/ansible/group_vars/prod.yml \
      && test -f infra/ansible/roles/common/files/sshd_config_overrides \
      && test -f infra/ansible/roles/sport-stack/tasks/main.yml \
      && grep -q "ansible_connection: local" infra/ansible/inventory/dev/hosts.yml \
      && grep -q "ansible_host: 148.253.214.156" infra/ansible/inventory/prod/hosts.yml \
      && grep -q "PermitRootLogin no" infra/ansible/roles/common/files/sshd_config_overrides \
      && grep -q "PasswordAuthentication no" infra/ansible/roles/common/files/sshd_config_overrides \
      && grep -q "ansible_user: deploy" infra/ansible/group_vars/all.yml \
      && grep -q "^sport_repo_url:" infra/ansible/group_vars/all.yml \
      && grep -q "^dev_admin_ips:" infra/ansible/group_vars/all.yml \
      && grep -q "stub" infra/ansible/roles/sport-stack/tasks/main.yml \
      && (! test -d infra/ansible/inventory/staging) \
      && (! test -d infra/ansible/inventory/sentry) \
      && (! test -d infra/ansible/roles/sentry-prep) \
      && cd infra/ansible && ansible-inventory -i inventory/prod --list 2>&1 | grep -q "prod-app-01" \
      && ansible-playbook --syntax-check site.yml -i inventory/prod
    </automated>
  </verify>
  <acceptance_criteria>
    - 8 файлов созданы (1 cfg + 1 site.yml + 2 inventory + 2 group_vars + 1 sshd config drop-in + 1 stub role for sport-stack)
    - dev/ inventory указывает на localhost (ansible_connection: local)
    - prod/ inventory hardcode'ит current VPS IP (default 148.253.214.156; user-overridable via Task 0)
    - sshd_config_overrides содержит PermitRootLogin no + PasswordAuthentication no
    - `ansible-inventory --list` парсит inventory без ошибок
    - `ansible-playbook --syntax-check site.yml -i inventory/prod` exit 0 (stub-роль `sport-stack` существует → forward-ref резолвится; `sentry-prep` НЕ упомянут в site.yml — D-23)
    - `group_vars/all.yml` содержит `sport_repo_url:` line (после Wave 0 sed pass — реальный git remote URL, не placeholder) + `dev_admin_ips:` list для UFW (D-24)
    - **Post-pivot negative-presence:** НЕТ `inventory/staging/`, `inventory/sentry/`, `roles/sentry-prep/` (все dropped per D-23)
  </acceptance_criteria>
  <done>
    Ansible scaffold готов post-pivot. Inventory структура INFRA-03 закрыта (dev+prod only). Group vars содержат deploy_user, caddy_host, dev_admin_ips (UFW), sport_repo_url для Wave 2 Jinja2 templating. SSH hardening config готов для common role в Task 3. Stub role для sport-stack удовлетворяет syntax-check.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: USER ACTION — DEV_B age pubkey + SSH pubkey delivery (Phase 2 follow-up #5)</name>
  <what-built>
    Task 1 создал group_vars/all.yml с `dev_ssh_pubkeys` map где dev_b закомментирован pending delivery, и `dev_admin_ips` list с placeholder для DEV_B IP.
  </what-built>
  <how-to-verify>
    Phase 2 carry-over follow-up #5: DEV_B age pubkey (для SOPS rotate-recipients) + DEV_B SSH pubkey (для deploy user authorized_keys на VPS) + DEV_B workstation IP (для UFW 22/TCP allow-list).

    Действия:

    1. **DEV_B генерирует age key** (на своей workstation):
       ```
       age-keygen -o ~/.config/sops/age/keys.txt
       ```
       Копирует **public** часть (строка `# public key:`) — это age pubkey формата `age1...`.

    2. **DEV_B генерирует SSH key** (или использует существующий):
       ```
       ssh-keygen -t ed25519 -C "dev_b@workstation"
       ```
       Копирует содержимое `~/.ssh/id_ed25519.pub`.

    3. **DEV_B получает свой public IP** (для UFW allow-list):
       ```
       curl -s https://api.ipify.org
       ```

    4. **Передаёт все 3 значения** Ismail'у через safe channel (in-person / Signal / Keybase).

    5. **После получения** (выполняет Ismail):

       (a) Обновить `.sops.yaml` — добавить DEV_B age pubkey в `creation_rules[].age` (через запятую):
       ```yaml
       creation_rules:
         - path_regex: \.secrets/.*\.yaml$
           age: >-
             age1ph7d4a62n9ngghvt5lzgh4eywfayzgrzx9mq6rfzpgp9sme0eg0snl33my,
             age1<DEV_B_PUBKEY>
       ```

       (b) Rotate recipients для всех существующих SOPS files (per Phase 2 RUNBOOK sops-edit.md §6 "Rotate-recipients"):
       ```
       for f in .secrets/**/*.yaml; do sops updatekeys -y "$f"; done
       ```

       (c) Обновить `infra/ansible/group_vars/all.yml` — расскомментировать dev_b в `dev_ssh_pubkeys` + добавить DEV_B IP в `dev_admin_ips`:
       ```yaml
       dev_ssh_pubkeys:
         dev_a: "ssh-ed25519 AAAA... dev_a@workstation"
         dev_b: "ssh-ed25519 AAAA... dev_b@workstation"   # ← реальный pubkey из DEV_B

       dev_admin_ips:
         - "<dev_a_ip>/32"
         - "<dev_b_ip>/32"   # ← реальный IP из DEV_B
       ```

    6. Verify:
       ```
       grep -c "age1" .sops.yaml    # должно показать минимум 2
       sops -d .secrets/dev/shared.yaml > /dev/null    # decrypt с dev_a key всё ещё работает
       grep dev_b infra/ansible/group_vars/all.yml | grep -v "^#"    # uncommented line найдена
       ```

    **Если DEV_B keys ещё не готовы:** пропустить этот checkpoint, продолжать Task 3 только с dev_a в authorized_keys + dev_admin_ips. DEV_B будет добавлен позже отдельным маленьким plan'ом (или в Plan 03-03 Task X — закрывается там же где документация). НЕ блокировать Wave 2 ради DEV_B.
  </how-to-verify>
  <resume-signal>
    Type "dev_b keys added" если delivery completed и all.yml обновлён.
    ИЛИ "skip dev_b — solo dev_a for now" если DEV_B не готов — Wave 2 продолжается только с dev_a. (Carry-forward: re-open follow-up #5 в Plan 03-03 SUMMARY.)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: common + docker + ufw roles — apply на existing prod VPS, verify idempotency</name>
  <files>
    infra/ansible/roles/common/tasks/main.yml,
    infra/ansible/roles/common/handlers/main.yml,
    infra/ansible/roles/common/defaults/main.yml,
    infra/ansible/roles/docker/tasks/main.yml,
    infra/ansible/roles/docker/defaults/main.yml,
    infra/ansible/roles/ufw/tasks/main.yml,
    infra/ansible/roles/ufw/defaults/main.yml
  </files>
  <read_first>
    .planning/phases/03-infrastructure-as-code/03-PATTERNS.md (common + docker role pattern excerpts),
    .planning/phases/03-infrastructure-as-code/03-RESEARCH.md (§Don't Hand-Roll table; §Pitfall 4 docker compose plugin; §UFW NEW section in SUPERSEDED banner),
    .planning/phases/03-infrastructure-as-code/03-CONTEXT.md (D-19 SSH hardening, D-20 narrow sudoers, D-24 NEW UFW policy),
    infra/ansible/roles/common/files/sshd_config_overrides (Task 1 output — drop-in уже создан),
    infra/ansible/group_vars/all.yml (Task 1 output — dev_admin_ips list для UFW)
  </read_first>
  <action>
    1. `infra/ansible/roles/common/defaults/main.yml`:
       ```yaml
       ---
       common_packages:
         - curl
         - gnupg
         - ca-certificates
         - python3
         - python3-apt
         - rsync
         - age   # для SOPS interop (если когда-то нужно decrypt'ить on remote; v1.0 — no)
         - ufw   # required для community.general.ufw module per D-24 NEW
       ```

    2. `infra/ansible/roles/common/tasks/main.yml` (CIS-aligned, ~30 lines):
       ```yaml
       ---
       - name: Apt update + base packages
         ansible.builtin.apt:
           name: "{{ common_packages }}"
           update_cache: true
           cache_valid_time: 3600
         tags: [common]

       - name: Ensure deploy group exists
         ansible.builtin.group:
           name: "{{ deploy_group }}"
           state: present
         tags: [common]

       - name: Create deploy user (non-root per CONTEXT D-20)
         ansible.builtin.user:
           name: "{{ deploy_user }}"
           group: "{{ deploy_group }}"
           shell: /bin/bash
           create_home: true
           state: present
         tags: [common]

       - name: Install authorized SSH keys для devs (D-19)
         ansible.posix.authorized_key:
           user: "{{ deploy_user }}"
           state: present
           key: "{{ item.value }}"
         loop: "{{ dev_ssh_pubkeys | dict2items }}"
         loop_control:
           label: "{{ item.key }}"
         tags: [common]

       - name: Install SSH hardening drop-in
         ansible.builtin.copy:
           src: sshd_config_overrides
           dest: /etc/ssh/sshd_config.d/99-hardening.conf
           owner: root
           group: root
           mode: '0644'
         notify: restart ssh
         tags: [common]

       - name: Narrow sudoers для deploy user (D-20)
         ansible.builtin.copy:
           dest: /etc/sudoers.d/deploy
           content: |
             {{ deploy_user }} ALL=(ALL) NOPASSWD: /bin/systemctl, /usr/bin/docker, /usr/bin/docker compose, /bin/shred /run/sport.env
           owner: root
           group: root
           mode: '0440'
           validate: '/usr/sbin/visudo -cf %s'
         tags: [common]
       ```

    3. `infra/ansible/roles/common/handlers/main.yml`:
       ```yaml
       ---
       - name: restart ssh
         ansible.builtin.systemd:
           name: ssh
           state: restarted
       ```

    4. `infra/ansible/roles/docker/defaults/main.yml`:
       ```yaml
       ---
       docker_apt_release_channel: stable
       docker_packages:
         - docker-ce
         - docker-ce-cli
         - containerd.io
         - docker-buildx-plugin
         - docker-compose-plugin   # CRITICAL: plugin form (docker compose, space) per RESEARCH Pitfall 4
       ```

    5. `infra/ansible/roles/docker/tasks/main.yml` (docs.docker.com/engine/install/ubuntu — official method):
       ```yaml
       ---
       - name: Apt deps для Docker repository
         ansible.builtin.apt:
           name: [ca-certificates, curl, gnupg]
           update_cache: true
           cache_valid_time: 3600
         tags: [docker]

       - name: Make /etc/apt/keyrings directory
         ansible.builtin.file:
           path: /etc/apt/keyrings
           state: directory
           mode: '0755'
         tags: [docker]

       - name: Download Docker apt key
         ansible.builtin.get_url:
           url: https://download.docker.com/linux/ubuntu/gpg
           dest: /etc/apt/keyrings/docker.asc
           mode: '0644'
           force: false
         tags: [docker]

       - name: Add Docker apt repository
         ansible.builtin.apt_repository:
           repo: "deb [arch={{ ansible_architecture | replace('aarch64','arm64') | replace('x86_64','amd64') }} signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} {{ docker_apt_release_channel }}"
           state: present
           filename: docker
           update_cache: true
         tags: [docker]

       - name: Install Docker Engine + Compose plugin
         ansible.builtin.apt:
           name: "{{ docker_packages }}"
           state: present
         tags: [docker]

       - name: Enable + start docker.service
         ansible.builtin.systemd:
           name: docker
           enabled: true
           state: started
         tags: [docker]

       - name: Add deploy user в docker group
         ansible.builtin.user:
           name: "{{ deploy_user }}"
           groups: [docker]
           append: true
         tags: [docker]

       - name: Verify docker compose (NOT docker-compose dash) installed
         ansible.builtin.command: docker compose version
         register: compose_check
         changed_when: false
         failed_when: compose_check.rc != 0
         tags: [docker]
       ```

    6. `infra/ansible/roles/ufw/defaults/main.yml` (NEW post-pivot per D-24):
       ```yaml
       ---
       # Default-deny ingress + allow-list per CONTEXT D-24. Egress always allow.
       ufw_allowed_tcp_public:
         - 443   # Caddy public HTTPS (D-16)
       ufw_denied_tcp_public:
         - 80    # Caddy redirects via redir 308 — no plaintext HTTP
         - 4222  # NATS internal-only per D-07
         - 5432  # Postgres internal-only
         - 6379  # Redis internal-only
         - 9000  # MinIO direct — served via Caddy at s3.<vps-ip>.sslip.io per D-08
       # SSH allow-list (22/TCP) sourced from group_vars/all.yml dev_admin_ips
       ```

    7. `infra/ansible/roles/ufw/tasks/main.yml` (NEW post-pivot per D-24 — community.general.ufw idempotent rules):
       ```yaml
       ---
       # CRITICAL ORDERING per RESEARCH §UFW gotcha:
       # 1. Set default policies (deny in, allow out)
       # 2. Allow SSH from dev_admin_ips FIRST (else Ansible locks itself out when UFW enables)
       # 3. Allow public ports (443 Caddy)
       # 4. Explicit deny public ports (4222/5432/6379/9000 — defense in depth; default deny already blocks them)
       # 5. Enable UFW LAST

       - name: Set UFW default policies
         community.general.ufw:
           direction: "{{ item.direction }}"
           policy: "{{ item.policy }}"
         loop:
           - { direction: incoming, policy: deny }
           - { direction: outgoing, policy: allow }
         tags: [ufw]

       - name: Allow SSH (22/TCP) from dev admin IPs (FIRST — else Ansible locks itself out)
         community.general.ufw:
           rule: allow
           port: '22'
           proto: tcp
           src: "{{ item }}"
         loop: "{{ dev_admin_ips }}"
         tags: [ufw]

       - name: Allow public TCP ports (Caddy 443 per D-16)
         community.general.ufw:
           rule: allow
           port: "{{ item | string }}"
           proto: tcp
         loop: "{{ ufw_allowed_tcp_public }}"
         tags: [ufw]

       - name: Explicit deny public TCP ports (defense in depth — D-07/D-08/D-24)
         community.general.ufw:
           rule: deny
           port: "{{ item | string }}"
           proto: tcp
         loop: "{{ ufw_denied_tcp_public }}"
         tags: [ufw]

       - name: Enable UFW (LAST — after allow rules в place)
         community.general.ufw:
           state: enabled
         tags: [ufw]

       - name: Show UFW status для verification
         ansible.builtin.command: ufw status numbered
         register: ufw_status
         changed_when: false
         tags: [ufw]

       - name: Display UFW status
         ansible.builtin.debug:
           var: ufw_status.stdout_lines
         tags: [ufw]
       ```

    8. **Pre-flight**: обновить `infra/ansible/inventory/prod/hosts.yml` `ansible_host` если default IP (148.253.214.156) изменился (Task 0 user-action). Если DEV_B keys ещё не доставлены (Task 2 skipped) — group_vars/all.yml содержит только dev_a в `dev_ssh_pubkeys` и dev_admin_ips — это OK, Task 3 рабочий с одним dev'ом.

    9. **Pre-flight ansible-galaxy collection install** (community.general для ufw + ansible.posix для authorized_key):
       ```
       ansible-galaxy collection install community.general ansible.posix
       ```

    10. **First-pass apply** на existing prod VPS (initial bootstrap — current SSH user из Task 0 для создания deploy user, потом переключение):

       **Bootstrap challenge:** Existing prod VPS возможно имеет SSH access только для `root` (или другого admin user из Task 0). Deploy user ещё не существует. Нужен one-time bootstrap pass.

       Команды (assuming default `root` SSH user; substitute если Task 0 указал другого):
       ```
       # Bootstrap pass: existing admin user, no deploy user yet
       ansible-playbook -i inventory/prod --tags=common,docker,ufw \
         --user root --extra-vars "ansible_user=root" \
         site.yml

       # После bootstrap deploy user существует — повторный idempotent apply под deploy user
       ansible-playbook -i inventory/prod --tags=common,docker,ufw site.yml
       ```

    11. **Verify idempotency** — повторный run без изменений на VPS:
       ```
       ansible-playbook -i inventory/prod --tags=common,docker,ufw site.yml
       # Expected output: "changed=0" в PLAY RECAP
       ```

    12. **Verify SSH hardening + UFW live:**
       ```
       ssh root@148.253.214.156     # должен FAIL с 'Permission denied' (PermitRootLogin no)
       ssh -p 22 deploy@148.253.214.156 "whoami && id"     # должен показать 'deploy' + uid + docker group
       ssh deploy@148.253.214.156 "docker compose version"   # должен показать v2.32+
       ssh deploy@148.253.214.156 "sudo ufw status numbered" # должен показать 'Status: active' + rules для 443/22/80/4222/5432/6379/9000
       ```

    13. **Documentation hint** для Plan 03-03: первый prod deploy (Plan 03-03 cutover) выполняет те же шаги bootstrap pattern, но уже под `deploy` user (если Wave 1 успешно применился).
  </action>
  <verify>
    <automated>
      cd infra/ansible \
      && ansible-playbook --syntax-check -i inventory/prod site.yml \
      && test -f roles/common/tasks/main.yml \
      && test -f roles/docker/tasks/main.yml \
      && test -f roles/ufw/tasks/main.yml \
      && test -f roles/ufw/defaults/main.yml \
      && grep -q "docker-compose-plugin" roles/docker/defaults/main.yml \
      && grep -qE 'NOPASSWD:\s+/bin/systemctl.*?/usr/bin/docker.*?/bin/shred' roles/common/tasks/main.yml \
      && grep -q "ansible.posix.authorized_key" roles/common/tasks/main.yml \
      && grep -q "src: sshd_config_overrides" roles/common/tasks/main.yml \
      && grep -q "community.general.ufw" roles/ufw/tasks/main.yml \
      && grep -q "state: enabled" roles/ufw/tasks/main.yml \
      && grep -q "dev_admin_ips" roles/ufw/tasks/main.yml \
      && ansible-playbook -i inventory/prod --tags=common,docker,ufw --check --diff site.yml 2>&1 | grep -qE "PLAY RECAP|ok="
    </automated>
  </verify>
  <acceptance_criteria>
    - Files exist: roles/common/{tasks/main.yml, handlers/main.yml, defaults/main.yml}; roles/docker/{tasks/main.yml, defaults/main.yml}; roles/ufw/{tasks/main.yml, defaults/main.yml}
    - `ansible-playbook --syntax-check` exit 0 (для full site.yml — stub sport-stack резолвится)
    - `ansible-playbook --tags=common,docker,ufw --check` exit 0
    - Sudoers pattern grep matches `NOPASSWD:\s+/bin/systemctl.*?/usr/bin/docker.*?/bin/shred` (matches command pattern, не username `deploy` который может быть templated через `{{ deploy_user }}`)
    - UFW role uses `community.general.ufw` module; references `dev_admin_ips`; sets `state: enabled` after allow rules (ordering invariant)
    - Live SSH test (manual): `ssh root@<prod>` отказывается; `ssh deploy@<prod>` работает с dev_a key; `docker compose version` >= 2.32; `sudo ufw status` показывает active + 443/22 allow + 80/4222/5432/6379/9000 deny
    - Idempotent: повторный `ansible-playbook --tags=common,docker,ufw` показывает `changed=0`
  </acceptance_criteria>
  <done>
    INFRA-01 partial (common+docker+ufw base) + INFRA-03 (inventory dev+prod) + INFRA-05 (UFW per D-24 NEW) закрыты. Prod VPS готов для sport-stack role (Wave 2 Plan 03-02). Deploy user + Docker Engine + Compose plugin + UFW установлены; SSH hardened. Wave 2 разблокирован.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation → existing prod VPS SSH | SSH key auth, deploy user (после bootstrap); root login disabled after common role applies |
| Internet → prod VPS SSH (port 22) | UFW (D-24 NEW post-pivot) ограничивает 22/TCP до `{{ dev_admin_ips }}` (group_var) — defense layer 1; sshd hardening — defense layer 2. Replaces original Hetzner Cloud Firewall (SUPERSEDED) |
| deploy user → root via sudo | Narrow sudoers — только 4 explicitly listed commands (D-20) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-AGN-01 | Tampering | Existing manual `/opt/sport/deploy.sh` flow на prod stays consumed during transition — риск two-deploy-paths confusion | MEDIUM | mitigate | Wave 1 НЕ trogает existing compose stack (только base packages + UFW + Docker + deploy user). Cutover (binding port 443 через Ansible-managed stack) выполняется в Wave 3 (Plan 03-03) с explicit `docker compose down` of existing manual stack ДО Ansible UP (B4 mitigation в Plan 03-03 §Task 2 step 3.5). Документация fallback path в `docs/RUNBOOKS/deploy.md §7 Rollback` (Plan 03-03 Task 1). |
| T-03-03 | Elevation of Privilege | Root SSH остаётся открытым после cutover | HIGH | mitigate | `roles/common/files/sshd_config_overrides` enforces `PermitRootLogin no` + `PasswordAuthentication no` + handler restart ssh. После Task 3 idempotent run на prod — root SSH отказывает. Verify через manual `ssh root@<ip>` → permission denied. |
| T-03-04 | Elevation of Privilege | Deploy user имеет полный NOPASSWD sudo | HIGH | mitigate | Sudoers drop-in `/etc/sudoers.d/deploy` whitelist'ит только 4 команды: `/bin/systemctl /usr/bin/docker /usr/bin/docker compose /bin/shred /run/sport.env`. `visudo -cf` валидация в Ansible copy task — sudoers поломки rejected. |
| T-03-05 | Information Disclosure | Plaintext /run/sport.env persists на disk (defense-in-depth) | MEDIUM | mitigate | `/run` = tmpfs (RAM-only). Phase 3 Plan 03-02 будет shred + ExecStopPost; этот план (03-01) не touch /run/sport.env. |
| T-03-06 | Information Disclosure | SOPS на remote (forbidden per D-12) | HIGH | mitigate | Этот план НЕ установит sops binary на prod (only `age` в common_packages — needed для interop, но key не передаётся). Plan 03-02 enforces `delegate_to: localhost`. |
| T-03-08 | Elevation of Privilege | Zero-downtime cutover broken if existing stack not stopped before Ansible UP | HIGH | mitigate | Wave 1 НЕ binds 443. Cutover в Plan 03-03 owns explicit `docker compose down` (B4 mitigation). |
| T-03-AGN-02 | Denial of Service | UFW enables BEFORE allowing SSH — Ansible locks itself out | HIGH | mitigate | `roles/ufw/tasks/main.yml` ordering: (1) set default policies, (2) allow 22/TCP from `dev_admin_ips` FIRST, (3) allow 443, (4) explicit denies, (5) `state: enabled` LAST. Verify ordering preserved (read_first lists task file). |

**Dropped (post-pivot, no longer applicable):**
- T-03-01 (state-in-repo — no TF state) — SUPERSEDED, no Terraform
- T-03-02 (leaked HCLOUD_TOKEN — no token) — SUPERSEDED, no Hetzner API
- T-03-07 (Sentry blast-radius — sentry-01 not in scope) — SUPERSEDED, sentry deferred to Phase 5

**block_on:** high
**HIGH-severity threats:** T-03-03 (mitigate), T-03-04 (mitigate), T-03-06 (mitigate), T-03-08 (mitigate, Plan 03-03 owns cutover), T-03-AGN-02 (mitigate) — все mitigated.
</threat_model>

<verification>
- `ansible-playbook --syntax-check site.yml -i inventory/prod` exit 0 (full file — stub `sport-stack` резолвит forward ref)
- `ansible-inventory --list` парсит обе inventory (dev + prod) без ошибок
- Bootstrap pass на prod VPS успешный: deploy user создан, docker-ce + compose plugin установлены, SSH hardening enforce'но, UFW active с правильными правилами
- Idempotency verify: 2nd-run `ansible-playbook --tags=common,docker,ufw` показывает `changed=0` в PLAY RECAP
- Live test: `ssh root@<prod>` rejected; `ssh deploy@<prod> "docker compose version"` returns v2.32+; `sudo ufw status` shows active + correct rules
- group_vars/all.yml имеет real `sport_repo_url` value (filled from `git remote get-url origin`) + real `dev_admin_ips` (либо placeholder если DEV_B IP не доставлен)
- **Post-pivot negative-presence:** НЕТ `infra/terraform/`, `infra/ansible/inventory/staging/`, `infra/ansible/inventory/sentry/`, `infra/ansible/group_vars/staging.yml`, `infra/ansible/group_vars/sentry.yml`, `infra/ansible/roles/sentry-prep/`, `.secrets/<env>/hetzner.yaml`
</verification>

<success_criteria>
- INFRA-03: `infra/ansible/inventory/{dev,prod}/` структура существует, ansible-inventory парсит без ошибок (staging/sentry dropped per D-23)
- INFRA-01 partial: Docker Engine + Compose plugin (latest stable, plugin form) установлены на prod VPS через `roles/docker/`. Postgres+TimescaleDB+Redis+NATS+MinIO+Caddy уже в docker-compose.prod.yml; Wave 2 wraps в systemd. Прямой apt-install этих сервисов НЕ происходит (per D-16 KEPT — Caddy остаётся containerized).
- INFRA-05: UFW (NEW post-pivot per D-24) идемпотентно enforces ports policy на prod VPS — `sudo ufw status` shows active с 443 allow public + 22 allow dev_admin_ips + 80/4222/5432/6379/9000 deny public (defense in depth с docker network internal-only)
- prod VPS готов принять sport-stack role (Wave 2 Plan 03-02)
- Phase 2 follow-up #5 либо закрыт (DEV_B keys delivered) либо carry-forward'ом помечен в SUMMARY
- Post-pivot scope: НЕТ Terraform, НЕТ staging/sentry inventory, НЕТ sentry-prep role (все SUPERSEDED per D-22/D-23)
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-as-code/03-01-SUMMARY.md` per template.

**Required SUMMARY content:**
- Bootstrap pass timing на existing prod VPS (для INFRA-07 baseline)
- Verify SSH hardening evidence: output `ssh root@<prod>` (rejected), `ssh deploy@<prod>` (accepted)
- Idempotency proof: 2nd-run `changed=0` в PLAY RECAP
- UFW status snapshot (`sudo ufw status numbered` output на prod) — D-24 NEW closure evidence
- DEV_B keys status: либо added (closes Phase 2 follow-up #5), либо deferred (carry-forward note для Plan 03-03)
- Decisions cited: D-03 PARTIAL (inventory dev+prod), D-19 (SSH hardening enforced), D-20 (narrow sudoers verified), D-24 NEW (UFW per pivot)
- RESEARCH correction cited: D-16 (Caddy НЕ apt-install в Wave 1; остаётся в compose) — explicit reminder для Plan 03-02 executor
- group_vars/all.yml real-value populating evidence (sport_repo_url, dev_admin_ips, dev_ssh_pubkeys)
- Post-pivot scope confirmation: НЕТ Terraform, НЕТ staging/sentry/sentry-prep артефактов (negative-presence grep evidence)
</output>
</content>
</invoke>