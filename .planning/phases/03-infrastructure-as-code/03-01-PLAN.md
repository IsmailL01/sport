---
plan_id: 03-01
phase: 3
phase_slug: infrastructure-as-code
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - infra/terraform/main.tf
  - infra/terraform/versions.tf
  - infra/terraform/backend.tf
  - infra/terraform/variables.tf
  - infra/terraform/terraform.tfvars.example
  - infra/terraform/firewall.tf
  - infra/terraform/servers.tf
  - infra/terraform/storage_box.tf
  - infra/terraform/object_storage_state.tf
  - infra/terraform/outputs.tf
  - infra/terraform/tf-wrap.sh
  - .secrets/dev/hetzner.yaml
  - .secrets/staging/hetzner.yaml
  - .secrets/prod/hetzner.yaml
requirements: [INFRA-02, INFRA-04, INFRA-05]
autonomous: false
estimated_duration: "2 h"

must_haves:
  truths:
    - "Terraform >= 1.11 и Ansible >= 2.16 установлены на dev workstation (Wave 0 prereq per RESEARCH §Standard Stack)"
    - "Terraform state живёт в Hetzner Object Storage (S3-compatible, eu-central) с use_lockfile=true, НЕ в Storage Box (Storage Box = SFTP/WebDAV, без conditional writes) per RESEARCH correction D-05"
    - "skip_requesting_account_id=true в backend.tf обязателен — иначе terraform init виснет на STS endpoint (HashiCorp issue #36924)"
    - "Существующий prod-app-01 (148.253.214.156) импортируется через terraform import, НЕ пересоздаётся — zero-downtime требование (CONTEXT D-02, Pitfall 10)"
    - "HCL block пишется ПЕРВЫМ, потом terraform import — иначе следующий apply уничтожит prod (RESEARCH Pitfall 10)"
    - "prod-app-01 атрибуты (server_type, location, image) НЕ хардкодятся в servers.tf литералами — читаются из `infra/terraform/imported.auto.tfvars` (gitignored), который заполняется в Task 1 user-action через `hcloud server describe ... | jq` (B1 fix — устраняет non-determinism исходного плана)"
    - "Hetzner Object Storage bucket для TF state создаётся вручную через Hetzner Console UI ДО первого terraform init (chicken-and-egg per RESEARCH Pattern 5)"
    - "HCLOUD_TOKEN + AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY живут в SOPS-encrypted .secrets/<env>/hetzner.yaml — никогда не в terraform.tfvars и никогда не в git"
    - "Firewall открывает 443/TCP из 0.0.0.0/0 (Caddy), 22/TCP из dev_admin_ips (var) — никаких 80/4222/5432/6379/9000 на public (CONTEXT D-06, D-07, D-08)"
    - "sentry-01 размера cx42 (8 vCPU / 16 GB RAM) per RESEARCH correction D-11 — Sentry self-hosted 2026 минимум 16 GB RAM"
    - "Storage Box (BX10) provisioned в этом плане как Phase 7 pgBackRest target — НЕ как TF state backend (D-05 resolution)"
    - "Если `hcloud_storage_box` resource недоступен в provider версии — WARN-and-escalate: пишем `infra/terraform/storage.tf.skipped` с explicit reason и плановое выполнение abort'ится (I1 fix — устраняет silent degradation INFRA-04)"
    - "tf-wrap.sh использует тот же SOPS-decrypt + trap shred pattern из docs/RUNBOOKS/sops-edit.md §9 (Phase 2 inheritance Pattern A)"
    - "[informational] D-01 (Ansible + Terraform split) — implemented как infra/terraform/ vs infra/ansible/ directory split"
  behaviors:
    - "После Wave 0: `terraform version` >= 1.11 и `ansible --version` >= 2.16 на dev workstation"
    - "После Task 1 (user-action): `infra/terraform/imported.auto.tfvars` существует, содержит 4 ключа (prod_server_id + prod_server_type + prod_location + prod_image) c реальными значениями из Hetzner Console / hcloud CLI"
    - "После Task 2: `terraform fmt -check && terraform validate` exit 0; `terraform plan` показывает 'to be created' для staging-app-01 + sentry-01 + firewall + object_storage + storage_box (НЕ для prod_app_01 после import); servers.tf prod_app_01 block ссылается на `var.prod_server_type` / `var.prod_location` / `var.prod_image`, БЕЗ literal cx22/cx32 или nbg1/fsn1"
    - "После Task 3 (import): `terraform plan` показывает 0 diff для hcloud_server.prod_app_01"
    - "После Task 3 (apply): staging-app-01 + sentry-01 + Storage Box BX10 существуют в Hetzner Console; nmap из public только 443 + 22 (от dev IP) открыты на app VPS"
    - "tf-wrap.sh prod plan читает HCLOUD_TOKEN из SOPS, не из shell env, и токен никогда не попадает в файл на диск (verify через `lsof | grep hetzner.yaml` после exit)"
  forbidden:
    - "Не commit'ить terraform.tfstate*, terraform.tfvars, imported.auto.tfvars, .terraform/, .terraform.lock.hcl только разрешено (lock-файл commitable)"
    - "Не использовать Hetzner Storage Box как TF state backend (нет conditional writes — RESEARCH correction)"
    - "Не запускать `terraform apply` ДО успешного `terraform import hcloud_server.prod_app_01` + clean plan"
    - "Не открывать 4222/5432/6379/9000/80 на public в hcloud_firewall — все internal (D-07)"
    - "Не класть HCLOUD_TOKEN в terraform.tfvars или provider block hardcoded — только из SOPS через tf-wrap.sh"
    - "Не использовать server_type=cx32 для sentry-01 (Sentry OOM-killed на 8 GB — RESEARCH Pitfall 1)"
    - "Не хардкодить literal cx22/cx32/nbg1/fsn1/ubuntu-24.04 для prod_app_01 в servers.tf — только через `var.prod_*` (B1 — предотвращает destroy+recreate из non-deterministic plan)"
    - "Не оставлять silent TODO-комментарий в storage_box.tf при отсутствии provider support — abort plan execution с WARN (I1)"
---

<objective>
## Цель плана 03-01

Развернуть Terraform scaffold для Hetzner Cloud + remote state backend на Hetzner Object Storage + импортировать существующий production VPS (148.253.214.156) в Terraform управление без пересоздания + provisioning'ить нового staging-app-01, sentry-01 (cx42 per RESEARCH correction) и Storage Box (BX10) для Phase 7 pgBackRest.

**Purpose:** Wave 1 закрывает INFRA-02 (Terraform управляет Hetzner Cloud resources), INFRA-04 (TF state в Hetzner Object Storage, не в repo) и INFRA-05 (explicit firewall rules без open `0.0.0.0/0` кроме 443+22). Это блокирует Wave 2 (Ansible использует Terraform outputs для inventory) и Wave 3b (sentry-01 host должен существовать).

**Output:**
- `infra/terraform/` scaffold (11 .tf файлов + tf-wrap.sh)
- 3 new SOPS slots `.secrets/{dev,staging,prod}/hetzner.yaml` с REPLACE_ME placeholders
- `infra/terraform/imported.auto.tfvars` (gitignored, runtime-generated) с реальными атрибутами prod-app-01
- Существующий prod-app-01 в Terraform state
- Provisioned: staging-app-01 (cx22), sentry-01 (cx42), Storage Box BX10
- `.gitignore` patches под Terraform artifacts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-infrastructure-as-code/03-CONTEXT.md
@.planning/phases/03-infrastructure-as-code/03-RESEARCH.md
@.planning/phases/03-infrastructure-as-code/03-PATTERNS.md
@.planning/phases/03-infrastructure-as-code/03-VALIDATION.md
@.planning/phases/02-secrets-and-config-hardening/02-CONTEXT.md
@docs/RUNBOOKS/sops-edit.md
@.sops.yaml
@.gitignore
@.secrets/README.md

<interfaces>
<!-- Phase 2 SOPS slot pattern — Phase 3 hetzner.yaml следует тому же формату -->

From .secrets/<env>/shared.yaml (Phase 2 baseline, ciphertext format):
```yaml
# ENC[AES256_GCM,...,type:comment]
IDENTITY_DB_URL: ENC[AES256_GCM,...,type:str]
...
sops:
    age:
        - enc: |
            -----BEGIN AGE ENCRYPTED FILE-----
            ...
          recipient: age1ph7d4a62n9ngghvt5lzgh4eywfayzgrzx9mq6rfzpgp9sme0eg0snl33my
    version: 3.13.0
```

From .sops.yaml (already auto-covers new slot via regex):
```yaml
creation_rules:
  - path_regex: \.secrets/.*\.yaml$
    age: age1ph7d4a62n9ngghvt5lzgh4eywfayzgrzx9mq6rfzpgp9sme0eg0snl33my
```

From docs/RUNBOOKS/sops-edit.md §9 (deploy.sh pattern — tf-wrap.sh заимствует):
```bash
set -euo pipefail
umask 077
export SOPS_AGE_KEY_FILE="${SOPS_AGE_KEY_FILE:-$HOME/.config/sops/age/keys.txt}"
ENV_FILE="$(mktemp /run/sport.env.XXXXXX)"
trap "shred -u '$ENV_FILE' 2>/dev/null || rm -f '$ENV_FILE'" EXIT
sops -d --output-type=dotenv .secrets/prod/shared.yaml > "$ENV_FILE"
```

Plaintext keys, which will live in `.secrets/<env>/hetzner.yaml` (per env):
- HCLOUD_TOKEN          (Hetzner Cloud API token, scope: read+write)
- AWS_ACCESS_KEY_ID     (Hetzner Object Storage S3 credentials)
- AWS_SECRET_ACCESS_KEY (Hetzner Object Storage S3 credentials)

`infra/terraform/imported.auto.tfvars` (runtime-generated in Task 1, gitignored):
```hcl
prod_server_id   = "<numeric server id из Hetzner Console>"
prod_server_type = "<cx22 | cx32 — реальное значение из hcloud server describe>"
prod_location    = "<nbg1 | fsn1 | hel1 — реальное значение>"
prod_image       = "<ubuntu-24.04 | ... — реальное значение>"
```
Terraform автоматически загружает любые `*.auto.tfvars` без флагов; этот файл — единственный source of truth для prod_app_01 server attributes (B1 fix).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 prerequisites — install Terraform + Ansible, .gitignore patch, SOPS slot creation</name>
  <files>
    .gitignore,
    .secrets/dev/hetzner.yaml,
    .secrets/staging/hetzner.yaml,
    .secrets/prod/hetzner.yaml
  </files>
  <read_first>
    .planning/phases/03-infrastructure-as-code/03-VALIDATION.md (§Wave 0 Requirements),
    .planning/phases/03-infrastructure-as-code/03-RESEARCH.md (lines 119-139 — Installation block),
    .secrets/README.md,
    .sops.yaml,
    .gitignore (existing entries — для понимания формата)
  </read_first>
  <action>
    1. Установить tooling через Homebrew: `brew install terraform ansible`. Verify: `terraform version` показывает >= 1.11 (use_lockfile стабилен с 1.11), `ansible --version` показывает >= 2.16.

    2. Установить Ansible collections, нужные на Wave 2/3: `ansible-galaxy collection install community.sops community.docker hetzner.hcloud`. Verify: `ansible-galaxy collection list community.sops community.docker hetzner.hcloud` показывает 3 строки с installed versions.

    3. Patch `.gitignore` — добавить в конец:
       ```
       # Terraform
       infra/terraform/.terraform/
       infra/terraform/terraform.tfstate
       infra/terraform/terraform.tfstate.backup
       infra/terraform/terraform.tfvars
       infra/terraform/imported.auto.tfvars
       infra/terraform/*.tfplan
       # Lock-файл МОЖНО commit'ить — .terraform.lock.hcl остаётся.
       ```
       НЕ добавлять `.terraform.lock.hcl` в gitignore (commitable per Terraform best practices). `imported.auto.tfvars` — runtime-generated в Task 1 user-action (B1 fix); содержит реальные prod server attrs, не должен попадать в git.

    4. Создать 3 SOPS slots — для каждого env (`dev`, `staging`, `prod`) выполнить:
       ```
       EDITOR=vim sops .secrets/<env>/hetzner.yaml
       ```
       и записать (plaintext в редакторе, SOPS зашифрует при сохранении):
       ```yaml
       HCLOUD_TOKEN: REPLACE_ME
       AWS_ACCESS_KEY_ID: REPLACE_ME
       AWS_SECRET_ACCESS_KEY: REPLACE_ME
       ```
       Реальные значения вставит user в Task 1 (user-action checkpoint). `.sops.yaml` regex `\.secrets/.*\.yaml$` автоматически покрывает новые файлы — никаких изменений в `.sops.yaml` НЕ требуется (Pattern C).

    5. Verify round-trip: `sops -d .secrets/dev/hetzner.yaml | grep HCLOUD_TOKEN` показывает `HCLOUD_TOKEN: REPLACE_ME` (decrypt работает с age key dev_A).
  </action>
  <verify>
    <automated>
      terraform version | grep -E "Terraform v1\.(1[1-9]|[2-9][0-9])" \
      && ansible --version | head -1 | grep -E "ansible \[core 2\.(1[6-9]|[2-9][0-9])" \
      && ansible-galaxy collection list 2>/dev/null | grep -E "community\.(sops|docker)|hetzner\.hcloud" | wc -l | grep -qE "^[[:space:]]*3$" \
      && grep -q "infra/terraform/terraform.tfstate" .gitignore \
      && grep -q "infra/terraform/.terraform/" .gitignore \
      && grep -v '^#' .gitignore | grep -q "infra/terraform/terraform.tfvars" \
      && grep -v '^#' .gitignore | grep -q "infra/terraform/imported.auto.tfvars" \
      && test -f .secrets/dev/hetzner.yaml \
      && test -f .secrets/staging/hetzner.yaml \
      && test -f .secrets/prod/hetzner.yaml \
      && sops -d .secrets/dev/hetzner.yaml | grep -q "HCLOUD_TOKEN: REPLACE_ME"
    </automated>
  </verify>
  <acceptance_criteria>
    - `terraform version` >= 1.11
    - `ansible --version` >= 2.16
    - Три collections (community.sops, community.docker, hetzner.hcloud) установлены
    - `.gitignore` содержит 5 паттернов для Terraform artifacts (state/tfstate.backup/tfvars/imported.auto.tfvars/tfplan) + `.terraform/` directory; `.terraform.lock.hcl` НЕ в .gitignore
    - 3 SOPS-encrypted файла `.secrets/{dev,staging,prod}/hetzner.yaml` существуют, содержат REPLACE_ME placeholders, decrypt'ятся через `sops -d`
  </acceptance_criteria>
  <done>
    Wave 0 prerequisites закрыты. Tooling + .gitignore + пустые SOPS slots готовы для Task 1.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: USER ACTION — Hetzner Cloud API token + Object Storage bucket + capture prod-app-01 attrs в imported.auto.tfvars + paste credentials в SOPS</name>
  <what-built>
    Task 0 создал пустые SOPS slots. Reality check: Hetzner Cloud Console UI — единственный canonical способ создать API token и Object Storage bucket для v1.0 (нет CLI для bootstrap). B1 fix: дополнительно собираем real prod-app-01 attributes (server_type, location, image) в `imported.auto.tfvars`, чтобы Task 2 servers.tf не зависел от литералов "(или cx32 — зависит от значения)".
  </what-built>
  <how-to-verify>
    Действия пользователя в Hetzner Cloud Console (https://console.hetzner.cloud/):

    1. **Войти в Hetzner Cloud проект** где живёт существующий prod VPS 148.253.214.156.

    2. **Создать API token:**
       - Project → Security → API Tokens → Generate API Token.
       - Permissions: **Read + Write**.
       - Description: `terraform-v1.0`.
       - **Скопировать токен** (показывается ОДИН раз).

    3. **Создать Object Storage bucket для TF state:**
       - Project → Object Storage → Create Bucket.
       - Name: `running-ecosystem-tfstate`.
       - Location: `fsn1` (Falkenstein) — выберите регион ближайший к app VPS (или nbg1/Nuremberg).
       - Visibility: **Private**.
       - **Создать S3 credentials:** Object Storage → Credentials → Generate. Скопировать `AWS_ACCESS_KEY_ID` + `AWS_SECRET_ACCESS_KEY`.

    4. **Записать существующий prod-app-01 attrs через hcloud CLI (B1 fix — eliminate non-determinism):**

       Установить hcloud CLI (если ещё не установлен): `brew install hcloud`. Аутентифицироваться: `hcloud context create terraform-v1.0` (paste API token из шага 2).

       Запустить:
       ```
       hcloud server describe prod-app-01 -o json \
         | jq -r '{server_type: .server_type.name, location: .datacenter.location.name, image: .image.name, id: .id} | to_entries | map("\(.key): \(.value)") | .[]'
       ```

       Альтернатива через Console UI (если hcloud CLI недоступен):
       - Project → Servers → 148.253.214.156 → Overview tab.
       - Запишите: `server_type` (`cx22` или `cx32`), `location` (`nbg1`/`fsn1`/`hel1`), `image` (`ubuntu-24.04` ожидается), server `id` (видно в URL `/projects/<pid>/servers/<id>`).

    5. **Записать значения в `infra/terraform/imported.auto.tfvars`** (B1 fix — этот файл gitignored, single source of truth для prod_app_01 attrs которые Task 2 servers.tf consumes):
       ```bash
       mkdir -p infra/terraform
       cat > infra/terraform/imported.auto.tfvars <<EOF
       prod_server_id   = "<id из шага 4>"
       prod_server_type = "<server_type из шага 4 — например cx22>"
       prod_location    = "<location из шага 4 — например nbg1>"
       prod_image       = "<image из шага 4 — например ubuntu-24.04>"
       EOF
       ```
       Verify: `cat infra/terraform/imported.auto.tfvars` показывает все 4 строки без `<...>` placeholder'ов.

    6. **Paste credentials в SOPS** (выполнить эти команды локально):
       ```
       EDITOR=vim sops .secrets/prod/hetzner.yaml
       ```
       Заменить `REPLACE_ME` на реальные значения для всех 3 ключей. Сохранить (SOPS перешифрует).
       Повторить для `.secrets/staging/hetzner.yaml` и `.secrets/dev/hetzner.yaml` (один тот же токен — single Hetzner project per CONTEXT D-02).

    7. **Verify decrypt:**
       ```
       sops -d .secrets/prod/hetzner.yaml | grep -v REPLACE_ME
       ```
       Должна вывести все 3 ключа с реальными значениями (без REPLACE_ME placeholders).
  </how-to-verify>
  <resume-signal>
    Type "credentials pasted + imported.auto.tfvars filled" когда:
    - SOPS .secrets/{dev,staging,prod}/hetzner.yaml содержат реальные значения (no REPLACE_ME);
    - `infra/terraform/imported.auto.tfvars` существует и содержит 4 заполненных строки (prod_server_id, prod_server_type, prod_location, prod_image).
    Task 2 будет читать эти значения через `var.prod_*` references в servers.tf — никакие литералы в Task 2 не выбираются вручную (B1 fix).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Terraform scaffold — main/versions/backend/variables/firewall/storage/outputs + tf-wrap.sh + servers.tf HCL первым (B1: prod attrs читаются из var.prod_*)</name>
  <files>
    infra/terraform/main.tf,
    infra/terraform/versions.tf,
    infra/terraform/backend.tf,
    infra/terraform/variables.tf,
    infra/terraform/terraform.tfvars.example,
    infra/terraform/firewall.tf,
    infra/terraform/servers.tf,
    infra/terraform/storage_box.tf,
    infra/terraform/object_storage_state.tf,
    infra/terraform/outputs.tf,
    infra/terraform/tf-wrap.sh
  </files>
  <read_first>
    .planning/phases/03-infrastructure-as-code/03-RESEARCH.md (lines 421-548 — Patterns 4-6 для servers.tf/backend.tf/firewall.tf),
    .planning/phases/03-infrastructure-as-code/03-PATTERNS.md (lines 60-200 — Terraform layer stubs),
    docs/RUNBOOKS/sops-edit.md (§9 — для tf-wrap.sh shape),
    services/backend/docker-compose.prod.yml (для подтверждения текущих portов: только Caddy 80/443 наружу через docker, всё остальное internal),
    infra/terraform/imported.auto.tfvars (Task 1 user-action output — source of truth для prod_app_01 attrs)
  </read_first>
  <action>
    **HCL FIRST** (RESEARCH Pitfall 10) — пишем все .tf файлы ДО любого terraform apply. После всех 11 файлов запускаем `terraform init` (создаёт remote state + downloads providers), потом `terraform plan` (sanity — должен показать "to be created" для staging-app-01 + sentry-01 + storage_box + firewall + object_storage_state, и для **prod_app_01 тоже** — это ожидаемо, потому что import ещё не сделан; не apply'им).

    **B1 fix:** prod_app_01 server attributes (server_type, location, image) НЕ выбираются executor'ом литерально в этом task. Они приходят из `infra/terraform/imported.auto.tfvars` (заполненного user'ом в Task 1) через `variable "prod_*"` declarations и `var.prod_*` references в servers.tf. Это устраняет non-determinism исходного плана и предотвращает destroy+recreate prod-app-01 в Task 3 plan.

    1. `infra/terraform/versions.tf` — `terraform { required_version = ">= 1.11" }` + `required_providers { hcloud = { source = "hetznercloud/hcloud"; version = "~> 1.62" } }`.

    2. `infra/terraform/main.tf` — `provider "hcloud" { token = var.hcloud_token }` + `locals { common_labels = { managed_by = "terraform", milestone = "v1.0" } }`.

    3. `infra/terraform/backend.tf` — `terraform { backend "s3" { bucket = "running-ecosystem-tfstate"; endpoint = "https://fsn1.your-objectstorage.com"; key = "infra/prod.tfstate"; region = "main"; skip_credentials_validation = true; skip_metadata_api_check = true; skip_region_validation = true; skip_requesting_account_id = true; use_lockfile = true; force_path_style = true } }`. **Critical:** все 4 skip-флага + `use_lockfile = true` + `force_path_style = true` — иначе init fails timeout (Pitfall 2).

    4. `infra/terraform/variables.tf` — declarations:
       - `variable "hcloud_token" { type = string, sensitive = true }`
       - `variable "dev_admin_ips" { type = list(string), description = "/32 IPs from dev workstations" }`
       - `variable "dev_ssh_pubkeys" { type = map(string), description = "name → pubkey" }`
       - `variable "prod_server_id" { type = string, description = "Existing 148.253.214.156 server_id; sourced from imported.auto.tfvars (Task 1 user-action)" }`
       - **`variable "prod_server_type" { type = string, description = "Existing prod-app-01 server_type (e.g. cx22 or cx32); sourced from imported.auto.tfvars (B1 fix — eliminates non-deterministic literal in servers.tf)" }`**
       - **`variable "prod_location" { type = string, description = "Existing prod-app-01 location (nbg1|fsn1|hel1); sourced from imported.auto.tfvars" }`**
       - **`variable "prod_image" { type = string, description = "Existing prod-app-01 image (e.g. ubuntu-24.04); sourced from imported.auto.tfvars" }`**

       **Note:** Terraform auto-loads `*.auto.tfvars` без явных `-var-file` флагов. `imported.auto.tfvars` создан в Task 1 и читается автоматически на любом `terraform plan/apply/import`. Никаких defaults для `prod_*` variables — если файла нет, plan fails fast (intentional: prevents executor от писания литералов).

    5. `infra/terraform/terraform.tfvars.example` — placeholder values, commitable; реальные `terraform.tfvars` НЕ commit'ятся (Task 0 .gitignore). prod_* НЕ дублируются здесь — они живут в `imported.auto.tfvars`:
       ```
       # Скопировать в terraform.tfvars и заполнить реальными значениями
       # HCLOUD_TOKEN/AWS credentials берутся из SOPS через tf-wrap.sh, не отсюда.
       # prod_server_id + prod_server_type + prod_location + prod_image живут в imported.auto.tfvars (Task 1 user-action; gitignored).
       dev_admin_ips = ["YOUR_WORKSTATION_IP/32"]
       dev_ssh_pubkeys = {
         dev_a = "ssh-ed25519 AAAA... dev_a@..."
         dev_b = "ssh-ed25519 AAAA... dev_b@..."  # пока пусто — Phase 2 follow-up #5, Plan 03-02 Task X
       }
       ```

    6. `infra/terraform/firewall.tf` — два `hcloud_firewall` ресурса (per RESEARCH Pattern 6):
       - `hcloud_firewall.app` (attached к staging-app-01 + prod-app-01): in 443/TCP from `["0.0.0.0/0", "::/0"]`, in 22/TCP from `var.dev_admin_ips`. Никаких 80/4222/5432/6379/9000. Egress default-allow (no rules = allow-all per Hetzner default).
       - `hcloud_firewall.sentry` (attached к sentry-01): in 443/TCP from `["0.0.0.0/0", "::/0"]`, in 22/TCP from `var.dev_admin_ips`. Идентичен app firewall (Phase 5 wires Sentry UI на 443).

    7. `infra/terraform/servers.tf` — три `hcloud_server` ресурса + `hcloud_ssh_key` resources (по одному на dev). **B1 fix: prod_app_01 attrs приходят из `var.prod_*` — НИ ОДНОГО литерала cx22/cx32/nbg1/ubuntu-24.04 для prod_app_01 в этом файле:**
       - `hcloud_ssh_key.devs` через `for_each = var.dev_ssh_pubkeys` (создаёт по одному key per dev).
       - `hcloud_server.prod_app_01`:
         - `server_type = var.prod_server_type` (NOT literal — B1)
         - `image       = var.prod_image` (NOT literal — B1)
         - `location    = var.prod_location` (NOT literal — B1)
         - `ssh_keys    = [for k in hcloud_ssh_key.devs : k.id]`
         - `firewall_ids = [hcloud_firewall.app.id]`
         - `labels = merge(local.common_labels, { env = "prod", role = "app" })`
         - `lifecycle { ignore_changes = [image, user_data, ssh_keys] }` (Pitfall 7 — ssh_keys drift не должен trigger'ить recreate)
       - `hcloud_server.staging_app_01`: НОВЫЙ ресурс — литералы здесь OK (это greenfield create, не import): `server_type = "cx22"`, `image = "ubuntu-24.04"`, `location = "nbg1"` (или `var.prod_location` — следовать prod region для latency parity), `firewall_ids = [hcloud_firewall.app.id]`, `labels = { env = "staging", role = "app" }`.
       - `hcloud_server.sentry_01`: НОВЫЙ ресурс — литералы OK: **`server_type = "cx42"`** (per RESEARCH correction D-11 — Sentry self-hosted 2026 требует 16 GB RAM, cx32 = 8 GB OOM-killed), `image = "ubuntu-24.04"`, `location = "nbg1"` (или `var.prod_location`), `firewall_ids = [hcloud_firewall.sentry.id]`, `labels = { env = "sentry", role = "observability" }`.

    8. `infra/terraform/storage_box.tf` — `hcloud_storage_box.pgbackrest` (через hetznercloud/hcloud provider): `name = "pgbackrest-prod"`, `location = "fsn1"`, `storage_box_type = "bx10"` (1 TB minimum, ~€3/mo) — provisioned в Phase 3 как Phase 7 pgBackRest target per RESEARCH D-05 resolution. **Не как TF state backend** — Object Storage уже отдельно (backend.tf).

       **I1 fix — WARN-and-escalate when provider lacks Storage Box support:** Если `hcloud_storage_box` resource недоступен в текущей `hetznercloud/hcloud` provider версии (verify через `terraform providers schema -json | jq -e '.provider_schemas | to_entries[] | select(.key | contains("hcloud")).value.resource_schemas.hcloud_storage_box'`):
       - НЕ оставлять silent TODO-комментарий.
       - Renaming `storage_box.tf` → `storage_box.tf.skipped` с explicit content:
         ```hcl
         # SKIPPED: hcloud_storage_box resource не поддержан в hetznercloud/hcloud provider версии $(terraform version -json | jq -r '.provider_selections."registry.terraform.io/hetznercloud/hcloud"').
         # INFRA-04 (Phase 7 pgBackRest target) BLOCKED until:
         #   - provider upgrade landing hcloud_storage_box, OR
         #   - manual provisioning via Hetzner Console + import (analogous to prod_app_01).
         # Plan execution MUST abort here; do not silently degrade.
         ```
       - Plan executor MUST abort task с non-zero exit и flag блокирующий issue в SUMMARY. Tasks 3 не запускается пока INFRA-04 не разблокирован (отдельный hygiene plan или manual Hetzner Console provisioning).

       Verify через `terraform providers schema` после `terraform init` (Task 3).

    9. `infra/terraform/object_storage_state.tf` — комментарий-only файл: bucket `running-ecosystem-tfstate` создан вручную в Task 1 user-action (chicken-and-egg per Pattern 5); НЕ ресурс в TF (потому что backend этого же state ссылается на него). Файл содержит документацию: "Bucket created out-of-band via Hetzner Console — see Task 1 user-action checkpoint and docs/RUNBOOKS/deploy.md §2."

    10. `infra/terraform/outputs.tf` — exports для Ansible inventory consumption (Wave 2):
        - `output "prod_app_ipv4" { value = hcloud_server.prod_app_01.ipv4_address }`
        - `output "staging_app_ipv4" { value = hcloud_server.staging_app_01.ipv4_address }`
        - `output "sentry_ipv4" { value = hcloud_server.sentry_01.ipv4_address }`
        - `output "storage_box_subuser" { value = hcloud_storage_box.pgbackrest.username, sensitive = true }` (если provider поддерживает — иначе пропустить per I1 escalation).

    11. `infra/terraform/tf-wrap.sh` — wrapper script (per PATTERNS Pattern A + sops-edit.md §9):
        ```bash
        #!/usr/bin/env bash
        set -euo pipefail
        umask 077

        ENV="${TF_ENV:-prod}"
        REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
        export SOPS_AGE_KEY_FILE="${SOPS_AGE_KEY_FILE:-$HOME/.config/sops/age/keys.txt}"

        # Decrypt Hetzner credentials в env vars (HCLOUD_TOKEN, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
        # без landing'а на disk. eval — потому что output sops dotenv format — это KEY=VALUE lines.
        eval "$(sops -d --output-type=dotenv "$REPO_ROOT/.secrets/${ENV}/hetzner.yaml" | sed 's/^/export /')"

        cd "$REPO_ROOT/infra/terraform"
        exec terraform "$@"
        ```
        `chmod +x infra/terraform/tf-wrap.sh`.

        **Use:** `./infra/terraform/tf-wrap.sh init` / `... plan` / `... apply` — никогда не `terraform` напрямую (или будет no creds).
  </action>
  <verify>
    <automated>
      test -f infra/terraform/main.tf \
      && test -f infra/terraform/versions.tf \
      && test -f infra/terraform/backend.tf \
      && test -f infra/terraform/variables.tf \
      && test -f infra/terraform/firewall.tf \
      && test -f infra/terraform/servers.tf \
      && (test -f infra/terraform/storage_box.tf || test -f infra/terraform/storage_box.tf.skipped) \
      && test -f infra/terraform/outputs.tf \
      && test -x infra/terraform/tf-wrap.sh \
      && grep -q 'skip_requesting_account_id\s*=\s*true' infra/terraform/backend.tf \
      && grep -q 'use_lockfile\s*=\s*true' infra/terraform/backend.tf \
      && grep -q 'variable\s\+"prod_server_type"' infra/terraform/variables.tf \
      && grep -q 'variable\s\+"prod_location"' infra/terraform/variables.tf \
      && grep -q 'variable\s\+"prod_image"' infra/terraform/variables.tf \
      && grep -q 'server_type\s*=\s*var\.prod_server_type' infra/terraform/servers.tf \
      && grep -q 'location\s*=\s*var\.prod_location' infra/terraform/servers.tf \
      && grep -q 'image\s*=\s*var\.prod_image' infra/terraform/servers.tf \
      && grep -q 'server_type\s*=\s*"cx42"' infra/terraform/servers.tf \
      && (grep -E 'port\s*=\s*"(4222|5432|6379|9000|80)"' infra/terraform/firewall.tf | grep -v '^[[:space:]]*#' | wc -l | grep -qE '^[[:space:]]*0$') \
      && grep -q 'lifecycle' infra/terraform/servers.tf \
      && grep -q 'ignore_changes' infra/terraform/servers.tf \
      && grep -q 'sops -d --output-type=dotenv' infra/terraform/tf-wrap.sh \
      && grep -q 'trap\|umask' infra/terraform/tf-wrap.sh \
      && cd infra/terraform && terraform fmt -check -recursive \
      && terraform validate -no-color 2>&1 | grep -qE "Success|Backend reinitialization" || true
    </automated>
  </verify>
  <acceptance_criteria>
    - Все 11 файлов существуют (10 .tf + 1 .sh + 1 .tfvars.example; storage_box.tf может быть переименован в .skipped per I1 escalation)
    - `terraform fmt -check` exit 0 (no formatting drift)
    - `backend.tf` содержит `skip_requesting_account_id = true` И `use_lockfile = true` И `force_path_style = true`
    - `variables.tf` содержит declarations для `prod_server_type` И `prod_location` И `prod_image` (B1 fix)
    - `servers.tf` для `hcloud_server.prod_app_01` ссылается на `var.prod_server_type` + `var.prod_location` + `var.prod_image` — НИ ОДНОГО литерала cx22/cx32/nbg1/fsn1/hel1/ubuntu-24.04 в prod_app_01 block (B1 fix — устраняет non-determinism)
    - `servers.tf` содержит `server_type = "cx42"` для sentry_01 (per RESEARCH correction D-11)
    - `firewall.tf` НЕ содержит ports 80/4222/5432/6379/9000 в `port = "..."` строках (filter out commented lines)
    - `servers.tf` содержит `lifecycle { ignore_changes = [...] }` для prod_app_01 (Pitfall 7 mitigation)
    - `tf-wrap.sh` executable, использует SOPS decrypt + umask 077 + trap shred pattern
  </acceptance_criteria>
  <done>
    Terraform HCL scaffold готов. `tf-wrap.sh` decrypt'ит credentials из SOPS без landing'а на disk. Все 4 skip-флага в backend.tf — иначе init fails. Sentry-01 = cx42 per RESEARCH correction. prod_app_01 attrs читаются из `imported.auto.tfvars` (B1 fix). Ничего ещё НЕ apply'ено.
  </done>
</task>

<task type="auto">
  <name>Task 3: terraform init + import prod-app-01 + reconcile drift + apply</name>
  <files>
    infra/terraform/terraform.tfvars
  </files>
  <read_first>
    .planning/phases/03-infrastructure-as-code/03-RESEARCH.md (lines 421-476 — Pattern 4 import sequence + Pitfall 10 sequencing rule),
    .planning/phases/03-infrastructure-as-code/03-PATTERNS.md (lines 116-148 — servers.tf stub + Pitfall 10 reminder),
    infra/terraform/imported.auto.tfvars (created в Task 1 user-action — auto-loaded by terraform; источник правды для prod_*)
  </read_first>
  <action>
    **HCL first (Task 2 done), import second, apply last** — per RESEARCH Pitfall 10.

    1. Создать локальный `infra/terraform/terraform.tfvars` (НЕ commit'ить — .gitignore покрывает) с реальными значениями НЕ-prod variables; prod_* живут в `imported.auto.tfvars` (Task 1 — не дублировать):
       ```
       dev_admin_ips = ["YOUR_WORKSTATION_IP/32"]  # Реальный /32 dev workstation IP (curl ifconfig.me)
       dev_ssh_pubkeys = {
         dev_a = "<реальный ssh pubkey из ~/.ssh/id_ed25519.pub>"
         # dev_b пока не указываем — Phase 2 follow-up #5, Plan 03-02 Task X добавит позже
       }
       ```

       Verify `imported.auto.tfvars` существует и auto-load работает: `terraform console <<< 'var.prod_server_id'` (после init) показывает значение.

    2. Запустить `./tf-wrap.sh init` (TF_ENV=prod is default):
       - Подключение к Hetzner Object Storage bucket `running-ecosystem-tfstate` (создан в Task 1 user action).
       - Download `hetznercloud/hcloud` provider ~> 1.62.
       - Verify init success — exit 0; output содержит "Terraform has been successfully initialized".
       - **If init виснет** на "Retrieving AWS account details" — Pitfall 2 не закрыт; проверить `skip_requesting_account_id = true` в backend.tf.

    3. Запустить `./tf-wrap.sh plan -out=plan.sanity.tfplan` — sanity check:
       - Должен показать "5 to add" (staging-app-01 + sentry-01 + 2 firewalls + storage_box) + "1 to add" для prod-app-01 (потому что import ещё не сделан).
       - **НЕ apply'им** этот plan.

    4. Запустить `./tf-wrap.sh import hcloud_server.prod_app_01 "$(grep prod_server_id imported.auto.tfvars | cut -d'"' -f2)"`:
       - Import читает state из Hetzner API и пишет в `running-ecosystem-tfstate/infra/prod.tfstate` через S3 backend.
       - Verify success: output содержит "Import successful!".

    5. Запустить `./tf-wrap.sh plan` повторно:
       - **B1 fix expectation:** поскольку `prod_server_type`/`prod_location`/`prod_image` уже выставлены в `imported.auto.tfvars` к точным значениям из Hetzner Console, plan should НЕ показывать drift на этих attributes. Возможный drift только на: labels (наши TF labels отсутствуют на existing server) и ssh_keys / firewall_ids (attached firewalls).
       - **MUST NOT** показывать "1 to destroy, 1 to add" для prod_app_01 — если показывает, executor STOPS и проверяет imported.auto.tfvars (B1: значения должны точно совпадать с Hetzner Console). NEVER apply destroy+recreate на prod.
       - **Reconcile:** если drift остаётся на labels/ssh_keys — это in-place updates, OK. Если на server_type/location/image — fix `imported.auto.tfvars`, повторить `terraform plan`. Если ssh_keys drift невыполнима — расширить `lifecycle.ignore_changes`.

    6. Когда `plan` показывает только in-place updates (labels) или 0 changes для prod_app_01 + "5 to add" для новых ресурсов:
       ```
       ./tf-wrap.sh apply
       ```
       Подтвердить `yes`. Apply должен создать: staging-app-01, sentry-01, hcloud_firewall.app, hcloud_firewall.sentry, hcloud_storage_box.pgbackrest (если provider support), hcloud_ssh_key.devs[<each dev>]. prod-app-01 либо без изменений, либо in-place label update (no recreate).

    7. Verify state:
       ```
       ./tf-wrap.sh state list
       ```
       Должен показать минимум 6 ресурсов: hcloud_server.prod_app_01, .staging_app_01, .sentry_01, hcloud_firewall.app, .sentry, hcloud_storage_box.pgbackrest (если provider support), + N × hcloud_ssh_key.devs.

    8. Verify outputs:
       ```
       ./tf-wrap.sh output staging_app_ipv4
       ./tf-wrap.sh output sentry_ipv4
       ./tf-wrap.sh output prod_app_ipv4   # должен = 148.253.214.156
       ```
  </action>
  <verify>
    <automated>
      cd infra/terraform && \
      ./tf-wrap.sh state list 2>&1 | grep -qE "hcloud_server\.prod_app_01" \
      && ./tf-wrap.sh state list 2>&1 | grep -qE "hcloud_server\.staging_app_01" \
      && ./tf-wrap.sh state list 2>&1 | grep -qE "hcloud_server\.sentry_01" \
      && ./tf-wrap.sh state list 2>&1 | grep -qE "hcloud_firewall\.app" \
      && ./tf-wrap.sh output -raw prod_app_ipv4 2>&1 | grep -q "148.253.214.156" \
      && ./tf-wrap.sh plan -detailed-exitcode 2>&1; test $? -eq 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `terraform init` succeeded против Hetzner Object Storage backend
    - `terraform state list` показывает prod_app_01, staging_app_01, sentry_01, оба firewalls, ssh keys (storage_box если provider support — иначе I1 escalation triggered и plan execution aborted)
    - `terraform output prod_app_ipv4` = `148.253.214.156` (import корректен)
    - `terraform plan -detailed-exitcode` = 0 (no diff after apply — состояние reconcile'но; B1 fix: prod_server_type/location/image из imported.auto.tfvars совпали с реальностью с первой итерации plan)
    - В Hetzner Console: новые VPS staging-app-01 и sentry-01 видимы; sentry-01 = cx42; prod-app-01 НЕ был пересоздан (uptime preserved)
  </acceptance_criteria>
  <done>
    INFRA-02 (Terraform управляет Hetzner Cloud), INFRA-04 (state в Object Storage с locking — если I1 не triggered), INFRA-05 (firewall rules explicit) — все три закрыты на уровне infra plane. Wave 2 (Ansible scaffold) разблокирован — может консумировать `tf output -raw <ipv4>` для inventory generation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation → Hetzner API | HCLOUD_TOKEN crosses TLS; токен grant'ит read+write на все ресурсы проекта |
| dev workstation → Hetzner Object Storage | AWS S3 creds; bucket содержит plaintext Terraform state (IPs, server IDs, маловероятно secrets — Hetzner Object Storage не хранит конфиденциальное per dialect) |
| Terraform state file → repo (must NOT cross) | State содержит cloud resource IDs + IPv4 — компрометация = roadmap для атакующего |
| Hetzner Cloud Firewall → public internet | 443 + 22 (dev IPs) единственные открытые порты |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Terraform state file | HIGH | mitigate | State лежит в Hetzner Object Storage private bucket с auth через AWS S3 creds; никогда не в git (`.gitignore` Task 0). `terraform.tfvars` + `imported.auto.tfvars` не commit'ятся. |
| T-03-02 | Tampering | HCLOUD_TOKEN leaked | HIGH | mitigate | Токен в SOPS-encrypted `.secrets/<env>/hetzner.yaml`; never в terraform.tfvars или git. `tf-wrap.sh` decrypt'ит в process env через `eval`, не на disk (umask 077 + tmpfs не нужны т.к. это process env, не файл). Если токен попадает в log — `sensitive = true` атрибут в variables.tf. |
| T-03-03 | Denial of Service | Open 22 from 0.0.0.0/0 | MEDIUM | mitigate | hcloud_firewall.app rule 22/TCP `source_ips = var.dev_admin_ips` (только /32 dev workstations). |
| T-03-04 | Tampering | TF state corruption from concurrent apply | MEDIUM | mitigate | `use_lockfile = true` в backend.tf — S3-native locking через If-None-Match conditional writes (Terraform 1.11+). |
| T-03-05 | Elevation of Privilege | prod-app-01 пересоздан вместо import → production downtime | HIGH | mitigate | HCL first → sanity plan → import → reconcile drift → clean plan → apply (RESEARCH Pitfall 10 sequence enforced в Task 3 action). `lifecycle { ignore_changes = [image, user_data, ssh_keys] }` для prod_app_01. **B1 fix:** prod_server_type/location/image сходят из `imported.auto.tfvars` (Task 1 user-action via `hcloud server describe`), не выбираются executor'ом литерально — устранён risk выбора неправильного литерала, который trigger'нул бы destroy+recreate. |
| T-03-06 | Repudiation | Хранение TF state в repo (auditing impossible если кто-то force-push'ит) | LOW | accept_risk | Mitigate Object Storage уже покрывает; repo-side гарантия — `.gitignore` Task 0 + Phase 2 pre-commit hooks из 02-03 (которые сканируют secrets). Дополнительная мера не нужна для closed-beta. |

**block_on:** high
**HIGH-severity threats:** T-03-01 (mitigate), T-03-02 (mitigate), T-03-05 (mitigate) — все mitigated, не accepted.
</threat_model>

<verification>
- Wave 0 tooling: `terraform version` >= 1.11; `ansible --version` >= 2.16; 3 collections установлены.
- `.gitignore` патчи: 5 Terraform-artifact паттернов (включая `imported.auto.tfvars` — B1 fix), `.terraform.lock.hcl` commitable.
- 3 SOPS slots `.secrets/{dev,staging,prod}/hetzner.yaml` существуют, decrypt'ятся.
- Hetzner Console reality check (user-action Task 1): API token + Object Storage bucket + S3 credentials созданы; реальные значения вставлены в SOPS slots; `imported.auto.tfvars` заполнен через `hcloud server describe ... | jq` (B1).
- Terraform scaffold: 11 .tf файлов + tf-wrap.sh + terraform.tfvars.example existable + valid (`terraform fmt -check && terraform validate`).
- variables.tf содержит declarations для `prod_server_type`/`prod_location`/`prod_image` (B1).
- servers.tf prod_app_01 ссылается на `var.prod_*` — никаких литералов cx22/cx32/nbg1/fsn1/ubuntu-24.04 в этом block (B1).
- `terraform init` против Hetzner Object Storage backend succeeded (use_lockfile + skip_requesting_account_id).
- `terraform import hcloud_server.prod_app_01 <id>` succeeded.
- `terraform plan` после import + reconcile = 0 diff (clean); никогда не показывает destroy+recreate prod (B1).
- `terraform apply` создал staging-app-01 + sentry-01 (cx42 per RESEARCH correction) + Storage Box (или I1 escalation triggered) + 2 firewalls БЕЗ пересоздания prod-app-01.
- Manual: `nmap -p 22,80,443,4222,5432,6379,9000 <staging-app-01-ipv4>` показывает только 443 открытый из public (per VALIDATION manual check INFRA-05) — отложено до Wave 2 когда есть staging app stack running на 443; Wave 1 апликабельная проверка: `hcloud_firewall.app.rule` count = 2 (443 + 22).
</verification>

<success_criteria>
- INFRA-02: `infra/terraform/` управляет 3 Hetzner Cloud VPS (prod-app-01 imported + staging-app-01 + sentry-01) + 2 firewalls + Storage Box (BX10; или I1 escalation explicit если provider missing).
- INFRA-04: Terraform state в Hetzner Object Storage (`running-ecosystem-tfstate` bucket в fsn1 region) с `use_lockfile = true`. State **никогда** не commit'ится в repo.
- INFRA-05: Explicit firewall rules в hcloud_firewall.app + hcloud_firewall.sentry — только 443/TCP from `0.0.0.0/0` + 22/TCP from `var.dev_admin_ips`. NATS 4222 / Postgres 5432 / Redis 6379 / MinIO 9000 — internal-only (verified in compose + RESEARCH §D-07).
- prod-app-01 (148.253.214.156) сохранил uptime — НЕ пересоздан (B1 fix prevents non-deterministic literal-driven destroy).
- Wave 2 разблокирован: `./tf-wrap.sh output -raw staging_app_ipv4` дает IP для Ansible inventory.
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-as-code/03-01-SUMMARY.md` per template.

**Required SUMMARY content:**
- Wave 0 tooling install confirmations (versions captured)
- Hetzner Console user-action output (server_id, server_type, location, image для prod-app-01; bucket region for Object Storage) — content of `imported.auto.tfvars` (B1)
- `terraform state list` output после apply
- `terraform output` всех 3 IPv4 + storage_box_subuser (если provider support — иначе note I1 escalation)
- Drift reconciliation notes (B1 fix: должно быть тривиальным или 0; если был — что именно отличалось от Hetzner Console)
- Phase 2 SOPS slot pattern confirmation (`.sops.yaml` regex auto-covered hetzner.yaml, NO config change required)
- Decisions cited: D-01 (Ansible+Terraform split), D-02 (Hetzner topology), D-04 (Storage Box for Phase 7 pgBackRest, NOT TF state), D-05 (Object Storage backend), D-06/D-07/D-08 (firewall rules), D-11 corrected (cx42 not cx32 for sentry-01), D-18 (sslip.io DNS, deferred to Wave 2)
- B1 / I1 closure evidence (no literal cx22/cx32/nbg1 в prod_app_01 servers.tf block; storage_box state present or escalation file written)
- Carry-forward для Wave 2: terraform output IPs нужны для Ansible inventory generation
</output>
</output>
