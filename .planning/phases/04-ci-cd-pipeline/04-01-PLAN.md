---
plan_id: 04-01
phase: 4
phase_slug: ci-cd-pipeline
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
requirements: [CICD-01]
autonomous: false
estimated_duration: "20-30 min (включая GH-side user-action)"
tags: [github, repo-setup, ghcr, prerequisite]
---

# Plan 04-01 — GitHub repo + remote + GHCR namespace bootstrap

<objective>
Создать GitHub remote под namespace `runningecosystem/sport` (или alt namespace + Claude обновляет D-02 refs), запушить `feat/cursona-redesign` + создать `main`, и удалить устаревший Phase 0 placeholder `.github/workflows/ci.yml`. Это разблокирует все остальные Phase 4 plans — без origin remote и first push в GitHub Actions tab никакой CI не сможет запуститься.

Дополнительно: после first push (CD ещё не существует — backend-cd.yml landing в Plan 04-03a) выполнить user-action настройки GHCR package visibility = **public** per RESEARCH §Open Q 1 + Architectural Responsibility Map (Go services не содержат proprietary algorithms, исключаем GHCR auth complexity на prod VPS).

Purpose: hard prerequisite для CICD-01 — без GitHub repo нечего CI'ить.
Output: Working `origin` remote at `git@github.com:runningecosystem/sport.git` (или alt namespace), pushed branches `feat/cursona-redesign` + `main`, deleted placeholder `ci.yml`, GHCR namespace ownership documented в SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ci-cd-pipeline/04-CONTEXT.md
@.planning/phases/04-ci-cd-pipeline/04-RESEARCH.md
@.planning/phases/04-ci-cd-pipeline/04-PATTERNS.md
@.planning/phases/04-ci-cd-pipeline/04-VALIDATION.md
@.github/workflows/ci.yml
@.github/workflows/backend-ci.yml

<interfaces>
<!-- Phase 4 cross-cutting identifiers — all subsequent plans depend on these -->

**GHCR namespace (D-02):** `ghcr.io/runningecosystem/<service>` — already hardcoded в `.github/workflows/backend-ci.yml` line 86. ALL subsequent plans (04-02 docker-build matrix, 04-03a CD publish job, 04-03b Makefile, 04-05 branch protection script) reference this exact namespace. Если user picks alt namespace, executor MUST sed-replace во всех workflow + compose + scripts перед commit.

**Repo URL (D-03):** `github.com/runningecosystem/sport` — recommended; user may pick alternative under same `runningecosystem` namespace OR personal account. Documented в SUMMARY.

**Default branch:** `main` — per CICD-06 branch protection target. Must exist before Plan 04-05 runs.

**Active branch:** `feat/cursona-redesign` IS the v1.0 dev line (per STATE.md §Current Position). Push it AS-IS; do not merge to main yet (Phase 21 E2E acceptance handles `v1.0-rc.1` tag flow).

**Package visibility:** **public** (per RESEARCH §Open Q recommendation + Architectural Responsibility Map). User toggles after first CD push lands an image (which happens in Plan 04-03a, not here — pre-flag during Plan 04-01 Task 0 so it's ready по time first image pushes).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: USER ACTION — Create GitHub repo + add origin remote + push</name>
  <files>(none modified by Claude — this is human-only via `gh` CLI)</files>
  <read_first>
    - `.planning/phases/04-ci-cd-pipeline/04-CONTEXT.md` §decisions D-02/D-03
    - `.planning/phases/04-ci-cd-pipeline/04-RESEARCH.md` §Environment Availability (line 1240 — "GitHub remote / origin — entire phase blocked")
  </read_first>
  <what-built>Empty placeholder — Plan 04-01 cannot proceed without GitHub remote.</what-built>
  <how-to-verify>
    1. Verify `gh` CLI installed + authenticated: `gh --version && gh auth status`. Если not: `brew install gh && gh auth login` (web flow; pick HTTPS; scope `repo,write:packages,workflow`).
    2. Verify current state: `git remote -v` returns empty (no origin yet).
    3. Создать private repo + добавить origin + push текущую ветку:
       ```bash
       cd /Users/ismail/Desktop/projects/sport
       gh repo create runningecosystem/sport --private --source=. --remote=origin --push
       # ↑ Creates github.com/runningecosystem/sport, adds origin remote pointing at it,
       # pushes the CURRENT branch (feat/cursona-redesign) tracking origin/feat/cursona-redesign.
       ```
       Если namespace `runningecosystem` уже занят OR user prefers personal account: substitute (e.g. `<your-gh-username>/sport`) AND TELL Claude — executor must sed-update all `ghcr.io/runningecosystem/` references в `.github/workflows/backend-ci.yml` + docker-compose.prod.yml + Plan 04-03a/04-03b/04-05 plan bodies before those plans run.
    4. Создать `main` branch на initial commit (since CI matches `branches: [main]` triggers):
       ```bash
       # Locally branch off feat/cursona-redesign HEAD, push к origin
       git branch main
       git push -u origin main
       ```
    5. Verify both branches landed:
       ```bash
       gh repo view runningecosystem/sport --json visibility,defaultBranchRef,nameWithOwner
       # Expected: {"visibility":"PRIVATE","defaultBranchRef":{"name":"main"},"nameWithOwner":"runningecosystem/sport"}
       git remote -v
       # Expected: origin git@github.com:runningecosystem/sport.git (fetch + push)
       ```
    6. **Pre-flag GHCR package visibility = public** (per RESEARCH §Open Q recommendation). Cannot be set BEFORE first image push, so это — reminder + bookmark to come back после Plan 04-03a's first CD run:
       ```bash
       # Bookmark for after Plan 04-03a first publish:
       # gh api -X PATCH /user/packages/container/<service-name>/visibility -f visibility=public
       # — Repeat для each of 8 services after first publish lands them in GHCR.
       ```
       Document chosen namespace в SUMMARY along with confirmation that GHCR visibility plan = public.
  </how-to-verify>
  <acceptance_criteria>
    - `git remote -v | grep origin` returns 2 lines (fetch + push) pointing at `runningecosystem/sport` (or alt namespace declared by user)
    - `gh repo view --json visibility,defaultBranchRef,nameWithOwner` returns valid JSON с `defaultBranchRef.name == "main"` AND `visibility == "PRIVATE"`
    - `git ls-remote --heads origin | awk '{print $2}' | sort` includes both `refs/heads/main` AND `refs/heads/feat/cursona-redesign`
    - GitHub Actions tab visible (https://github.com/runningecosystem/sport/actions) — first push may have triggered partial `backend-ci.yml` runs (some may fail because Plan 04-02 hasn't fixed matrix yet — that's expected; acceptance is "repo exists + Actions tab populated", NOT "everything green").
  </acceptance_criteria>
  <resume-signal>Type "approved — namespace=runningecosystem/sport" OR "approved — namespace=&lt;alt&gt; → please update D-02 refs in [list of files]"</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Delete Phase 0 placeholder `.github/workflows/ci.yml`</name>
  <files>.github/workflows/ci.yml (DELETE)</files>
  <read_first>
    - `.github/workflows/ci.yml` (29-line noop placeholder)
    - `.planning/phases/04-ci-cd-pipeline/04-CONTEXT.md` §scout_findings — confirms ci.yml is Phase 0 placeholder superseded by backend-ci.yml
    - `.planning/phases/04-ci-cd-pipeline/04-PATTERNS.md` §File Classification — `.github/workflows/ci.yml` (DELETE) classification row
  </read_first>
  <action>
    Remove the placeholder workflow file. `ci.yml` (created Phase 0 / May 6) is a noop that только echoes TODOs — superseded by `backend-ci.yml`. Leaving it in tree creates a phantom "CI" check in PR UI that always passes vacuously, which dilutes branch protection signal value (per D-19).

    Use `git rm` (not Bash `rm`) so deletion is staged:
    ```bash
    git rm .github/workflows/ci.yml
    ```

    Commit message: `chore(ci): remove Phase 0 placeholder ci.yml, superseded by backend-ci.yml (CICD-01 prereq)`.
  </action>
  <verify>
    <automated>test ! -f .github/workflows/ci.yml &amp;&amp; git status --porcelain .github/workflows/ci.yml | grep -q '^D' &amp;&amp; echo "DELETED+STAGED"</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/ci.yml` no longer exists в working tree
    - Deletion staged in git (`git diff --cached --name-status` shows `D  .github/workflows/ci.yml`)
    - Single commit landed с deletion + matching commit message
    - Pre-existing `backend-ci.yml` untouched (`git status` shows no other changes от этого task)
  </acceptance_criteria>
  <done>Placeholder deleted, committed, and `backend-ci.yml` remains как единственный CI workflow до Plan 04-02 extension.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation → github.com | First push exposes whole git history к GitHub-side scanning (if visibility public would also expose к world). Initial visibility = PRIVATE per D-03 mitigates the latter. |
| GitHub repo → GHCR package storage | Future image publishes (Plan 04-03a) inherit repo visibility unless explicitly overridden. Documented as user-action to set visibility=public для closed-beta. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information disclosure | GitHub repo visibility | mitigate | Initial `gh repo create --private` — repo opens private. Public-flip к GHCR happens only после first image lands, и applies only к package metadata, не к source code (source remains private). Documented в SUMMARY. |
| T-04-PIV-NS | Tampering | GHCR namespace mismatch | mitigate | Task 0 verification step requires `gh repo view --json nameWithOwner` matches expected namespace OR user-declared alternative. Если mismatch, executor sed-updates ALL `ghcr.io/runningecosystem/` literals в codebase BEFORE Plan 04-02 begins. Prevents silent push к wrong namespace. |

**Block-on-high gate:** No HIGH-severity threats в Plan 04-01. Both threats above are MEDIUM, mitigated. `block_on: high` satisfied.
</threat_model>

<must_haves>
  truths:
    - "D-02: GHCR namespace `ghcr.io/runningecosystem/<service>` preserved (or sed-updated к alt + documented в SUMMARY)"
    - "D-03: GitHub repo created at `runningecosystem/sport` (or alt namespace), initial visibility=PRIVATE, `origin` remote points at it"
    - "D-21 [informational]: branch protection setup deferred к Plan 04-05; first green CI run from Plan 04-02 is the prerequisite per RESEARCH Pitfall 1"
    - "RESEARCH §Open Q 1: GHCR namespace ownership choice (personal vs org) documented in SUMMARY — affects Plan 04-02 gitleaks-action license requirement (org repos = GITLEAKS_LICENSE secret needed)"
    - "RESEARCH §Open Q 5: GHCR package visibility = public (recommended for v1.0 closed-beta — Go services contain no proprietary algorithms per Architectural Responsibility Map A2)"
  behaviors:
    - "`git remote -v` returns origin pointing at GHCR-namespace GitHub repo"
    - "`gh repo view --json visibility,defaultBranchRef` returns visibility=PRIVATE + defaultBranchRef=main"
    - "Both branches `main` AND `feat/cursona-redesign` exist on origin (`git ls-remote --heads origin`)"
    - "Phase 0 `.github/workflows/ci.yml` no longer present in tree"
    - "GitHub Actions tab loads (proves repo + workflows reachable for Plan 04-02)"
  forbidden:
    - "No public repo visibility at creation time (visibility=PRIVATE per D-03; only GHCR PACKAGE visibility flips к public later)"
    - "No alternative `ghcr.io/...` namespace silently committed без updating D-02 references in subsequent plans"
    - "No deletion of `backend-ci.yml` (it's the SEED для Plan 04-02 EXTEND)"
    - "No branch protection setup attempted в this plan (sequence guard — must wait для Plan 04-05 after first green CI run)"
</must_haves>

<verification>
- `gh repo view --json visibility,nameWithOwner,defaultBranchRef | jq '.visibility == "PRIVATE" and .defaultBranchRef.name == "main"'` returns `true`
- `test ! -f .github/workflows/ci.yml` exits 0
- `test -f .github/workflows/backend-ci.yml` exits 0 (seed for Plan 04-02 preserved)
- Single commit landed in this plan: `chore(ci): remove Phase 0 placeholder ci.yml ...`
- SUMMARY documents: chosen GHCR namespace, confirmed visibility=PRIVATE (repo) + planned visibility=PUBLIC (GHCR packages, after Plan 04-03a first publish), gitleaks-action license requirement (yes/no depending on personal vs org).
</verification>

<success_criteria>
1. GitHub repo exists at `github.com/<chosen-namespace>/sport` (или equivalent), private visibility, default branch `main`.
2. `origin` remote configured on dev workstation, pushes both `main` + `feat/cursona-redesign`.
3. `.github/workflows/ci.yml` deleted + committed.
4. SUMMARY documents D-02 namespace choice + Plan 04-02 ready to extend `backend-ci.yml`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ci-cd-pipeline/04-01-SUMMARY.md` recording:
- Final chosen GHCR namespace + whether ownership = personal account or organization (affects Plan 04-02 GITLEAKS_LICENSE decision)
- `git remote -v` output verbatim
- `gh repo view --json visibility,nameWithOwner,defaultBranchRef` JSON output verbatim
- First GitHub Actions tab URL (so subsequent plans can reference it)
- Confirmation that pre-existing `backend-ci.yml` triggered on first push (success/fail status — both acceptable; informational only)
- Bookmark reminder: after Plan 04-03a's first CD run lands images in GHCR, run `gh api -X PATCH /user/packages/container/<svc>/visibility -f visibility=public` for each of 8 services.
</output>
