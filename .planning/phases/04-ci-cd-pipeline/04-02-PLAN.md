---
plan_id: 04-02
phase: 4
phase_slug: ci-cd-pipeline
wave: 2
depends_on: [04-01]
files_modified:
  - .github/workflows/backend-ci.yml
  - .github/workflows/secret-scan-full.yml
  - .golangci.yml
  - .trivyignore.yaml
requirements: [CICD-01, SEC-01]
autonomous: false
estimated_duration: "60-120 min (включая итерации после CI failures)"
tags: [ci, golangci-lint, gosec, semgrep, govulncheck, trivy, gitleaks, trufflehog, sec-01-closure]
---

# Plan 04-02 — Extend backend-ci.yml: 8-service matrix + lint + 5 scanners + nightly cron secret scan

<objective>
Поднять `.github/workflows/backend-ci.yml` от существующего partial (test on 2 of 8 services, vet only, docker-build verify on 2 services) до production-grade CI matrix per ROADMAP success criterion #1 + CICD-01 + RESEARCH §Code Examples §A:

- Test matrix expands от `[pkg, identity, activity-sync]` к все 9 entries (8 services + pkg)
- Replace `go vet` job-step с `golangci-lint v2` (separate `lint` job, blocks merge на HIGH severity)
- Add 4 SAST/vuln scanner jobs: `gosec`, `semgrep`, `govulncheck`, plus `secrets-scan-diff` (gitleaks + trufflehog, PR-diff only)
- Add `docker-build` matrix job covering все 8 services (currently 2) с `load: true` для Trivy scan downstream
- Add `trivy` image-scan step inside docker-build job (blocks merge на HIGH+CRITICAL per D-10; uses `.trivyignore.yaml` for documented exceptions per RESEARCH §Open Q 2)
- Add `no-latest-tag-guard` job (negative-grep against `:latest` literal в workflows + compose per RESEARCH §Open Q 3)
- Add `permissions:` top-level block (`contents: read` + `security-events: write` для SARIF upload)
- NEW `.github/workflows/secret-scan-full.yml` — Sunday-03:00-UTC cron full-history scan (gitleaks + trufflehog с `fetch-depth: 0`) — split per RESEARCH Pitfall 10 (don't run full-history on every PR — slow)
- NEW `.golangci.yml` — golangci-lint v2 conservative config (RESEARCH §Code Examples §D verbatim, tune после first run)
- NEW `.trivyignore.yaml` — empty initially; format documented для future entries с rationale-per-CVE + expiry (RESEARCH §Code Examples §I)

Iterate jobs until ALL green на test PR branch — RESEARCH Pitfall 1 sequence requires successful CI run BEFORE Plan 04-05 branch protection lock-down. **Closes SEC-01** (Phase 2 left CI invocation deferred to Phase 4; this plan wires gitleaks + trufflehog into ongoing CI).

Purpose: deliver CICD-01 acceptance + close SEC-01 partial from Phase 2.
Output: 4 new/extended files + green CI run on scratch PR + `[VERIFIED]` annotation in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ci-cd-pipeline/04-CONTEXT.md
@.planning/phases/04-ci-cd-pipeline/04-RESEARCH.md
@.planning/phases/04-ci-cd-pipeline/04-PATTERNS.md
@.planning/phases/04-ci-cd-pipeline/04-VALIDATION.md
@.planning/phases/04-ci-cd-pipeline/04-01-SUMMARY.md
@.github/workflows/backend-ci.yml
@services/backend/Makefile
@services/backend/identity/Dockerfile

<interfaces>
<!-- Identifiers required by ALL scanner + lint jobs — taken from RESEARCH §Code Examples §A and verified pin versions §Standard Stack -->

**Action versions (RESEARCH §State of the Art — verified 2026-05-17, valid until 2026-06-17):**
- `actions/checkout@v4`
- `actions/setup-go@v5` с `go-version: "1.25"`
- `golangci/golangci-lint-action@v8` с `version: v2.x`
- `golang/govulncheck-action@v1`
- `gitleaks/gitleaks-action@v2` (env `GITLEAKS_LICENSE` — OPTIONAL для personal account; REQUIRED для organization repo per RESEARCH Pitfall 4 + §Open Q 1)
- `trufflesecurity/trufflehog@main` (no version pin per upstream docs)
- `aquasecurity/trivy-action@master` (no version pin per upstream docs)
- `docker/setup-buildx-action@v3` + `docker/build-push-action@v5`
- `github/codeql-action/upload-sarif@v3`
- `returntocorp/semgrep` (container image; bypasses action-version pinning)

**Service matrix (D-08 — verified from `ls services/backend/`):** `[pkg, identity, activity-sync, feed, media, messaging, notifications, realtime-gw, social-graph]` for test job. Docker-build job omits `pkg` (no Dockerfile — shared library only). 8 docker-build entries, 9 test entries.

**`paths:` filter (D-09):** `services/backend/**`, `.github/workflows/backend-ci.yml`, `.github/workflows/secret-scan-full.yml`, `Makefile` (top-level — will exist after Plan 04-03b), `.golangci.yml`, `.trivyignore.yaml`. Workflows do NOT run on docs/planning/mobile changes.

**Trivy block-severity (D-10 + RESEARCH §Open Q 2):** `severity: HIGH,CRITICAL` + `ignore-unfixed: false` + `exit-code: 1`. Forces every CRITICAL — даже без upstream fix — into `.trivyignore.yaml` с rationale + 90-day expiry.

**Lint config path:** `.golangci.yml` AT REPO ROOT (not `services/backend/.golangci.yml` — `golangci-lint-action@v8` walks up from `working-directory` to find config). Verified RESEARCH §Code Examples §D.

**Cron schedule (RESEARCH §Open Q 3 timezone clarification + Pitfall 9):** `cron: "0 3 * * 0"` (Sunday 03:00 UTC = 06:00 Moscow MSK in winter / 06:00 MSK summer — MSK is UTC+3 year-round per Russian Federation policy since 2014, so timezone-stable).

**8 verbatim job `name:` strings — PINNED CONTRACT consumed by Plan 04-05 branch-protection JSON:**
```
Test (Go 1.25)
Lint (golangci-lint v2)
SAST (gosec)
Vuln (govulncheck)
SAST (semgrep)
Secrets (gitleaks + trufflehog — PR diff)
Docker build (no push, verify)
Guard (no :latest)
```
Executor MUST NOT vary spelling, casing, punctuation, OR em-dash glyph. Any drift → branch protection (Plan 04-05) waits forever на nonexistent check context → `T-04-LOCKOUT` HIGH realized.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EXTEND `.github/workflows/backend-ci.yml` — 8-service matrix + lint + 4 scanner jobs + docker-build с Trivy + `no-latest-tag-guard`</name>
  <files>.github/workflows/backend-ci.yml</files>
  <read_first>
    - `.github/workflows/backend-ci.yml` (existing 89-line partial)
    - `.planning/phases/04-ci-cd-pipeline/04-RESEARCH.md` §Code Examples §A lines 521-700 (full workflow body verbatim)
    - `.planning/phases/04-ci-cd-pipeline/04-PATTERNS.md` Pattern A (BuildKit GHA cache), Pattern B (job permissions), Pattern C (matrix entry per service), Pattern D (SARIF upload composition)
    - `services/backend/identity/Dockerfile` (Dockerfile structure all 8 services follow)
  </read_first>
  <action>
    Replace `.github/workflows/backend-ci.yml` contents с extended version per RESEARCH §Code Examples §A. **CRITICAL:** Each of the 8 jobs MUST use the verbatim `name:` strings pinned в `<interfaces>` block above — these are the contract consumed by Plan 04-05 branch protection. Concrete changes от existing:

    1. **Triggers (`on:` block):** add `Makefile`, `.golangci.yml`, `.trivyignore.yaml`, `.github/workflows/secret-scan-full.yml` к `paths:` filter (lists in BOTH `push` AND `pull_request` blocks). Keep `branches: [main]` filter.

    2. **Top-level `permissions:` block** — add (currently missing — Pattern B):
       ```yaml
       permissions:
         contents: read
         security-events: write   # SARIF upload к Security tab
       ```

    3. **`test` job — matrix expand:** replace existing `defaults.run.working-directory: services/backend` + 3-service hardcoded sequence (lines 17-57) с matrix-strategy job. **Job `name:` MUST be verbatim literal `Test (Go 1.25)`** — DO NOT use `name: Test (Go ${{ matrix.go-version }})` template (matrix has no `go-version` dimension; template would interpolate empty → literal `Test (Go )` → breaks branch-protection contract). Matrix entries `[pkg, identity, activity-sync, feed, media, messaging, notifications, realtime-gw, social-graph]`. Each entry: `working-directory: services/backend/${{ matrix.service }}`; `cache-dependency-path: services/backend/${{ matrix.service }}/go.sum`; commands `go mod download && go test -race -coverprofile=coverage.out ./...`; coverage summary к `$GITHUB_STEP_SUMMARY`.

    4. **NEW `lint` job (name: `Lint (golangci-lint v2)`):** uses `golangci/golangci-lint-action@v8` с `version: v2.x` + `working-directory: services/backend`. Reads root `.golangci.yml` (Task 3 creates).

    5. **NEW `gosec` job (name: `SAST (gosec)`):** `go install github.com/securego/gosec/v2/cmd/gosec@latest` (within job — no dedicated action available); `gosec -fmt sarif -out gosec.sarif -severity high ./... || true` (`|| true` per Pattern D — fail downstream via SARIF, не in scanner); upload SARIF к Security tab.

    6. **NEW `govulncheck` job (name: `Vuln (govulncheck)`):** uses `golang/govulncheck-action@v1` с `go-version-input: "1.25"` + `work-dir: services/backend`. Exit non-zero on vulnerability — blocks merge per D-10.

    7. **NEW `semgrep` job (name: `SAST (semgrep)`):** runs `semgrep ci --config=p/golang --config=p/owasp-top-ten --sarif --output=semgrep.sarif --severity ERROR --severity WARNING || true` inside `returntocorp/semgrep` container; upload SARIF.

    8. **NEW `secrets-scan-diff` job (name: `Secrets (gitleaks + trufflehog — PR diff)` — verbatim, including em-dash `—` U+2014):** `if: github.event_name == 'pull_request'` (PR-only — saves CI minutes на push к main). Uses `actions/checkout@v4` с `fetch-depth: 0` (gitleaks needs commits); `gitleaks/gitleaks-action@v2` с `GITHUB_TOKEN`. Add commented `GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}` line с inline comment "Uncomment если namespace = organization (verify в 04-01-SUMMARY: ownership=personal → keep commented; ownership=organization → uncomment + add secret via `gh secret set GITLEAKS_LICENSE`)". Then trufflehog: `trufflesecurity/trufflehog@main` с `extra_args: --results=verified,unknown` (base/head auto-detected on PR).

    9. **EXTENDED `docker-build` job (name: `Docker build (no push, verify)`):** keep skeleton; extend matrix к 8 services (add feed, media, messaging, notifications, realtime-gw, social-graph); add `load: true` to `docker/build-push-action@v5` (loads built image к local Docker для Trivy); add per-service `cache-from: type=gha,scope=${{ matrix.service }}` + `cache-to: type=gha,mode=max,scope=${{ matrix.service }}` (per-service scope avoids cross-service eviction).

       **CRITICAL — `ci-` tag prefix:** The build-step `tags:` field MUST emit `ghcr.io/ismaill01/${{ matrix.service }}:ci-${{ github.sha }}` (with explicit `ci-` prefix — distinguishes CI verify-builds от CD-published images per Plan 04-03a hybrid mode). Update existing build-step `tags:` line from `:${{ github.sha }}` к `:ci-${{ github.sha }}` so the downstream Trivy step finds the image it scans.

       Then add Trivy step after build:
       ```yaml
       - name: Trivy image scan
         uses: aquasecurity/trivy-action@master
         with:
           image-ref: ghcr.io/ismaill01/${{ matrix.service }}:ci-${{ github.sha }}
           format: sarif
           output: trivy-${{ matrix.service }}.sarif
           severity: HIGH,CRITICAL
           exit-code: 1
           ignore-unfixed: false
           trivyignores: .trivyignore.yaml
       - uses: github/codeql-action/upload-sarif@v3
         if: always()
         with:
           sarif_file: trivy-${{ matrix.service }}.sarif
           category: trivy-${{ matrix.service }}
       ```

    10. **NEW `no-latest-tag-guard` job (name: `Guard (no :latest)`):** standalone (RESEARCH §Open Q 3 recommendation — separate job for visibility):
        ```yaml
        no-latest-tag-guard:
          name: Guard (no :latest)
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - name: Negative grep — forbid `:latest`
              run: |
                # Strip comment lines first; grep -c counts comments otherwise (header prose self-invalidates per planner-antipatterns).
                if grep -v '^#' .github/workflows/*.yml services/backend/docker-compose.prod.yml \
                     | grep -E ':latest["\x27]?\s*$' ; then
                  echo "FAIL: \`:latest\` tag found (excluding comment lines) — ROADMAP hard rule violated"
                  exit 1
                fi
                echo "PASS — no :latest tags found in non-comment lines"
        ```

    Use Write tool to replace entire file contents. Do NOT use Bash heredoc.

    Commit message: `feat(ci): extend backend-ci.yml — 8-service matrix + 5 scanners + Trivy + :latest guard (CICD-01, SEC-01 closure)`.
  </action>
  <verify>
    <automated>actionlint .github/workflows/backend-ci.yml &amp;&amp; grep -q 'matrix:' .github/workflows/backend-ci.yml &amp;&amp; grep -q 'social-graph' .github/workflows/backend-ci.yml &amp;&amp; grep -q 'golangci-lint-action@v8' .github/workflows/backend-ci.yml &amp;&amp; grep -q 'govulncheck-action@v1' .github/workflows/backend-ci.yml &amp;&amp; grep -q 'gitleaks-action@v2' .github/workflows/backend-ci.yml &amp;&amp; grep -q 'trivy-action' .github/workflows/backend-ci.yml &amp;&amp; grep -q 'no-latest-tag-guard' .github/workflows/backend-ci.yml &amp;&amp; ! grep -q 'attest-build-provenance' .github/workflows/backend-ci.yml &amp;&amp; grep -qF 'tags: ghcr.io/ismaill01/${{ matrix.service }}:ci-${{ github.sha }}' .github/workflows/backend-ci.yml &amp;&amp; for NAME in "Test (Go 1.25)" "Lint (golangci-lint v2)" "SAST (gosec)" "Vuln (govulncheck)" "SAST (semgrep)" "Secrets (gitleaks + trufflehog — PR diff)" "Docker build (no push, verify)" "Guard (no :latest)"; do grep -qF "name: $NAME" .github/workflows/backend-ci.yml || { echo "FAIL: missing job name: $NAME"; exit 1; }; done &amp;&amp; echo "STRUCTURE OK — all 8 job names pinned, ci- tag present, attest-build-provenance correctly absent"</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/backend-ci.yml` exits 0 (no YAML/expression syntax errors)
    - Matrix entries grep'd: `grep -E 'service:\s*\[' .github/workflows/backend-ci.yml` returns line containing все 8 services + pkg
    - 5 distinct scanner jobs present: `grep -cE '^\s*(lint|gosec|govulncheck|semgrep|secrets-scan-diff):' .github/workflows/backend-ci.yml` returns 5
    - `no-latest-tag-guard` job exists + its grep step uses `grep -v '^#'` filter (prevents self-invalidation per critical_rules)
    - `permissions:` top-level block contains `security-events: write`
    - Per-service cache scope present: `grep -cE 'scope=\$\{\{ matrix\.service \}\}' .github/workflows/backend-ci.yml` returns ≥2 (build cache-from + cache-to)
    - **JOB-NAME CONTRACT PIN** — all 8 verbatim `name:` strings grep'd via:
      ```bash
      for NAME in "Test (Go 1.25)" "Lint (golangci-lint v2)" "SAST (gosec)" "Vuln (govulncheck)" "SAST (semgrep)" "Secrets (gitleaks + trufflehog — PR diff)" "Docker build (no push, verify)" "Guard (no :latest)"; do
        grep -qF "name: $NAME" .github/workflows/backend-ci.yml || { echo "FAIL: missing job name: $NAME"; exit 1; }
      done
      echo "✓ All 8 backend-ci job names pinned"
      ```
      Этот block is the upstream half of the contract consumed by Plan 04-05 (downstream pinning happens via setup-branch-protection.sh's JSON body + Task 1 cross-check loop там).
    - **`ci-` tag prefix verified:** `grep -qF 'tags: ghcr.io/ismaill01/${{ matrix.service }}:ci-${{ github.sha }}' .github/workflows/backend-ci.yml` exits 0 — Trivy image-ref will find the built image
    - **Negative-grep fail-closed:** `! grep -q 'attest-build-provenance' .github/workflows/backend-ci.yml` exits 0 — attest-build-provenance belongs к backend-cd.yml only; presence here would indicate accidental copy-paste from Plan 04-03a. Negative `!` inverts grep так that match (wrong) → exit 1 → fails as intended (W2 fix: previous formulation used `&& echo "warning" || echo "OK"` which fails open — silent pass on match).
  </acceptance_criteria>
  <done>backend-ci.yml replaces existing с 9-entry test matrix + 8-entry docker-build matrix + 5 scanner jobs + no-latest guard + security-events permissions block + 8 verbatim job names pinned + `:ci-${sha}` tag prefix on build step. actionlint clean.</done>
</task>

<task type="auto">
  <name>Task 2: CREATE `.github/workflows/secret-scan-full.yml` — Sunday-03:00-UTC cron full-history gitleaks + trufflehog scan</name>
  <files>.github/workflows/secret-scan-full.yml</files>
  <read_first>
    - `.planning/phases/04-ci-cd-pipeline/04-RESEARCH.md` §Code Examples §H lines 1069-1101 (full workflow body)
    - `.planning/phases/04-ci-cd-pipeline/04-RESEARCH.md` §Common Pitfalls 9 (cron timezone) + 10 (full-history scan separation)
  </read_first>
  <action>
    Создать new file `.github/workflows/secret-scan-full.yml` per RESEARCH §Code Examples §H verbatim. Key elements:

    - `on.schedule.cron: "0 3 * * 0"` (Sundays 03:00 UTC = 06:00 MSK — low-traffic window per RESEARCH §Open Q 3)
    - `on.workflow_dispatch` (manual `gh workflow run secret-scan-full.yml` trigger)
    - `permissions: { contents: read, security-events: write }`
    - Single `full-history` job:
      - `actions/checkout@v4` с `fetch-depth: 0` (full git history — Pitfall 10 sequence guard: NEVER use это в PR-trigger workflows; cron-only)
      - `gitleaks/gitleaks-action@v2` с `GITHUB_TOKEN` env + `GITLEAKS_ENABLE_COMMENTS: false` (cron run, no PR к comment on)
      - `trufflesecurity/trufflehog@main` с `base: ""` (no base restriction → scan all) + `head: ${{ github.ref_name }}` + `extra_args: --results=verified,unknown`

    Cite SEC-01 closure rationale в YAML header comment: "Closes SEC-01 partial from Phase 2 (one-time full-history scan was 2026-05-16; this workflow runs it weekly as ongoing protection)."

    Commit message: `feat(ci): add nightly cron full-history secret scan (closes SEC-01, complements backend-ci PR-diff scanner)`.
  </action>
  <verify>
    <automated>actionlint .github/workflows/secret-scan-full.yml &amp;&amp; grep -q 'cron: "0 3 \* \* 0"' .github/workflows/secret-scan-full.yml &amp;&amp; grep -q 'fetch-depth: 0' .github/workflows/secret-scan-full.yml &amp;&amp; grep -q 'workflow_dispatch' .github/workflows/secret-scan-full.yml &amp;&amp; grep -q 'gitleaks-action@v2' .github/workflows/secret-scan-full.yml &amp;&amp; grep -q 'trufflesecurity/trufflehog' .github/workflows/secret-scan-full.yml</automated>
  </verify>
  <acceptance_criteria>
    - `actionlint .github/workflows/secret-scan-full.yml` exits 0
    - Cron expression `0 3 * * 0` verified
    - `fetch-depth: 0` present (full history) — confirms separation from backend-ci.yml fast-path
    - Both gitleaks AND trufflehog steps present (defense-in-depth per RESEARCH §Architecture Pattern 2)
    - `workflow_dispatch` trigger present (manual run capability via `gh workflow run`)
  </acceptance_criteria>
  <done>Cron workflow exists; future Sunday-03:00-UTC run will execute (no live-test required в этой phase — verification = file structure + actionlint).</done>
</task>

<task type="auto">
  <name>Task 3: CREATE `.golangci.yml` — golangci-lint v2 conservative starter config + run-against-existing-code tuning loop</name>
  <files>.golangci.yml</files>
  <read_first>
    - `.planning/phases/04-ci-cd-pipeline/04-RESEARCH.md` §Code Examples §D lines 815-895 (full config body)
    - `.planning/phases/04-ci-cd-pipeline/04-PATTERNS.md` §.golangci.yml section lines 270-281 (tuning workflow)
    - `services/backend/identity/`, `services/backend/pkg/` — sample sources для tuning context if linter flags major false-positive avalanche
  </read_first>
  <action>
    1. Create `.golangci.yml` at REPO ROOT (not под `services/backend/`) per RESEARCH §Code Examples §D verbatim. Key elements:
       - `version: "2"` header (v2 schema mandatory per RESEARCH §State of the Art line 1168)
       - `run.timeout: 5m`, `run.go: "1.25"`, `modules-download-mode: readonly`, `tests: true`
       - `linters.default: none` + explicit-enable list of 15 conservative linters: govet, ineffassign, unused, errcheck, staticcheck, gosimple, bodyclose, errorlint, rowserrcheck, sqlclosecheck, contextcheck, copyloopvar, nilerr, gofmt, goimports, misspell
       - `exclusions.presets:` for comments, common-false-positives, legacy, std-error-handling
       - `exclusions.rules:` for `_test.go` (relax errcheck, gosec, dupl, gocyclo) + generated files
       - `formatters.enable: [gofmt, goimports]`
       - `issues.max-issues-per-linter: 0` + `max-same-issues: 0` + `new: false`
       - `severity.default: error`
       - Deferred-linter comment block (gocyclo, dupl, revive, gocritic, paralleltest, testifylint) для post-baseline tuning

    2. **First tuning pass:** invoke locally (если golangci-lint installed) или wait для CI run в Task 5:
       ```bash
       cd services/backend && golangci-lint run --timeout=5m
       ```
       Expect: некоторое количество false-positives — это normal для conservative starter config. Если avalanche (>100 findings, mostly errcheck on intentional `_, _ = ...` patterns или similar), add к `exclusions.rules:` с rationale comment ("Disabled in v1.0 baseline — generates X false-positive per Y; revisit v1.0.1").

    3. **DO NOT silently disable** linters to "make CI green" — every disabled linter / rule gets a `# Disabled — <rationale>; revisit v1.0.1` comment inline. Iteration loop is expected per RESEARCH Architectural Responsibility Map A4.

    Commit message: `feat(ci): add conservative golangci-lint v2 config (15 linters, doc'd tuning loop)`.
  </action>
  <verify>
    <automated>test -f .golangci.yml &amp;&amp; grep -q 'version: "2"' .golangci.yml &amp;&amp; grep -q 'default: none' .golangci.yml &amp;&amp; grep -qE 'govet|errcheck|staticcheck' .golangci.yml &amp;&amp; (command -v golangci-lint &gt;/dev/null 2>&amp;1 &amp;&amp; (cd services/backend &amp;&amp; golangci-lint run --timeout=5m --new-from-rev=HEAD~1 2>&amp;1 | tail -5 || echo "(local lint had issues; iterate in CI per RESEARCH A4)") || echo "(golangci-lint not installed locally — CI run в Task 5 will validate)")</automated>
  </verify>
  <acceptance_criteria>
    - File `.golangci.yml` exists at REPO ROOT
    - `version: "2"` header present (v2 mandatory)
    - `linters.default: none` present (explicit-enable mode per RESEARCH §Code Examples §D)
    - At least 10 of 15 linters from RESEARCH §Code Examples §D explicitly enabled
    - Если linter has been disabled OR rule added к exclusions.rules: corresponding comment exists с rationale + `revisit v1.0.1` annotation
    - Either local `golangci-lint run` exits 0, OR plan documents "first CI run в Task 5 will validate" (CI-only feedback acceptable per Architectural Responsibility Map A4)
  </acceptance_criteria>
  <done>.golangci.yml committed; first-pass tuning либо complete locally либо deferred к Task 5 CI iteration (allowed per RESEARCH).</done>
</task>

<task type="auto">
  <name>Task 4: CREATE `.trivyignore.yaml` — empty YAML template с rationale-per-CVE + expiry format documented</name>
  <files>.trivyignore.yaml</files>
  <read_first>
    - `.planning/phases/04-ci-cd-pipeline/04-RESEARCH.md` §Code Examples §I lines 1103-1134 (template + process)
    - `.planning/phases/04-ci-cd-pipeline/04-PATTERNS.md` §.trivyignore.yaml section lines 284-301
  </read_first>
  <action>
    Создать `.trivyignore.yaml` at REPO ROOT per RESEARCH §Code Examples §I verbatim. Body is EMPTY arrays + documented process:

    ```yaml
    # .trivyignore.yaml
    # Note: requires `--ignorefile .trivyignore.yaml` flag (does NOT auto-load — RESEARCH §State of the Art line 1171)
    # Trivy version: 0.55+ (YAML ignore-file support)

    vulnerabilities: []
    # Format for new ignores:
    #   - id: CVE-YYYY-NNNNN
    #     statement: <rationale — why this is OK to ignore>
    #     expired_at: YYYY-MM-DD          # MUST set expiry — forces revisit
    #     paths:                          # optional — limit scope
    #       - "usr/local/lib/..."
    #     purls:                          # optional — limit к specific packages
    #       - "pkg:deb/debian/libssl"

    misconfigurations: []
    secrets: []
    licenses: []

    # Process for adding an ignore entry (block-on-fix-unavailable per RESEARCH §Open Q 2):
    # 1. Identify CVE from Trivy CI output.
    # 2. Verify NO fix is available upstream (pkg.go.dev/vuln/CVE-... or CVE database).
    # 3. Verify CVE is irrelevant к our threat model (e.g., CVE в test-only dep,
    #    CVE в base-image we can't upgrade yet).
    # 4. Add entry с rationale + expired_at = +90 days from today.
    # 5. Submit PR с link к upstream fix-tracking issue в commit message.
    # 6. Reviewer (or solo-dev self-review per D-20) verifies rationale soundness.
    # 7. When expired_at hits, CI re-fails → re-evaluate (fix arrived? threat changed?).
    ```

    Empty initially — populated only когда a real CVE forces explicit ignore decision per RESEARCH §Open Q 2 recommendation ("block on unfixed CRITICAL forces every exception к be documented с rationale + expiry").

    Commit message: `feat(ci): add empty .trivyignore.yaml с rationale-per-CVE format (CICD-01 image-scan)`.
  </action>
  <verify>
    <automated>test -f .trivyignore.yaml &amp;&amp; python3 -c "import yaml,sys; d=yaml.safe_load(open('.trivyignore.yaml')); sys.exit(0 if d.get('vulnerabilities')==[] and d.get('secrets')==[] and d.get('misconfigurations')==[] and d.get('licenses')==[] else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.trivyignore.yaml` exists at REPO ROOT
    - YAML parses cleanly via `python3 -c "import yaml; yaml.safe_load(open('.trivyignore.yaml'))"` (exit 0)
    - All 4 arrays (`vulnerabilities`, `misconfigurations`, `secrets`, `licenses`) present + empty initially
    - Format documentation comment present (process for adding entries + expiry mandatory)
    - Reference к `--ignorefile` flag requirement preserved (since YAML format does NOT auto-load per RESEARCH §State of the Art line 1171; backend-ci.yml Trivy step from Task 1 must pass `trivyignores: .trivyignore.yaml` input)
  </acceptance_criteria>
  <done>`.trivyignore.yaml` committed; ready для future CVE-ignore entries with rationale/expiry hygiene enforced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: ITERATE CI on scratch PR branch until ALL jobs green — supervised by user (sequence guard для Plan 04-05)</name>
  <files>(test branch only — no main-line modifications)</files>
  <read_first>
    - `.planning/phases/04-ci-cd-pipeline/04-RESEARCH.md` §Common Pitfalls 1 (branch protection lockout sequence) + 4 (gitleaks license requirement) + 8 (SARIF rate-limit)
    - `.planning/phases/04-ci-cd-pipeline/04-PATTERNS.md` §Anti-Patterns (sequence guard line 605)
    - `.planning/phases/04-ci-cd-pipeline/04-01-SUMMARY.md` (для namespace ownership decision — affects gitleaks license decision)
  </read_first>
  <what-built>4 new/extended files (backend-ci.yml, secret-scan-full.yml, .golangci.yml, .trivyignore.yaml) — all need first-run validation against actual GitHub Actions environment, with iteration on inevitable false-positives.</what-built>
  <how-to-verify>
    1. Создать scratch branch + push к origin (CI triggers on `branches: [main]` push AND PR-к-main pull_request — for scratch testing, open PR):
       ```bash
       git checkout -b chore/04-02-ci-iteration
       git add .github/workflows/backend-ci.yml .github/workflows/secret-scan-full.yml .golangci.yml .trivyignore.yaml
       git commit -m "chore(ci): smoke-run extended backend-ci.yml + scanners (Plan 04-02 Task 5)"
       git push -u origin chore/04-02-ci-iteration
       gh pr create --base main --head chore/04-02-ci-iteration --title "chore(ci): Plan 04-02 backend-ci extension smoke" --body "Iteration branch для CICD-01. Not for merge."
       ```

    2. Watch CI run via `gh pr checks --watch` (или `gh run watch`); expect ~10-15 min for full matrix (9 test entries + 8 docker-build entries + 5 scanners + guard).

    3. **Iterate on failures** (allowed per RESEARCH Pitfall 1 — sequence guard says branch protection comes AFTER first green CI run; failures here are normal):
       - **golangci-lint false-positives:** add к `.golangci.yml` exclusions.rules с rationale (per Task 3 done-criteria — never silently disable).
       - **gosec false-positives:** add `// #nosec G104` annotation inline в Go source с rationale (per RESEARCH Architectural Responsibility Map A4) — OR add к gosec's exclusion list inside the job step.
       - **gitleaks ORG-license failure** (если 04-01-SUMMARY ownership=organization): uncomment `GITLEAKS_LICENSE` line в backend-ci.yml job, then `gh secret set GITLEAKS_LICENSE -b "<license-from-gitleaks.io>"` (free для org via registration).
       - **Trivy CRITICAL без fix:** add к `.trivyignore.yaml` с rationale + expired_at=+90 days (per Task 4 process).
       - **Semgrep false-positives** (rare с p/golang ruleset): adjust ruleset OR add `# nosemgrep: <rule-id>` annotation inline.
       - **`actionlint` schema issues** (unlikely если Task 1 passed local actionlint): fix YAML structure.

    4. Each iteration: amend OR new commit. Repeat `gh pr checks --watch` until all jobs green.

    5. **Acceptance:** `gh pr checks <pr-num>` returns ALL of: test (9 matrix entries), lint, gosec, govulncheck, semgrep, secrets-scan-diff, docker-build (8 matrix entries), no-latest-tag-guard — green.

    6. **Cleanup:** decide whether к (a) merge the iteration PR (lands tuned `.golangci.yml` + `.trivyignore.yaml` к main + closes Plan 04-02), OR (b) squash all iteration commits + cherry-pick к feat/cursona-redesign + delete scratch PR. **Recommended (a)** — first PR merge demonstrates the workflow + creates a "first successful push к main" record для Plan 04-05 sequence guard.

    7. Tell user the iteration is complete + summarize: number of iterations needed, какие exclusions were added к `.golangci.yml`, какие entries (if any) к `.trivyignore.yaml`, gitleaks license decision applied.
  </how-to-verify>
  <acceptance_criteria>
    - `gh pr checks <pr-num> | grep -cE '^(test|lint|gosec|govulncheck|semgrep|secrets-scan-diff|docker-build|no-latest-tag-guard)\s+pass'` returns ≥ 18 (9 test + 8 docker-build + 5 scanners + 1 guard = но minimum 18 distinct check rows; allow для multi-matrix dedup в gh output formatting)
    - `gh pr view <pr-num> --json statusCheckRollup --jq '.statusCheckRollup[].conclusion' | sort -u` returns ONLY `["SUCCESS"]` (no FAILURE, no PENDING)
    - PR merged к main (option a recommended) OR scratch branch cherry-picked к feat/cursona-redesign + PR deleted
    - 04-02-SUMMARY documents: iteration count, exclusions added к `.golangci.yml` (with rationale), entries added к `.trivyignore.yaml` (with rationale + expiry), gitleaks license decision (yes/no per 04-01-SUMMARY ownership)
    - **Sequence guard checkpoint:** "✓ first green CI run achieved; Plan 04-05 branch protection setup may now proceed"
  </acceptance_criteria>
  <resume-signal>Type "all-green PR-#&lt;num&gt; merged" OR "iteration in progress — &lt;current status&gt;"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR-author → GitHub Actions runner | Untrusted PR code runs в `pull_request` context (но NOT `pull_request_target` — Pattern D); `GITHUB_TOKEN` scoped read-only contents. |
| Scanner outputs → GitHub Security tab | SARIF uploads land на GH Security tab; visible к anyone с repo read access. |
| `:latest` literal anywhere в workflows/compose → prod accidentally pulling moving tag | Hard rule + negative-grep guard блокирует merge. |
| backend-ci.yml job `name:` strings → Plan 04-05 branch-protection JSON contexts | Implicit contract — any drift in spelling causes branch-protection lockout (`T-04-LOCKOUT` HIGH). Verbatim pin enforced в Task 1 acceptance loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information disclosure | Scanner findings → real CVE slips through false-positive avalanche | mitigate | D-10 forces HIGH+CRITICAL blocks merge; `.trivyignore.yaml` requires rationale + expiry (90d) per Task 4; iteration loop в Task 5 forces explicit per-rule decisions (never silent disable). MEDIUM. |
| T-04-PIV-LIC | Availability (silent CI failure) | `gitleaks-action@v2` без `GITLEAKS_LICENSE` для org repos | mitigate | Task 1 includes commented license env var + inline note tying к 04-01-SUMMARY ownership decision. Task 5 iteration loop explicitly resolves it (uncomment + add secret OR confirm personal account = N/A). MEDIUM. |
| T-04-LATEST | Tampering | `:latest` tag accidentally introduced в backend-ci OR backend-cd OR compose | mitigate | `no-latest-tag-guard` job (Task 1) — negative-grep с `grep -v '^#'` filter (prevents comment-line self-invalidation per critical_rules planner-antipatterns). Exit-1 on match. Confirmed via Task 5 CI run green. HIGH. |
| T-04-SOPS-CI | Information disclosure | SOPS-decrypt accidentally added к CI workflow (D-22 violation) | mitigate | Plan 04-02 deliberately introduces ZERO SOPS-decrypt calls в new workflow files. Future-proof guard recommended (RESEARCH Pitfall 7): `! grep -rE 'sops -d\|SOPS_AGE_KEY\|\.secrets/' .github/workflows/` should return empty — manual verification в Task 5 after iteration converges. HIGH. |
| T-04-LOCKOUT-UP | Availability | Job-name spelling drift between backend-ci.yml (producer) and Plan 04-05 setup-branch-protection.sh (consumer) → branch protection requires nonexistent check context → permanent lockout | mitigate | Task 1 acceptance includes verbatim grep loop over all 8 pinned strings; `<interfaces>` block declares them as immutable contract. Plan 04-05 Task 1 cross-checks the same 8 strings (defense-in-depth). HIGH. |

**Block-on-high gate:** Three HIGH-severity threats (T-04-LATEST, T-04-SOPS-CI, T-04-LOCKOUT-UP). All mitigated via explicit guard mechanisms. `block_on: high` satisfied.
</threat_model>

<must_haves>
  truths:
    - "D-07: backend-ci.yml = three-workflow architecture's PR-time leg (test + lint + scanners + verify-build, no push)"
    - "D-08: test matrix covers 8 services + pkg = 9 entries (currently 3); docker-build matrix covers 8 services"
    - "D-09: paths filter restricts CI к backend/workflow/Makefile/lint-configs — saves CI minutes (~80% of repo activity is non-backend)"
    - "D-10: HIGH+CRITICAL findings block merge (per scanner — gosec/govulncheck/semgrep/trivy); gitleaks/trufflehog block on any finding (no severity tier)"
    - "D-11: MEDIUM/LOW = advisory only (surfaced в Security tab + GH-Actions step summary, не block merge)"
    - "D-12: golangci-lint v2 supersedes existing `go vet` baseline (vet still part of golangci-lint default rules)"
    - "D-22: NO SOPS-decrypt в CI — guards against compromised CI environment exfiltrating prod secrets"
    - "D-23: only secret in repo settings = `GITHUB_TOKEN` (auto-provisioned, scoped, short-lived); optionally `GITLEAKS_LICENSE` for org repos"
    - "RESEARCH §State of the Art: `golangci-lint v2` config syntax (`version: \"2\"` header) — v1 schema deprecated"
    - "RESEARCH §Open Q 2 applied: Trivy blocks UNFIXED CRITICAL (`ignore-unfixed: false`) — forces explicit `.trivyignore.yaml` entry с rationale-per-CVE"
    - "RESEARCH §Open Q 3 applied: `no-latest-tag-guard` is standalone job (explicit visibility) — not inline в other jobs"
    - "RESEARCH Pitfall 1 sequence guard: Plan 04-05 branch protection DEPENDS ON Plan 04-02 first-green-CI-run"
    - "RESEARCH Pitfall 4 applied: GITLEAKS_LICENSE env var present commented с decision tied к 04-01-SUMMARY ownership"
    - "RESEARCH Pitfall 10 applied: full-history secret scan = separate cron workflow (NEVER on every PR)"
    - "RESEARCH Pitfall 6 applied: negative-grep uses `grep -v '^#'` filter (prevents header-comment self-invalidation per critical_rules)"
    - "SEC-01 closure: Phase 2 left CI invocation deferred; this plan wires gitleaks + trufflehog into ongoing CI"
    - "backend-ci.yml 8 jobs have EXACT name: strings pinned to match the verbatim list consumed by Plan 04-05 branch-protection JSON. Executor MUST NOT vary spelling. Cross-checked в Task 1 verify (grep -qF loop) AND defense-in-depth cross-check в Plan 04-05 Task 1."
    - "Trivy image-ref tag MUST match build-step `tags:` field: both use `:ci-${{ github.sha }}` prefix (CI verify-builds; distinct from CD `:${{ github.sha }}` published images per Plan 04-03a hybrid mode)."
    - "Task 1 verify uses `! grep -q 'attest-build-provenance' .github/workflows/backend-ci.yml` (negative-grep fail-closed) — attest-build-provenance belongs к backend-cd.yml only; presence here = wrong, exits 1."
  behaviors:
    - "`actionlint .github/workflows/*.yml` exits 0"
    - "`gh pr checks <num>` shows ALL of test(×9), lint, gosec, govulncheck, semgrep, secrets-scan-diff, docker-build(×8), no-latest-tag-guard green"
    - "First green CI run committed к history (record для Plan 04-05 sequence guard)"
    - "Trivy `--ignorefile .trivyignore.yaml` invocation present в backend-ci.yml docker-build job (Task 1 acceptance)"
    - "Cron full-history scan workflow exists + `workflow_dispatch` trigger lets dev manually invoke for verification"
    - "All 8 backend-ci.yml job `name:` fields match verbatim contract strings (grep -qF loop в Task 1 verify exits 0)"
    - "backend-ci.yml docker-build step tags image as `ghcr.io/ismaill01/${{ matrix.service }}:ci-${{ github.sha }}` — Trivy image-ref resolves к real built image"
  forbidden:
    - "NO `:latest` tag anywhere в `.github/workflows/*.yml` OR `services/backend/docker-compose.prod.yml` (negative-grep guard enforces)"
    - "NO SOPS-decrypt commands (`sops -d`, `SOPS_AGE_KEY` env, `.secrets/*` paths) в any workflow file (D-22)"
    - "NO `pull_request_target` triggers (RESEARCH §Security Domain — workflow injection vector)"
    - "NO `@latest` OR `@main` action version pins для security-critical actions (RESEARCH §Security Domain — supply chain)"
    - "NO `enforce_admins: true` setup attempted (Plan 04-05 owns branch protection; this plan provides green run only)"
    - "NO branch protection enable attempt в this plan (sequence guard — RESEARCH Pitfall 1)"
    - "NO silent linter/scanner disable — every exclusion has inline `# Disabled — <rationale>; revisit v1.0.1` annotation"
    - "NO variation in spelling/casing/punctuation/em-dash of the 8 pinned job `name:` strings — Plan 04-05 branch protection requires verbatim match"
    - "NO `name: Test (Go ${{ matrix.go-version }})` template (matrix has no go-version dimension — would interpolate empty → `Test (Go )` literal — breaks contract)"
    - "NO build-step `tags:` field без `ci-` prefix on the SHA (Trivy step references `:ci-${{ github.sha }}` — mismatch = silent missing-image error)"
    - "NO `&& echo \"OK\" || ...` chains in negative-grep verify steps that fail open on grep-match — use `! grep -q ...` for fail-closed semantics"
</must_haves>

<verification>
- `actionlint .github/workflows/backend-ci.yml .github/workflows/secret-scan-full.yml` exits 0
- `gh pr checks <scratch-PR-num>` returns all green (zero FAILURE, zero PENDING after iteration converges)
- `grep -cE '^\s*(test|lint|gosec|govulncheck|semgrep|secrets-scan-diff|docker-build|no-latest-tag-guard):' .github/workflows/backend-ci.yml` returns ≥8 distinct job names
- `! grep -rE ':latest["\x27]?\s*$' .github/workflows/*.yml services/backend/docker-compose.prod.yml | grep -v '^#'` exits 0 (no `:latest` in non-comment lines anywhere)
- `! grep -rE 'sops -d|SOPS_AGE_KEY|\.secrets/' .github/workflows/*.yml` exits 0 (D-22 compliance)
- **JOB-NAME CONTRACT (verbatim grep loop):**
  ```bash
  for NAME in "Test (Go 1.25)" "Lint (golangci-lint v2)" "SAST (gosec)" "Vuln (govulncheck)" "SAST (semgrep)" "Secrets (gitleaks + trufflehog — PR diff)" "Docker build (no push, verify)" "Guard (no :latest)"; do
    grep -qF "name: $NAME" .github/workflows/backend-ci.yml || { echo "FAIL: missing job name: $NAME"; exit 1; }
  done
  echo "✓ All 8 backend-ci job names pinned"
  ```
- **Trivy `ci-` tag prefix:** `grep -qF 'tags: ghcr.io/ismaill01/${{ matrix.service }}:ci-${{ github.sha }}' .github/workflows/backend-ci.yml` exits 0
- **Fail-closed negative grep:** `! grep -q 'attest-build-provenance' .github/workflows/backend-ci.yml` exits 0
- 04-02-SUMMARY records: green PR URL, iteration count, exclusions added к `.golangci.yml`/`.trivyignore.yaml` с rationale, gitleaks license decision applied per 04-01-SUMMARY
</verification>

<success_criteria>
1. CICD-01 acceptance: PR-trigger backend-ci.yml runs test/race + golangci-lint + gosec + semgrep + govulncheck + Docker-build matrix + Trivy image scan — все 5 scanner classes + lint + build + test gates working as designed.
2. SEC-01 closure: gitleaks + trufflehog wired в CI (PR diff scan + nightly cron full-history) — Phase 2 deferred item closes.
3. 4 new/extended files committed к main: backend-ci.yml (EXTEND), secret-scan-full.yml (NEW), .golangci.yml (NEW), .trivyignore.yaml (NEW).
4. First-green-CI-run record present (Plan 04-05 sequence-guard prerequisite satisfied).
5. 8 verbatim job `name:` strings pinned + cross-validated against Plan 04-05 consumer.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ci-cd-pipeline/04-02-SUMMARY.md` recording:
- Final green PR URL + commit SHA (first record для Plan 04-05 sequence guard)
- Iteration count (= number of pushes-к-PR before all-green)
- Each `.golangci.yml` exclusion added during iteration + rationale + revisit-tag (v1.0.1)
- Each `.trivyignore.yaml` entry added during iteration (with CVE-id + rationale + expired_at + upstream-fix-tracking-link)
- Gitleaks license decision: confirmed personal account (no license needed) OR confirmed organization (license added via `gh secret set GITLEAKS_LICENSE` + uncommented в backend-ci.yml)
- Any false-positive patterns discovered worth noting для Plan 04-03a (similar scanner behavior likely в CD workflow)
- SEC-01 closure confirmation + reference к Phase 2 commit `28acb2d..ac2ebd5` для traceability
- Confirmation: 8 pinned job `name:` strings present verbatim (grep loop output captured); upstream half of Plan 04-05 branch-protection contract satisfied
</output>
</output>
